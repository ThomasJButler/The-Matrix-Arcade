# MAGIC DOC: [Implementation Plan.md - The Matrix Arcade]

This file is auto-generated and updated by Ralph during planning and building loops.

> **Completed work (R1–R50) is archived in [`COMPLETED_WORK.md`](COMPLETED_WORK.md).**
> This live plan tracks only open / remaining work. Status snapshot, finished phases, and resolved bugs live in the archive.

## Status: **R84 open — R84.CI iteration 5 (SR match-point announcements for Vortex Pong) shipped 2026-04-19 night.** Closes the three-game a11y-priority-2 lane: CI-2 shipped SR score-milestones for Matrix Bird, CI-4 extended the pattern to Matrix Snake, CI-5 now plugs Vortex Pong via a dedicated `matchPoint` event type rather than overloading `scoreMilestone`. Why a new type: Pong's scoring is win-condition-based (first to `WIN_SCORE=10`), not cumulative-threshold like Bird/Snake — "match point" is the semantic state of either side reaching `WIN_SCORE - 1` (= 9, one away from winning), and that maps to `{ side: 'player' | 'opponent' }` rather than `{ value: number }`. Keeping the payload shapes distinct means the TypeScript type system rejects a Bird scene from accidentally emitting a match-point event and vice-versa. **Three changes ship together**: (1) new `matchPoint` entry in `GameEventType` + `MatchPointEventData` type; (2) new `buildMatchPointAnnouncement` helper in `a11y.ts` returning `'Match point.'` / `'Opponent match point.'` / `''` on malformed payload (same defensive shape as the other two builders); (3) new `case 'matchPoint'` branch in `PhaserGame.tsx:handleGameEvent` that routes the builder output through the same nonce-bumping `setSrAnnouncement` state as `scoreMilestone`. Pong scene changes: per-side flag state `matchPointAnnounced: { player, opponent }` (both sides announce independently, so 9-9 tight-finish scenarios get both tension beats); `resetState` zeros both flags (R-restart re-announces, matching Bird/Snake milestone reset semantics); new `checkMatchPointAnnouncement(side)` helper enforces the `score === WIN_SCORE - 1` equality (exact crossing, not `>=` band — the flag is the safety net not the primary guard); two callsites in `checkGoals` — `'opponent'` right after the `aiScore += 1` scoring branch, `'player'` right after `playerScore += 1` + FIRST_POINT achievement. **Deliberately NOT announcing halfway-at-5 or first-blood**: the `score` SFX already beats every goal for sighted players and FIRST_POINT announces via the achievement-toast system, so the only a11y gap was the match-point tension moment where sighted players read "one away from winning" from the score digit flipping to 9 instantly; halfway or first-blood would add noise without closing a gap. Phrasing mirrors tennis/pong sports-announcer vernacular ("Match point.", "Opponent match point.") so it reads naturally through a screen reader; `'opponent'` literal rather than `'ai'` leaves the announcement game-agnostic for a hypothetical future 2-player Pong. Gates: tsc exit 0, eslint 0 errors + 4 pre-existing warnings unchanged (`KeyPanel.tsx:123`, `useSoundSystem.ts:745+885`, `PhaserGame.tsx:288` — note the `PhaserGame.tsx` warning line shifted 275→288 because the new `case 'matchPoint'` block pushed the existing hook-deps warning down; same warning, no new lint debt), `npm run build` green 7.64s (292 precache entries, 11916.06 KiB), full vitest sweep **2,433 pass + 2 todo / 49 files** (was 2,419 post-CI-4, +14 = 5 PhaserGame builder specs + 9 Pong scene specs under `describe('R84.CI-5 — Match point SR event emission')`). Pong scene tests 220 green (was 211). PhaserGame wrapper 16 green (was 11). Bird scene 223 unchanged; Snake scene 269 unchanged. The 9 Pong specs pin: player-side threshold emission via the end-to-end `checkGoals` path, opponent-side mirror, pre-threshold no-emit at score 5, flag-guard repeat suppression, side-independence (both sides reach 9 in same run → both announcements fire), past-threshold no-emit at 9→10 win transition (gameOver owns that beat), `resetState` clears both flags, direct helper idempotence at the exact threshold, and direct helper off-threshold no-op at scores 8 and 10. The filter pattern `mock.calls.filter(c[0]?.type === 'matchPoint')` adopted from CI-2/CI-4 cleanly separates milestone emits from the `score`/`achievement`/`gameOver` events `checkGoals` also fires per tick. **Three-game a11y-priority-2 lane now fully shipped.** Next CI burns candidate: focus-visible ring refinement on Pong/Snake/Bird inner controls (CI priority 1 residue — the last remaining a11y lane); performance micro-opts (CI priority 2 — object-pool audit for Bird pipes, Phaser texture atlases); attract-mode integration (CI priority 3); juice audits (CI priority 4); sound-mix balance (CI priority 5); visual detailing (CI priority 6). R84.CI stays `[ ]` by design (Tom ticks post-playtest); Tom-only terminator phrase still to be written. Prior R84.CI-4 (Snake SR score-milestone) Status content archived into the per-task checkbox body below. Extends CI-2's Bird-only `scoreMilestone` bridge event pattern to Snake's three score-mutation paths (`eatFood`, reverse +50 pickup, glitch +100 pickup) via a single `checkScoreMilestones()` helper called after each `this.score +=`. Thresholds are `[50, 100, 250, 500]` — the first three match Bird for cross-game AT consistency (so a player running all three games hears the same verbal cadence), plus a 500-pt landmark that sits beyond Snake's first speed-up wave and marks the longest-run beat Tom flagged during V2 playtest. Unlike CI-2, Snake deliberately does NOT fire a new COLLECTIBLE SFX on milestone crossings: the existing `levelUp` stinger already plays at every `POINTS_PER_SPEED_UP` (50-pt) boundary, so sighted players already hear the beat; layering a second cue on top would crowd the mix. AT users now get parity via the shared PhaserGame SR live region — the wrapper's `case 'scoreMilestone'` branch consumes the event and writes `Score milestone 50.` / `100.` / `250.` / `500.` into the live region via CI-2's existing `buildScoreMilestoneAnnouncement` helper. The helper is wired into all three score-bump paths specifically to cover the glitch-pickup edge case where a single +100 bonus can cross two thresholds simultaneously (45 → 145 pts fires both `50` and `100` in the same frame — pinned by a dedicated spec). `resetState()` clears `milestonesHit` so a restart re-announces the early cues, matching Bird's CI-2 reset contract. Gates: tsc exit 0, eslint 0 errors + 4 pre-existing warnings unchanged (`KeyPanel.tsx:123`, `useSoundSystem.ts:745+885`, `PhaserGame.tsx:275`), `npm run build` green 11.63s (292 precache entries, 11915 KiB), full vitest sweep **2,419 pass + 2 todo / 49 files** (was 2,410 post-CI-3, +9 Snake CI-4 specs under `describe('R84.CI-4 — Score milestone SR event emission')` pinning threshold-value-not-score semantics, freshly-crossed vs. already-hit guard, two-in-one-call crossing, 250/500 landmarks, no-emit beyond 500, reset semantics, and reverse/glitch bonus paths). Snake-scoped scene tests 269 green (was 260). CI-1 gated Bird camera shake + flash; CI-2 added Bird SR score-milestone announcements; CI-3 extended the Bird a11y contract to object-scale tweens; CI-4 (this iteration) extends SR milestone coverage to the second game. R84.CI stays `[ ]` by design (Tom ticks post-playtest); Tom-only terminator phrase still to be written.** R84.BL outcome: ran `npx playwright test e2e/visual/ --project=chromium --update-snapshots` against the live darwin dev server at 1280×800 — **19/19 specs passed, zero baseline PNGs modified**, `git status` clean post-run. Interpreted as "snapshots already synchronised with current render output" — the 3-game polish (Pong atmosphere, Snake density/scanlines/yellow-wall offset, Bird pipes+parallax) stayed inside Playwright's tolerance envelope (`playwright.config.ts:108–117`: `maxDiffPixelRatio: 0.2`, `threshold: 0.5`). Worth noting **why this is the right no-op outcome** rather than a gap: (a) visual specs target landing/portal/modals/settings/achievements/CTRL-S — no direct Pong or Bird in-game baselines exist, so Pong vortex atmosphere + Bird pipe/parallax rework had nothing to invalidate; (b) Snake `portal-in-play.png` does capture live Snake gameplay, but the generous tolerance + 600ms post-morph settle (`ui/portal.spec.ts:27`) absorbed Stream C's density/scanline/yellow-wall shifts; (c) landing card thumbnails are CSS-rendered static assets, so R83/R84 display-name renames already landed in R83.G6 baselines. **Linux baselines intentionally NOT regenerated this iteration** — Docker daemon was offline (`docker info` → "Cannot connect to Docker daemon at unix:///Users/tombutler/.docker/run/docker.sock"), so `chromium-linux.png` stays on R79.1's shipped set. Given darwin showed zero drift at the shared tolerance, linux drift at that same tolerance is extremely unlikely; CI's linux-in-Docker run catches any actual regression, and the fallback path is `npm run test:e2e:docker -- --update-snapshots` once the daemon is back. Gates post-regen: tsc exit 0, eslint 0 errors + 4 pre-existing warnings unchanged (R84.S10 carried them forward), no source diff. **R84 stream scoreboard**: Stream A V1/V2/V3 `[x]`; Stream B P1–P12 `[x]`; Stream C S1–S12 minus S7 `[x]` (11/12; S7 boss-snake `[DEFER]` to R85 per original brief); Stream D B1–B12 `[x]` (12/12); Stream U UP1+UP2 `[x]`; **Stream E R84.BL `[x]` (this iteration); R84.CI stays `[ ]` by design (R82.13 pattern — Tom ticks after playtest).** Next load-bearing work is either Tom's hand-playtest + terminator-phrase write on Status, OR Ralph burns remaining loop iterations on the R84.CI priority list (a11y refinement → perf micro-opts → attract-mode integration → juice audits → sound-mix balance → visual detailing). Prior Status content (R84.S10 Snake coverage-refresh narrative, R84.B12 Bird coverage-refresh narrative) archived into the per-task checkbox bodies below.

> **Archived phases** (full detail in [`COMPLETED_WORK.md`](COMPLETED_WORK.md)): R1–R75 (various), R76 final polish, R77 retro scoreboard, R78 assets + infrastructure, R79 residue closeout, R80 CTRL-S Phaser rewrite, R81 juice polish + repo trim, R82 iPod Classic portal redesign, R83 global polish + CTRL-S atmospheric rewrite.

<!-- ARCHIVE-COLLAPSE-START: legacy Status-line paragraphs + R78.x/R76/R75/R73/R72/R71 inline notes. Originals preserved via git history; full semantic equivalents live in COMPLETED_WORK.md. -->

<details>
<summary>Legacy Status archive (expand for historical context)</summary>

Old R83 Round 2 / Round 3 Status narrative, R82.13 accessibility iteration notes, R78.1–R78.7 inline completion blurbs, R76 pre-plan findings, R75 planning findings, R73/R74 delta notes, R72 pattern change, R71 screenshot audit — all moved to [`COMPLETED_WORK.md`](COMPLETED_WORK.md) for their respective phases. The raw paragraphs used to live here; kept only as a one-line pointer so the top of the plan stays navigable.
</details>


## Reference Docs

- [`PLAYWRIGHT_BULKUP.md`](PLAYWRIGHT_BULKUP.md) — R50 test-infra overhaul (43/43 specs, ~91 baselines, test-mode seam, seeded RNG, DOM/Phaser ready markers). Source of the Phase 6 playwright residue below.
- ~~`rebuildingoldgames/`~~ — Deleted in R81.2. 13 pre-rebuild scoping docs (0 items ticked, all resolved by R76–R80 Phaser rebuilds). Summary archived in [`COMPLETED_WORK.md § Rebuildingoldgames Archive`](COMPLETED_WORK.md#rebuildingoldgames-archive-r81).
- [`.claude/skills/phaser-gamedev/SKILL.md`](.claude/skills/phaser-gamedev/SKILL.md) — Phaser 3 gamedev skill with mandatory pre-work protocol (spritesheet measurement, arcade physics, tilemaps, performance).
- [`specs/phaser-games.md`](specs/phaser-games.md) — specs for all 5 Phaser game rebuilds + Cloud Jumper.
- [`specs/game-architecture.md`](specs/game-architecture.md) — mandatory interfaces, hooks, state machine, performance budgets.
- [`specs/ux-guidelines.md`](specs/ux-guidelines.md) — palette, typography, screen flows, accessibility, controls.

---

## Priority Legend

- **P0**: Critical/blocking — games unplayable, users cannot interact
- **P1**: High — bugs that degrade experience, failing tests, memory leaks
- **P2**: Medium — infrastructure, UX improvements, code quality
- **P3**: Low — rebuilds, new features, polish, assets

---

## Open Work

> **R76, R77, R78, R79 fully shipped and archived.** See [`COMPLETED_WORK.md § R76`](COMPLETED_WORK.md#r76--final-polish-phase), [`§ R77`](COMPLETED_WORK.md#r77--retro-arcade-scoreboard), [`§ R78`](COMPLETED_WORK.md#r78--assets--infrastructure), and [`§ R79`](COMPLETED_WORK.md#r79--r78-residue-closeout). All R62–R75 resolved items also archived.

### Tests + Gates Status (Post-R79)

- Unit tests: **1,896 pass** (up from 1,867 at end of R78)
- E2E: **73 specs pass** (up from 69)
- TypeScript: clean
- Lint: 0 errors, 14 intentional warnings (6 in CTRL-S `DEFERRED-CTRLS-DEDICATED-PHASE`, rest in circular hook deps / mount-only effects / context colocation)
- PWA + build: green
- Docker linux baselines: shipped R79.1 (daemon now available)
- Manual testing aid: see `MANUAL_TESTING_CHECKLIST.md` for per-game hand-run QA — NOT for Ralph loop, Tom's personal tool

---

## R83 — Global Polish + CTRL-S Rewrite — **AWAITING TOM'S SIGN-OFF**

All 38 sub-bullets shipped across 3 rounds. Full detail archived at [`COMPLETED_WORK.md § R83`](COMPLETED_WORK.md#r83--global-polish--ctrl-s-rewrite-2026-04-19).

- [ ] **R83.CTRLS** (umbrella) — stays `[ ]` until Tom hand-plays CTRL-S from JACK IN → CTRL-S climax under Round 3 polish + Round 2 dread atmosphere + new ASCII library, without hitting a blocking bug. Tom-only tick.
- **Terminator phrase**: Tom manually writes `R83 COMPLETE — global polish + CTRL-S rewrite shipped` into the Status line at top. Ralph cannot self-write this.
- **If Tom's playthrough surfaces blocking bugs**: log under `### R84 Discovered Work` or add R83 Round 4 sub-bullets `.29+` (scope-permitting). Small enough issues fold into R84 continuous-improvement.

---

## R84 — 3-Game Polish + Verification — **CURRENT ACTIVE PHASE** (LARGE — ~38 tasks, 50-loop cap)

> **Scope source**: `manual-testing-sessions/MANUAL_TESTING_CHECKLIST_{Vortex_Pong,Snake_Classic,Matrix_Cloud}.md`. Every task below traces back to an unchecked Known-Issue line OR a prose note in one of those 3 docs. When Ralph picks a task, re-read the corresponding doc first — Tom's colloquial notes have reproduction detail the terse task summary here omits.
>
> **Why this phase exists**: R83 shipped per-game polish umbrellas S1/V1/B1 but the Known Issues checkboxes in the 3 testing docs were never re-ticked post-Ralph-loop. Plus Tom's prose notes contain additional scope (Pong scoring-model overhaul, Snake yellow-wall spacing, Bird full display-name audit, performance concerns) that wasn't inside the Round 1 umbrella tasks. R84 = verify + extend.

### R84 Ordering Rule (IMPORTANT for Ralph)

Pick tasks in strict stream order, numerical within each stream. Do NOT jump ahead:

1. **Stream A — Verification passes (R84.V1 → V3)**: play each of Vortex Pong / Matrix Snake / Matrix Bird end-to-end against the Known Issues checklist. Tick each doc line verified working; log any R83.S1/V1/B1 shipped item that ISN'T actually working under `### R84 Discovered Work`. These 3 iterations set the baseline for Streams B-D.
2. **Stream B — Vortex Pong polish (R84.P1 → P12)**: only after R84.V1 is `[x]`.
3. **Stream C — Matrix Snake polish (R84.S1 → S10)**: only after R84.V2 is `[x]`.
4. **Stream D — Matrix Bird polish (R84.B1 → B12)**: only after R84.V3 is `[x]`.
5. **Stream U — Upstream + live-retest (R84.UP1, UP2)**: added 2026-04-19 post Stream A audit. **Pickable any time after Stream A** (parallel-safe with B/C/D) — UP1 hunts cross-game upstream GamePortal/App for duplicate pause-overlay source, UP2 live-retests the 5 likely-stale complaints Ralph flagged (D1/D2/D5/D10/D12). Front-loading UP2 is valuable because it un-sticks tasks in Streams B/C/D that might already be fixed.
6. **Stream E — Baselines + continuous improvement (R84.BL, R84.CI)**: only after B+C+D+U all `[x]`. Regen visual baselines, then burn remaining iterations on CI polish bucket.

**Cross-stream rule**: globals from R83 (G1 mute, G2 click-to-play, G3 pause overlays, G5 Matrix launcher, G6 trophy IDs) MUST be verified across all 3 games during Stream A. If any global regressed, log + fix inside Stream A's iteration.

### R84 Guardrails

- **CTRL-S OFF-LIMITS**. Tom's reviewable post-R83, no further polish. If Ralph is tempted to touch `src/components/games/phaser/CtrlSWorld/`, STOP.
- **8 untested games OFF-LIMITS for per-game polish** (Invaders, Metris, Frogger, NeoJump, AgentChase, Rhythm, CloudJumper, CodeBreaker). Shared-infrastructure changes that inherit via BaseScene/useSoundSystem/GamePortal are fine. Per-game logic/content edits = R85.
- **iPod portal (R82) locked** — no restructuring. Trophy ID fixes are bug-only.
- **R83 tests must stay green** (~1,927 unit across 45 files). Any red gate blocks commit.
- **UK English + procedural audio only** (no new audio files). `MATRIX_COLORS` constants, no raw hex.
- **One task per commit**: `R84.V2: Snake verification pass`, `R84.P3: Pong 5s countdown`, etc.
- **Visual baselines regen separately** under `R84.BL`.

### R84 Task List

**Stream A — Verification (R84.V1 → V3, ~3 iterations):**

- [x] **R84.V1 [P1]** Vortex Pong verification pass *(R84.V1 — Ralph static audit 2026-04-19; all 5 R83.V1 Pong-specific fixes code-verified LANDED: AI predictive tracking `GameScene.ts:308–365`, keyboard hold `268–297`, goal-flash dim + reduced-motion `862–875`, `stopAllAudio()` on R-restart `255,906–911`, paddle-hit trail `473,497,881–899`. 5-sec countdown also present `GameScene.ts:121` — R84.P1 prerequisite met, P1 itself effectively auto-satisfied pending live confirmation. Globals G1/G2/G3 flagged for live re-test under D1–D3 (code is clean post R83 commits; Tom's 2026-04-19 testing-doc notes may predate fix commits `6540b52`). Testing doc `MANUAL_TESTING_CHECKLIST_Vortex_Pong.md` updated with per-item evidence table.)*
- [x] **R84.V2 [P1]** Matrix Snake verification pass *(R84.V2 — Ralph static audit 2026-04-19 night; 4 of 5 R83.S1 items code-verified LANDED: apple shrunk to 0.75 cell `GameScene.ts:426–430`, BGM volume 0.7 `line 99`, Matrix atmosphere triple-stack (rain density 10→14 `line 79`, scanline overlay `787–799`, chromatic aberration flash `750–779`), power-up legend fade-in/out `805–843`. **Rename partial**: registry + MenuScene title renamed, but ASCII block-letter title `lib/asciiArt.ts:62` still spells `SNAKE / CLASSIC` (visible via `GamePortal.tsx:658`) and achievement labels in `useSaveSystem.ts:148–154` still say 'Snake Classic' — logged D6 + D7. Yellow wall asymmetry root-caused via static math: `GRID_OFFSET_Y=20` + walls at grid-row `-1`/`GRID_ROWS` yields top=4px margin, bottom=44px margin on 400px canvas — matches Tom's complaint, fix belongs to R84.S1 (set `GRID_OFFSET_Y=40`). Globals G1/G2/G3 stay flagged cross-game (code is symmetric; live re-test required). Testing doc `MANUAL_TESTING_CHECKLIST_Snake_Classic.md` updated with per-item evidence table.)*
- [x] **R84.V3 [P1]** Matrix Bird verification pass *(R84.V3 — Ralph static audit 2026-04-19 night; **all 7 R83.B1 items code-verified LANDED** with no rename gaps (unlike Snake V2). Evidence: display-name rename spans registry `gameRegistry.ts:53`, ASCII title `asciiArt.ts:64` (`'MATRIX', 'BIRD'`), `SaveLoadManager.tsx:89`, `Scoreboard.tsx:13`, `AttractMode.tsx:14`, all 8 achievement rows `useSaveSystem.ts:165–174`; ground-death via dedicated `handleGroundDeath()` path `GameScene.ts:680–688` (skips shield-consume bounce); 5s pause countdown via `resumeGame()` override `GameScene.ts:292–296`; jump SFX is procedural triangle pluck 800→400Hz over 80ms `useSoundSystem.ts:60–74` — NOT the `sfx_landing.mp3` Tom flagged; slow-powerup jump scaling `impulseScale = 0.6 × JUMP_VELOCITY` at `GameScene.ts:265–266`; scene-local `SFX_VOLUME_SCALE = 0.75` via `playSound` override `GameScene.ts:89, 281–283` — applies to every SFX callsite without touching other 11 games; high-score lifted before `reportScore()` at `GameScene.ts:516–517` fixing the iPod trophy staleness (R83.G6). Registry ID stays `matrix-cloud` by design for storage stability — acknowledged in `GameHighScores.test.tsx:115–118`. Testing doc `MANUAL_TESTING_CHECKLIST_Matrix_Cloud.md` updated with per-item evidence table.)*

**Stream B — Vortex Pong polish (R84.P1 → P12, ~12 iterations):**

- [x] **R84.P1 [P2, DEMOTED]** 5-second countdown after MenuScene *(R84.P1 — Ralph Playwright-drove Pong at 1280×800 on 2026-04-19: clicked Vortex Pong landing card → iPod PLAY → Pong MenuScene reached clean (no click-to-play overlay, R83.G5 reveal worked) → pressed ENTER → GameScene fired `startCountdown(5)` at `GameScene.ts:121`. Countdown digit "5" rendered centre-canvas at T+0.5s, "4" at T+2.5s (per-second tick cadence from `BaseScene.tickCountdownStep` at `BaseScene.ts:216–258` confirmed), gameplay unfrozen at T+~6s with ball-in-play + AI paddle intercept + power-up spawn. `update()` early-return under `isCountingDown` at `GameScene.ts:126` confirmed — paddles, pickups and ball all visible behind the countdown digit but frozen until the tween completes. Evidence screenshots `.playwright-mcp/r84-p1-{01-portal,02-menuscene,03-countdown-5,04-countdown-3,05-gameplay-post-countdown}.png`. Pong testing doc lines 41-42 both ticked. D-P1 closed — no scene code change needed.)*
- [x] **R84.P2 [P0]** Scoring model overhaul *(R84.P2 — shipped 2026-04-19. Match score is now tight classic-Pong 1-per-goal (stripped scoreMultiplier doubling + combo bonus additive from `checkGoals`). New `multiBallsTriggered` counter increments on each multi_ball power-up activation. New `computeHighScore()` method at `GameScene.ts:561–579` implements Tom's formula — `max(0, playerScore - aiScore) × 100 + powerUpsCollected × 50 + multiBallsTriggered × 200 + maxRally × 10 + (playerScore ≥ WIN_SCORE ? 500 : 0)`. Formula commented with worked examples (10-4 win=1670, 10-0 flawless=2500, 4-10 loss=210) for Tom tuning. Both win/loss branches of `checkWinCondition` now submit the weighted score via `reportScore()` + as the `gameOver()` `score` + `level` args (HighScoreEntryScene thus records the composite). R-restart mid-match also reports computed High Score so accrued rally/power-up bonuses aren't lost. Game-over stats panel now also surfaces Multi-balls count. Tests: stripped 3 obsolete multiplier/combo tests, added 13 new R84.P2 tests covering all formula components + counter increments + reportScore wiring + session-best persistence. Suite 1,942 pass (up from 1,927). Gates all green — tsc, lint, vitest, build.)*
- [x] **R84.P3 [P1]** AI difficulty second-pass *(R84.P3 — shipped 2026-04-19 night. Three tiers (Easy/Normal/Hard) scale four AI params together via `DIFFICULTY_TIERS` map in `config.ts:94–108`: `trackingMultiplier` (0.60/1.00/1.40 → baseTracking 2.4/4.0/5.6), `maxSpeedFactor` (0.75/0.95/1.15 → paddle top-speed 360/456/552 px·s⁻¹), `errorMultiplier` (2.5/1.0/0.3 → prediction-error amplitude scales ±36/±14/±4 px), `outgoingTrackingFactor` (0.25/0.45/0.60 → laziness when ball recedes). **Normal preserves R83.V1a numbers exactly** (multipliers all 1.0) so existing behaviour/tests keep passing. **Persistence**: `localStorage` key `matrixArcade.vortexPong.difficulty`, read/write via `readStoredDifficulty`/`writeStoredDifficulty` in config — both guarded against `localStorage` exceptions (private-browsing safe). `MenuScene` adds a cycling `DIFFICULTY: NORMAL` button at y=0.70 (fits cleanly in the 0.63–0.75 gap between instruction band and START), exposes `cycleDifficulty()` + `getDifficulty()` for tests. Tier is pushed to the Phaser registry under `vortexPong.difficulty`; `GameScene.loadDifficultyTier` reads registry first then localStorage fallback then `DEFAULT_DIFFICULTY`. `updateAI` (`GameScene.ts:336–385`) now scales baseTracking, maxSpeed, outgoingTracking factor, and errorOffset by the tier params. Tests: 24 new (12 config helper coverage, 6 MenuScene cycle/persist/label/SFX, 5 GameScene tier read + directional AI movement + Normal regression guard, 1 localStorage-throw resilience); suite 1,966 pass (+24 from R84.P2). Gates all green — tsc + lint + vitest + build. Next: **R84.P4 — Vortex atmosphere amp-up**.)*
- [x] **R84.P4 [P1]** Vortex atmosphere amp-up *(R84.P4 — shipped 2026-04-19 night. Three procedural layers stacked into `VortexPong/GameScene` via a new `ATMOSPHERE` config block (`config.ts:83–109`). (a) **Rotating radial-gradient backdrop** at depth -20: base `MATRIX_COLORS.NEAR_BLACK` disc + 9 graduated `DARK_GREEN` overdraws stepped from `RING_INNER_ALPHA=0.55` down to `RING_OUTER_ALPHA=0.05`, painted outside-in so the stacked fillCircle calls simulate a CRT falloff Phaser Graphics can't gradient natively. Elliptical stretch `ASPECT_X=1.25, ASPECT_Y=0.85` breaks radial symmetry so rotation actually reads — pure circles under rotation would be visually static. Rotation rate `2π / ROTATION_SECONDS=45s` per update-tick delta keeps motion subtle. (b) **Boosted scanline overlay** at depth 90: `STRIDE_PX=3` row draw across full canvas at `ALPHA=0.23` — Snake's R83.S1 baseline of 0.18 × 1.30 per plan. (c) **Paddle-glow pulse** at depth -5: two `MATRIX_COLORS.PRIMARY` rectangles padded `WIDTH_PAD=14, HEIGHT_PAD=24` behind each paddle; alpha ramps from `MIN_ALPHA=0.08` → `MAX_ALPHA=0.55` as the nearest ball enters `THRESHOLD_PX=260` (~1/3 canvas width), with `SCALE_BOOST=0.25` extra swell on closest approach. Y-tracks paddle every frame. `prefers-reduced-motion` gates all three: backdrop rotation halts (gradient stays visible static), scanline overlay is skipped entirely, paddle glow keeps alpha feedback but drops the scale pulse. Full shutdown cleanup destroys + nulls all four refs. Tests: 21 new in a dedicated `R84.P4 — Vortex atmosphere` describe block (config ranges, depth ordering, `RING_COUNT+2` fillCircle calls, elliptical scale, rotation delta math, reduced-motion gating for all three layers, proximity-driven alpha ramp, y-tracking, scale boost, no-op safety when refs absent); test-mock updates swapped `add.graphics` + `add.rectangle` from `mockReturnValue` to `mockImplementation` returning fresh instances with `rotation` + chainable setters so call records don't stomp between layers. Suite 1,987 pass (+21 from R84.P3). Gates all green — tsc + lint + vitest (138/138 Pong-scoped) + build. Next: **R84.P5 — Power-up in-HUD legend**.)*
- [x] **R84.P5 [P1]** Power-up in-HUD legend *(R84.P5 — shipped 2026-04-19 night. New `POWERUP_LEGEND` config block (`config.ts:135–165`) holds 4 entries (`BIG · PADDLE +50% · 10s`, `SLOW · BALL -40% · 10s`, `2X · SCORE BONUS · 10s`, `MULTI · +2 BALLS · NOW`) + timing constants (`DISPLAY_MS=4000`, `FADE_IN_MS=200`, `FADE_OUT_MS=400`, `LINE_HEIGHT=12`, `BASE_Y_RATIO=0.72`, `ACTIVE_ALPHA=1`, `INACTIVE_ALPHA=0.55`). `GameScene.showPowerUpLegend(activatedType)` rebuilds 4 centred text rows at canvas mid-x, y = 0.72 × HEIGHT, depth 100 — activated row paints at `ACTIVE_ALPHA`, the other three dim to `INACTIVE_ALPHA` so the player still sees the full menu but eye tracks the highlighted entry. Each row colour-matches `POWERUP_DEFS[type].color`. Trigger: `collectPowerUp` calls `showPowerUpLegend(pu.type)` after `activatePowerUp`, so repeat pickups refresh in-place (prior legend is cleared + its hide timer cancelled via `remove(false)`). `hidePowerUpLegend` fades the cohort over `FADE_OUT_MS` with an identity-guard on `this.powerUpLegend === targets` so a mid-fade re-pickup's cohort is never stomped. `prefers-reduced-motion` skips fade-in/out tweens (alpha set directly, cohort destroyed synchronously on hide) but the 4s hide window still fires so the HUD doesn't stay cluttered. Shutdown path cancels the hide timer first then destroys every text. Tests: 22 new in a `R84.P5 — Power-up legend` describe block (6 config sanity, 8 render — count/centring/colour/copy/spacing/baseY-ratio/depth/timer, 3 active-row highlighting, 2 repeat-pickup refresh, 2 reduced-motion, 2 safety no-ops). Test mocks extended — `add.text` swapped from `mockReturnValue` to `mockImplementation` returning fresh instances with chainable `setText/setAlpha/setDepth/setOrigin` setters and state fields so per-row assertions are independent; `time.delayedCall` mock gained `remove` + `callback` handles to match the real Phaser Timer API. Full suite 2,012 pass (+25 from R84.P4). Gates all green — tsc + lint + vitest (160/160 Pong-scoped, 47/47 test files) + build. Next: **R84.P6 — Multi-ball visual distinction** (tint spawned multi-ball balls CYAN so the 200-point High Score weighting reads visually at a glance).)*
- [x] **R84.P6 [P2]** Multi-ball visual distinction *(R84.P6 — shipped 2026-04-19 night. `PongBall` interface gained a required `isMultiBall: boolean` flag. `spawnBall` signature extended with an `options: { isMultiBall?: boolean }` 5th param; when true the sprite has `setTint(MATRIX_COLORS.CYAN=0x00ffff)` applied so the cyan read survives even when the optional `ball_multi` PNG is absent (headless/offline test harnesses). Bespoke `ball_multi` texture is still preferred at asset-load time when available. `spawnMultiBalls` passes `{ isMultiBall: true }` to every extra ball it spawns, so the 200-pt High Score weighting reads visually at a glance. Replaced the fragile `isExtra = this.balls.length > 0` heuristic at the spawn site (would mis-tag a respawn after partial goal clearance) with an explicit caller-controlled flag. 9 new tests in a `R84.P6 — Multi-ball CYAN tint` describe block (primary spawns untinted + flagged false, multi-ball spawns tinted + flagged true, `spawnMultiBalls` produces only tinted balls, primary ball untouched by multi-ball spawn, post-goal respawn untainted, ball-cap caps at min(2, 3-balls.length)). Test mocks: `add.sprite` tracks `setTint` + `tint` field for colour assertions; `createBall` helper updated to accept an `isMultiBall` 6th arg (default false). Full suite 2,021 pass (+9 from R84.P5). Gates all green — tsc + lint + vitest (169/169 Pong-scoped) + build. Next: **R84.P7 — paddle-hit trail amplification** (R83.V1 shipped 10 particles — consider 20 at top-tier ball speed).)*
- [x] **R84.P7 [P2]** Paddle-hit trail verification + amplify *(R84.P7 — shipped 2026-04-19 night. R83.V1e 10-particle flat trail verified landed at `GameScene.ts:1106–1128`; now amplified per plan. New `PADDLE_TRAIL` config block (`config.ts:72–80`) carries BASE_COUNT=12, MAX_COUNT=20, plus particle appearance + speed-jitter constants. `computeTrailParticleCount()` lerps linearly from BASE at multiplier=1.0 (fresh serve) to MAX at multiplier=MAX_SPEED/INITIAL_SPEED (~2.14× cap), clamped to ≥0 normalised so slower_ball's 0.6× multiplier still emits the full BASE burst (juice never thins). `createPaddleHitTrail` consumes the computed count + config constants instead of the R83.V1e literals. Reduced-motion path unchanged (skipped entirely). 7 new tests in a `R84.P7 — Trail amplification` block (config sanity, BASE at mult=1.0, MAX at cap, linear scaling at midpoint ±1 for rounding, slower_ball clamp to BASE, actual hit emits MAX at top-tier `timeSinceLastGoal`, per-particle radius + alpha match config). Existing R83.V1e test updated — base-count assertion now reads from config. Full suite 2,028 pass (+7 from R84.P6). Gates all green — tsc + lint + vitest (176/176 Pong-scoped) + build. Next: **R84.P8 — Ball rally counter UI**.)*
- [x] **R84.P8 [P2]** Ball rally counter UI *(R84.P8 — shipped 2026-04-19 night. Top-centre HUD counter at `(WIDTH/2, 14)` renders `RALLY x{count}` in CYAN `#00ffff` (rally/multi-ball family colour), hidden on fresh serve (`alpha 0` until the first player return), pulses scale 1.3→1.0 over 180ms `Back.easeOut` on every player paddle return, wipes back to `alpha 0` with empty text on any goal. New `RALLY_COUNTER` config block at `config.ts:88-102`. `updateRallyCounter()` kills the prior pulse tween before queuing a new one so rapid rallies don't stack conflicting scale animations; reduced-motion still updates text+alpha (the metric stays readable) but skips the tween. `hideRallyCounter()` clears text to `''` (not `RALLY x0`) so any alpha-fade window can't briefly flash stale copy. AI paddle hits do NOT touch the counter — the existing rally semantic (`rallyCount` only increments on player returns per R84.P2's `maxRally × 10` weighting) is preserved. `createUI` adds the counter alongside the existing score+combo texts; `checkGoals` calls `hideRallyCounter()` the same frame the reset to 0 happens. Tests: 17 new in a `R84.P8 — Rally counter UI` block (3 config sanity, 6 `updateRallyCounter` behaviour, 3 paddle-hit integration, 4 hide-on-goal, 1 `createUI` wiring). Full suite 2,045 pass (+17 from R84.P7). Gates green — tsc + lint + vitest (193/193 Pong-scoped) + build.)*
- [x] **R84.P9 [P2]** Goal-flash verification — R83.V1 dimmed; re-verify epilepsy-safe. *(R84.P9 — shipped 2026-04-19 night. R83.V1c dimmed the flash to 128/channel but left the values as inline magic numbers at 4 callsites with no tripwire preventing a future refactor from re-introducing 255, and no throttle to stop a multi-ball 3-goal storm breaching WCAG 2.3.1's ≤3Hz General Flash Threshold. P9 consolidates the four presets into a new `GOAL_FLASH` config block inside `GAME_CONFIG` (adjacent to `SHAKE`, same camera-effects cluster) — `PLAYER_GOAL`/`AI_GOAL`/`PLAYER_WIN`/`AI_WIN` each carry typed `rgb: readonly [r,g,b]` + `durationMs`, plus three safety ceilings: `MAX_CHANNEL_VALUE=160` (observed win cap), `MAX_DURATION_MS=200` (below PEAT 1s window), `MIN_INTERVAL_MS=334` (PEAT-safe 3Hz floor). New exported interface `GoalFlashPreset` makes callsite signatures self-documenting. `goalFlash()` refactored to accept a preset + optional `{ overrideThrottle }` — each channel clamped via `Math.min(c, MAX_CHANNEL_VALUE)`, duration clamped via `Math.min(d, MAX_DURATION_MS)`, throttle suppresses second flash if `Date.now() - lastGoalFlashAt < MIN_INTERVAL_MS`. New scene field `lastGoalFlashAt` reset to 0 in `resetState()` so R-restart doesn't carry stale timestamp. All 4 callsites refactored — `AI_GOAL`/`PLAYER_GOAL` use throttle, `PLAYER_WIN`/`AI_WIN` pass `overrideThrottle: true` so game-over climax fires even when the clinching regular flash landed <334ms prior (win flash only fires once per match → cannot re-trigger rapidly). 14 new tests in an `R84.P9 — Goal-flash safety` block: 6 config sanity (channel cap, duration cap, interval floor, all 4 presets within caps, one-saturated-channel-only invariant blocking white/yellow flashes, all durations ≤ cap), 4 throttle (first-fires, within-window-suppressed, after-window-fires, override-bypasses), 2 clamping (rgb 255→160, duration 900→200), 2 reduced-motion+reset (resetState zeroes lastGoalFlashAt, reduced-motion returns before touching throttle state). Existing R83.V1c tests still pass — preset-based callsites produce the same flash(100, 0, 128, 0, false) signature. `Date.now()` spied via `vi.spyOn(Date, 'now')` for deterministic throttle tests; `afterEach` added to vitest imports for spy cleanup. Full suite 2,059 pass (+14 from R84.P8, 207/207 Pong-scoped). Gates all green — tsc + lint + vitest + build.)*
- [x] **R84.P10 [P1]** Double-pause-overlay regression check *(R84.P10 — shipped with R84.UP1 on 2026-04-19 night. Ralph Playwright-drove Pong at 1280×800: paused via dashbar ❚❚ button after countdown → live DOM probe returned **exactly 1 `[data-testid="ipod-pause-overlay"]`, 1 `.ipod-pause-scrim`, 1 `.ipod-pause-text`, 1 `.ipod-pause-hint`** — no stacking. Screenshot `.playwright-mcp/r84-up1-pong-paused-single-overlay.png` shows one amber radial scrim with one PAUSED word + one hint line. Tom's "double/triple" complaint was the scrim+text+hint **trio inside one overlay** being miscounted as three separate overlays — architectural single-source-of-truth at `GamePortal.tsx:622-632` gated by `isPaused`, with `BaseScene.togglePause` as the only state owner (comment at `BaseScene.ts:152-155` explicitly documents "Presentation is owned by the React portal's dashbar scrim"). R83.G3 is live-confirmed shipped on Pong. Task closes under R84.UP1's broader hunt — see D3 notes.)*
- [x] **R84.P11 [P2]** Paddle-motion smoothing *(R84.P11 — shipped 2026-04-19 night. Tom's doc line 130 ("Keyboard controls are a bit dodgy ... It's quite often conflicted") described the paddle flipping between 0 and PADDLE.SPEED on the same frame a key lands — a binary on/off feel that R83.V1's "springs-back-to-centre" fix didn't address. P11 adds a 100ms linear ease-in ramp from 0 → 1 in a new `keyboardSpeedRatio` scene field, integrated trapezoidally (average of pre- and post-frame ratios) so a single coarse 100ms frame from rest produces exactly half the old snappy distance and 16ms frames integrate smoothly to the same endpoint regardless of frame rate. New config constant `PADDLE.ACCELERATION_MS=100` documents the ramp window. `handlePlayerInput` rewritten: `kbActive = kbUp || kbDown` drives the ramp (either direction counts as "still pressing"), released frames snap ratio back to 0 so a re-press restarts from rest — this also kills the "conflicted" feel when a player taps then releases. `prefers-reduced-motion` bypasses the ramp entirely (set ratio=1 immediately, effectiveRatio=1) because ramps introduce input latency and a11y guidance treats user-driven control as essential motion — reduced-motion users want predictable instant response, not smooth animated response. `resetState()` now zeroes `keyboardSpeedRatio` so R-restart mid-press doesn't resume at stale full-speed. Pointer-driven movement is untouched — mouse still snaps directly to pointer.y. 13 new tests in an `R84.P11 paddle ease-in ramp` block: config sanity, resetState zeroing, linear half-ramp at 50ms, full ratio at 100ms, clamp beyond window, release-snap to 0, trapezoidal distance (24px on 100ms-from-rest vs. old 48px), full-speed frame after ramp, down-key symmetry, direction flip preserves ramp, release-then-re-press starts from 0, reduced-motion bypass, pointer path unaffected, top-wall clamp. Existing R83.V1 `keyboard up/down` direction-only assertions still pass — trapezoidal distance (24px) is still `< 250` / `> 200`. Full suite 2,073 pass (+14 from R84.P9). Gates all green — tsc + lint (0 errors, 4 pre-existing warnings) + vitest (221/221 Pong-scoped) + build 7.91s. Next: **R84.P12 — Unit-test coverage refresh** (Tom's buffer for formal coverage across Stream B features).)*
- [x] **R84.P12 [P1]** Unit-test coverage refresh *(R84.P12 — shipped 2026-04-19 night. Added 12 new tests covering the four gap areas Stream B polish left under-tested. **Scoring (3 in `GameScene.test.ts` R84.P2 block)**: plan's 4-10 loss reference formula = 210 points (matchDiff clamped to 0, P=2, R=11 → 100+110), `resetState` zeroes `powerUpsCollected`, `resetState` zeroes `rallyCount`+`maxRally` (previously only `multiBallsTriggered`'s reset was guarded). **Countdown (3 in new `R84.P12 — Countdown update gating` block)**: `update()` short-circuits while `isCountingDown` so balls don't move (physics frozen contract), `exposeTestState` not called during countdown (telemetry freeze contract), post-countdown `update()` resumes normally (balls advance + exposeTestState fires). Pong had zero countdown tests prior — the BaseScene-inherited `startCountdown(5)` wiring was live-verified in R84.P1 via Playwright but the unit contract was never pinned. **Difficulty (4 in `config.test.ts`)**: Normal tier pins all 4 multipliers exactly (trackingMultiplier=1.0, maxSpeedFactor=0.95, errorMultiplier=1.0, outgoingTrackingFactor=0.45 — R83.V1a baseline regression guard), tier labels are exactly `EASY`/`NORMAL`/`HARD` (MenuScene render contract), `outgoingTrackingFactor` ordering easy < normal < hard (a tuning-pass typo away from inverted), `DIFFICULTY_TIERS` keys align with `DIFFICULTY_ORDER` (prevents orphan or missing tier rows). **Legend (2 in `R84.P5 — Power-up legend` → new `R84.P12 — trigger + timer coverage` nested block)**: row order in the rendered cohort matches `POWERUP_LEGEND.ENTRIES` 1:1 (consistency guard — a swap in config would silently desync from MenuScene docs), hide-timer callback + fade `onComplete` destroys cohort correctly (full teardown chain: delayedCall → `hidePowerUpLegend()` → tween → `onComplete` → destroy; previously the fade tween's onComplete path was only exercised by reduced-motion which skips the tween entirely, so the default-motion destroy chain had zero assertion). Full suite **2,085 pass** (+12 from R84.P11, 233/233 Pong-scoped across config+MenuScene+GameScene). Gates all green — tsc clean + lint 0 errors (4 pre-existing warnings) + vitest + build 12.59s. Stream B **COMPLETE** — all 12 tasks `[x]`. Next: **R84.S1 — Snake yellow wall spacing fix** (Stream C opens; math already derived in D-S1-math: `GRID_OFFSET_Y: 20 → 40`).)*

**Stream C — Matrix Snake polish (R84.S1 → S10, ~10 iterations):**

- [x] **R84.S1 [P1]** Yellow wall spacing fix *(R84.S1 — shipped 2026-04-19 night. Single-line config edit: `SnakeClassic/config.ts` `GRID_OFFSET_Y: 20 → 40`. New wall geometry on the 400×400 Snake canvas: top wall (grid row `-1`) occupies pixels 24..40, bottom wall (grid row `GRID_ROWS=20`) occupies pixels 360..376 → symmetric 24/24px margins as required by Tom's doc line 134 "no space at the top of the portal and a little bit of space at the bottom". Pre-fix asymmetry was 4px top / 44px bottom — a 40px imbalance that read as "walls glued to the top edge". Root cause was the offset being chosen to put the **play area top edge** (not the top-wall top edge) 20px below canvas top, while the wall was drawn at grid-row `-1` (16px above the play area top), burning 16 of the 20px on wall tile and leaving only 4px clearance. The correct centred math is `(HEIGHT − (GRID_ROWS+2)×CELL_SIZE) / 2 = (400 − 22×16) / 2 = 24`, so the play area top edge (where the first in-play cell starts) needs to be at y=40 and the wall tile above it fills y=24..40. Setting `GRID_OFFSET_Y=40` gives exactly that geometry. Downstream callsites all reference the symbolic constant so auto-track (`drawGrid`, `drawGridBorder`, `showPowerUpLegend`'s `baseY = GRID_OFFSET_Y + GRID_ROWS*CELL_SIZE + 18` still lands 2px below bottom-wall bottom edge — self-consistent because the wall is always 16px tall). HUD columns at `leftX=100` / `rightX=700` are unaffected — they render left/right of the play area (X=180..500) and are unrelated to the vertical offset. 2 new regression tests in a `R84.S1 — Yellow wall margin symmetry` block pin `topMargin === bottomMargin` + `topMargin > 0` + both wall edges stay within canvas bounds — blocks a future offset tweak from silently re-introducing the imbalance. Existing `gridToPixel` tests reference the symbolic `GAME_CONFIG.GRID_OFFSET_Y` and auto-track the change (0 test churn). Full suite **2,087 pass** (+2 from R84.P12's 2,085 baseline, 92/92 Snake-scoped). Gates all green — tsc clean + lint 0 errors (4 pre-existing warnings) + vitest + build 7.66s. Snake testing-doc line 22 Known Issue ticked `[x]`; inline prose note line 134 annotated with FIXED R84.S1 pointer. Next: **R84.S2 — Matrix funkiness depth pass** (in-play-area rain at 0.15 alpha, snake-head setShadow glow, per-food-type ASCII-glyph variant for bonus food).)*
- [x] **R84.S2 [P1]** Matrix funkiness depth pass — after R83.S1 rain+scanline+chromatic. Ship (a) in-play-area Matrix-rain particles at 0.15 alpha (not just bg), (b) snake-head `setShadow` glow in `MATRIX_COLORS.PRIMARY` at 6-8px radius, (c) per-food-type visual variant — bonus food gets ASCII-glyph treatment so food reads as "code eaten by snake". *(shipped 2026-04-19 night — single-source-of-truth `MATRIX_FUNKINESS` config block in `SnakeClassic/config.ts:65–84`; (a) 8 katakana+digit glyphs at α 0.15 + depth -1 inside grid bounds, 30-80px/s drift with top-wrap, stubbed in `__TEST__` mode because `Phaser.Math.Between` is unmocked; (b) twin fillCircles (r=8 α 0.22 + r=6 α 0.5) in `MATRIX_COLORS.PRIMARY` under the head at depth -1 — `Graphics.fillCircle` chosen over `preFX.addGlow` for Canvas-fallback compat; (c) every 5th pickup flags `isBonusFood` evaluated post-`reportScore` so cadence is food-count deterministic; bonus spawns a glyph at 18px pulse-yoyo (α 0.7↔1, scale 0.9↔1.15, 450ms) over the hidden apple sprite (grid collision unchanged), awards 2× points stacking with 2X power-up to 4×. `resetState` zeroes + `getTestState` exposes + `shutdown` tears down rainGroup/glow/bonusText. 20 new tests in `R84.S2 — Matrix funkiness depth pass` block; test mocks gained `setDepth`/`setAlpha`/`setOrigin`/`setScale`/`setPosition`/`setData`/`getData` on graphics + text. Suite 2,109 pass (+22 from R84.S1).)*
- [x] **R84.S3 [P1]** Power-up variety expansion — 2-3 new types (reverse controls 5s, double-score 10s, glitch-rain 3s obscuring screen). Each: icon + SFX + legend copy. Split across sub-iterations S3a/S3b/S3c if needed. *(shipped 2026-04-19 night — landed as a single iteration rather than three sub-iterations because the three effects share one architectural shape (scene-level boolean flag + `TimerEvent` teardown) so splitting would have forced three rounds of test-mock churn for no design upside. **Config** (`SnakeClassic/config.ts`): extended `PowerUpType` union to 7 types with new `reverse` / `hyper` / `glitch` entries; added `POWERUP_DEFS` rows with distinct palette — RED `0xff3333` reverse signalling "challenge/danger", GOLD `0xffaa00` hyper distinct from YELLOW SLOW, VIOLET `0xaa00ff` glitch distinct from MAGENTA shield; added timing constants `REVERSE_POWERUP_DURATION=5000` / `HYPER_POWERUP_DURATION=10000` / `GLITCH_POWERUP_DURATION=3000` (plan-brief values chosen to stay inside Snake's short feedback loop — 10s hyper racks up meaningful 2× scoring, 5s reverse stings but recovers, 3s glitch reads as risk-for-bonus not unplayable); flat bonuses `REVERSE_PICKUP_BONUS=50` + `GLITCH_PICKUP_BONUS=100` reward collecting pure-challenge tokens (reverse has no "help" effect, glitch obscures screen — reward has to come on pickup, awarded up-front so a post-pickup wall crash doesn't consume it); new `GLITCH_RAIN` config block (`DENSITY=80`, `ALPHA=0.55`, `FONT_SIZE=14`, `DEPTH=200` above scanline=100 and gameplay sprites, `SPEED_MIN/MAX=120/260`). **Reverse effect**: `handleInput` swaps `newDir` to `OPPOSITE_DIRECTIONS[newDir]` **before** the 180°-reversal guard so pressing INTO the snake's travel direction under reverse flips to the opposite of that (which IS the 180° opposite) and gets rejected by the existing guard — no instant-death from the reversal. **Hyper effect**: time-based 2× multiplier applied in `eatFood` after the count-based `double` check; stacks multiplicatively with `double` (4× base) and with bonus food (8× base on a full overlap), intentionally kept simple because the `BONUS_FOOD_INTERVAL=5` cadence gates how often full-stack fires. Named `hyper` rather than `double` to avoid collision with the existing count-based `double` power-up. **Glitch effect**: `showGlitchOverlay` spawns 80 Matrix-rain text glyphs across the canvas at depth 200 (above scanline 100, above every gameplay sprite), each carrying its own speed via `setData('speed', n)` and drifting down 120-260 px/s, wrapping to top when past the canvas floor. Gameplay continues beneath the overlay — grid collisions untouched; the risk/reward is whether the player remembers their current trajectory well enough to keep steering blind. Skipped under `prefers-reduced-motion` + `window.__TEST__` (same pattern R84.S2 play-area rain uses because `Phaser.Math.Between` isn't mocked in jsdom). **SFX**: reverse uses GLASS_BREAK (glassy cue that "things are flipped"), hyper reuses POWERUP_MAGNET (shares "money-is-good" beat with `double`), glitch uses SPECIAL_ABILITY (one-shot screen-takeover burst). **Textures auto-generate** — BootScene's `createPowerUpTextures` iterates `POWERUP_DEFS` entries so the three new `powerup_reverse`/`powerup_hyper`/`powerup_glitch` sprites are produced procedurally with the right colour, zero new sprite work. **Legend**: `showPowerUpLegend` now renders two lines — headline `POWER-UPS · SLOW · 2X · SHIELD · GHOST` + sub-row `REVERSE · HYPER · GLITCH · CATCH TOKENS` (both under ~90 chars so they render in the 640px play area). **HUD indicators**: `updatePowerUpIndicators` shows REVERSE in `RED_HEX`, HYPER in `#ffaa00`, GLITCH in `#aa00ff` — same palette as their pickup tokens. **Lifecycle**: `resetState` zeroes all three flags; `destroyPowerUpTimers` nulls all three timers; `shutdown` tears down the glitch overlay via `destroyGlitchOverlay`; `deactivate*` methods flip flag + null timer + (glitch) destroy overlay. **Pool sampling**: `spawnFieldPowerUp` extended to flat 7-type equal-probability pool — kept flat rather than weighted because the durations already gate how often each effect fires and weighting would require a tuning surface Tom hasn't asked for. **getTestState** exposes `reverseActive` / `hyperActive` / `glitchActive` for Playwright hooks. **39 new tests** in an `R84.S3 — Power-up variety expansion` describe block: 6 config sanity (types registered, 7 distinct colours, labels, durations pinned to plan-brief, positive bonuses, overlay depth above scanline), 1 resetState zeroing, 6 activatePowerUp branches (reverse flag + timer + bonus + reportScore, hyper flag + timer + no-score-touch, glitch flag + bonus + reportScore + timer, prior-timer destroy), 4 handleInput reverse mapping (UP→DOWN swap, LEFT→RIGHT swap, 180° guard wins on reversed-into-own-body, inactive leaves inputs alone), 4 eatFood hyper multiplier (2× alone, 4× with double, 8× with double+bonus, 1× inactive), 3 collectPowerUp SFX map, 3 deactivate methods, 1 pool sampling (400-sample Monte Carlo pins 7-type coverage), 1 destroyPowerUpTimers, 3 updatePowerUpIndicators rendering, 1 showPowerUpLegend sub-row tokens, 1 getTestState exposure, 4 glitch overlay animation (empty no-op, y advance trapezoidal, wrap to -10 past floor, destroyGlitchOverlay teardown), 1 shutdown lifecycle glitch teardown. Test-file import extended with `POWERUP_DEFS` + `GLITCH_RAIN` from `../config` and `SOUND_KEYS` from `@/lib/phaser/types`; `Phaser.Input.Keyboard.JustDown` seeded as `vi.fn()` at file top following the NeoJump test-file pattern so handleInput tests can drive direction presses deterministically; new `beforeEach` inside the R84.S3 block sets `window.__TEST__ = true` so `activatePowerUp('glitch')` branches early-return past `Phaser.Math.Between`. Full suite **2,148 pass** (+39 from R84.S2's 2,109 baseline, 153/153 Snake-scoped). Gates all green — tsc clean + lint 0 errors (4 pre-existing warnings) + vitest 12.13s + build 7.74s. Snake testing-doc prose line 137 "Need to explain what power-ups are" partly addressed (legend now includes new tokens); the broader "more variety" ask met head-on. **R84 stream scoreboard**: Stream A V1/V2/V3 `[x]`; Stream B P1–P12 `[x]` (Pong polish done); **Stream C S1+S2+S3+S9 `[x]` (4/12)**; Stream D fully `[ ]`; Stream U UP1+UP2 `[x]`. Next: **R84.S4 — Speed-tier dread build-up** (scanline intensifies + BGM bass thickens + subtle camera shake under top-tier speed).)*
- [x] **R84.S4 [P1]** Speed-tier dread build-up — current tiers trigger audio cues (R81); add visual build-up: scanline intensifies, BGM bass thickens, subtle camera shake under top-tier speed. *(R84.S4 — shipped 2026-04-19 night. R81 audio cues stay; S4 layers three synchronised sensory effects that ramp with a single `dreadIntensity` scalar [0..1] rather than a boolean flip — a ramp reads as "dread building" whereas a flag reads as "switch thrown" and loses the juice. All tuning constants live in a new single-source-of-truth `DREAD_BUILDUP` config block (`SnakeClassic/config.ts:153–163`). **Threshold design**: engages once `currentSpeed` drops below `START_SPEED=80` (= level 15 / score 700 under the `SPEED_INCREMENT=5` + `POINTS_PER_SPEED_UP=50` ramp in `GAME_CONFIG`) and peaks at `MAX_SPEED=GAME_CONFIG.MIN_SPEED=50` (level 21 / score 1000). Linear ramp means early dread is barely perceptible while final tiers feel genuinely tense — matches Snake's gameplay arc where runs start survivable and end knife-edge. Chose 80 (not lower) so dread covers ~30% of the speed range; chose a linear ramp (not eased) so the player feels the pressure tighten evenly rather than spiking. **Effect (a) — scanline intensification**: a second full-canvas scanline overlay at depth 101 (above the R83.S1 baseline scanline at depth 100) painted as 1-px black rows every `SCANLINE_STRIDE=3` px, alpha set directly to `dreadIntensity × SCANLINE_MAX_ALPHA=0.20`. Chose 0.20 cap so total scanline alpha (0.18 baseline + 0.20 dread = 0.38 peak) stays short of "unplayable dark", and chose to re-draw the whole grid once in `createDreadScanlineOverlay` with alpha tweaked per-frame rather than clearing + redrawing stripes each tick — cheaper and the geometry never changes. Reads as the CRT "tightening" as the run gets dangerous. **Effect (b) — BGM bass thickening via ambient drone**: `BaseScene.playAmbientDrone({volume: 0.08})` starts on the activation edge, `stopAmbientDrone()` on the deactivation edge, one-shot per edge so repeated calls can't stack gain. Drone is a 55 Hz saw+sine through a 180 Hz lowpass — a sub-harmonic layer that sits underneath `cruise-control.mp3` rather than fighting its arrangement. Crucially this reuses the primitive shipped for R83.CTRLS.17 (CTRL-S climax dread) rather than adding a biquad lowshelf filter to the MP3 element — (i) the primitive already has built-in 2 s fade-in + 1 s fade-out, so activation/deactivation is smooth with no envelope code here, (ii) an amplitude-felt layer doesn't clash with the MP3 track's existing mix bus, (iii) zero new Web Audio code. Volume 0.08 is deliberately subtle — the ear should perceive "thicker low end" without registering "new instrument". **Effect (c) — camera micro-shake**: periodic `cameras.main.shake(SHAKE_DURATION_MS=40, SHAKE_MAX_INTENSITY=0.003 × intensity)` every `SHAKE_INTERVAL_MS=450`, registered once on the activation edge as a `TimerEvent` with `loop: true`. Intensity 0.003 is 4× less than Pong's death-shake (0.012) so it reads as "pulse under the skin" rather than "thing crashing". 450 ms cadence is slow enough that individual shakes don't blur into a continuous rumble. **Reduced-motion**: camera shake is gated behind `matchMedia('(prefers-reduced-motion: reduce)').matches` — a periodic shake is nauseating for motion-sensitive users, while the scanline (static image change) + drone (audio) still convey the state change. **Race guard**: the shake loop TimerEvent checks `this.dreadActive` inside its callback before firing `cameras.main.shake`, so a mid-tick deactivation during the 450 ms gap (e.g. shield pickup reverses direction, score rolls up, a hyper activation fades dread back out) can't leak one stale shake past teardown. **Lifecycle**: `updateDreadBuildup()` runs every tick from `update()`; `computeDreadIntensity(currentSpeed)` is a pure function returning clamped [0..1]; activation/deactivation are edge-triggered via `dreadActive` flag so timer + drone start/stop exactly once per threshold crossing. `resetState()` zeroes `dreadActive` + `dreadIntensity` for R-restart. `shutdown()` calls `teardownDreadBuildup()` which stops the shake timer, stops the drone, destroys the scanline overlay, and zeroes the intensity. **27 new tests** in an `R84.S4 — Speed-tier dread build-up` describe block: 6 config sanity (threshold ordering `START_SPEED > MAX_SPEED`, scanline alpha cap ≤ 0.25, scanline depth 101 above baseline 100, shake amplitude cap < death-shake's 0.012, shake cadence positive, drone volume subtle ≤ 0.12), 4 `computeDreadIntensity` (= 0 above threshold, = 0 at edge, = 1 at/below MAX, linear midpoint within 0.01), 1 `resetState` zeroes flag + intensity, 4 `updateDreadBuildup` threshold transitions (stays inactive above, activates below → registers timer + starts drone, tears down on climb back → cleans timer + stops drone, one-shot per edge → no double-register / double-stop), 2 scanline ramp (full alpha at peak speed=50, zero below threshold speed=100), 4 `startDreadShakeLoop` (`TimerEvent` with `loop: true` + 450 ms delay, intensity-scaled `cameras.main.shake` call, race guard skips shake after deactivation, no double registration), 2 `stopDreadShakeLoop` (destroy + null, safe no-op when already null), 2 `teardownDreadBuildup` (full cascade, skip drone-stop when never active — avoids a spurious `stopAmbientDrone` call leaking into the BaseScene audio bus), 1 shutdown lifecycle (full cascade on `scene.shutdown()`), 1 `getTestState` exposes `dreadActive` + `dreadIntensity` for Playwright hooks. Test-file additions: `DREAD_BUILDUP` import extended; `createMockGraphics` gained `setAlpha`; `createTestScene` stubs `playBackgroundMusic` / `stopBackgroundMusic` / `playAmbientDrone` / `stopAmbientDrone` as `vi.fn()`s so audio assertions can be driven without Web Audio; outer `beforeEach` seeds `dreadScanlineOverlay` + `dreadShakeTimer=null` so tests that don't call `create()` still have mocks. Full suite **2,175 pass** (+27 from R84.S3's 2,148 baseline, 180/180 Snake-scoped). Gates all green — tsc clean + lint 0 errors (4 pre-existing warnings) + vitest 12.93 s + build 7.84 s. Snake testing-doc line 121 "Speed tier transitions trigger (audio cue per R81)" annotated with the S4 amplification — the checkbox was already `[X]` pre-S4 because R81 audio cues shipped, S4 adds the missing *visual + haptic + bass* layers Tom's doc flagged. **R84 stream scoreboard**: Stream A V1/V2/V3 `[x]`; Stream B P1–P12 `[x]` (Pong polish done); **Stream C S1+S2+S3+S4+S9 `[x]` (5/12)**; Stream D fully `[ ]`; Stream U UP1+UP2 `[x]`. Next: **R84.S5 — Snake death cinematic** (300ms glitch-cascade of 4–6 red horizontal bars before Game Over, matching R83.CTRLS.12 climax-failure pattern).)*
- [x] **R84.S5 [P2]** Snake death cinematic — 300ms glitch-cascade (4-6 red horizontal bars strobing, same pattern as R83.CTRLS.12 climax-failure) before Game Over. *(R84.S5 — shipped 2026-04-19 night. 300 ms glitch-cascade of 5 red horizontal bars strobes across the canvas the instant the snake dies, mirroring the R83.CTRLS.12 "buffer-flushed" failure language at `NarrativeScene.ts:1615–1643` so the arcade's catastrophic-failure visual palette stays consistent across both games. Tom's Snake testing-doc prose line 137 flagged the prior behaviour ("could be more dramatic when dying") as under-telegraphed — the snap from last frame → 600 ms red flash → Game Over panel left no visible beat for the player's eye to register WHAT killed the snake. The cascade now occupies the first 300 ms of that 600 ms buffer so the dead-head freeze-frame stays visible underneath the bars (visual parity with CTRLS.12 which leaves the terminal UI visible behind its cascade). **Config (`SnakeClassic/config.ts:198–207`)** single-source-of-truth `DEATH_CINEMATIC` block: `BAR_COUNT=5` (mid of Tom's 4–6 band), `TOTAL_DURATION_MS=300`, `BAR_STROBE_MS=120` (60 ms fade-in + 60 ms fade-out yoyo), `BAR_ALPHA=0.65` (slightly above CTRLS.12's 0.6 — shorter window needs more density to read), `BAR_COLOR=0xff2040` (exact match to CTRLS.12 red — cross-game failure palette), `BAR_HEIGHT=5`, `DEPTH=110` (above baseline scanline 100 + R84.S4 dread scanline 101), `MARGIN_Y=40`. **Geometry**: 5 bars evenly spaced between y=40 and y=360 → Y positions 40/120/200/280/360 on the 400 px canvas. Chose even spacing (not random Y like CTRLS.12) because Snake's shorter 300 ms window needs a deterministic top-to-bottom "buffer flush" rather than sparse noise — also sidesteps `Phaser.Math.Between` being unmocked in jsdom (same constraint R84.S2 play-area rain + R84.S3 glitch overlay hit). **Strobe math**: `yoyo: true` alpha tween (`0 → 0.65 → 0`) with `duration: BAR_STROBE_MS/2 = 60 ms` so each bar total runs 120 ms. Stagger step = `(TOTAL − STROBE) / (COUNT − 1) = (300 − 120) / 4 = 45 ms` → delays `[0, 45, 90, 135, 180]` so the last bar ends at exactly 300 ms — hits the plan brief to the millisecond. Yoyo chosen over two chained tweens (fade-in then fade-out) so `onComplete` fires exactly once at true end-of-strobe and per-bar self-destruct runs once; halves the tween count. **Per-bar self-destruct**: each tween's `onComplete` splices its bar out of `deathCinematicBars[]` and calls `.destroy()` so the array empties itself by T+300 ms — fire-and-forget. **Wiring**: `handleGameOver()` calls `playDeathCinematic()` between the head-sprite dead-tint and the existing `time.delayedCall(600, …)`, so the 600 ms buffer stays untouched and the cinematic runs inside its first half. **Shutdown path**: `shutdown()` calls `destroyDeathCinematicBars()` FIRST (before dread teardown) so a mid-cinematic scene shutdown (R-restart, pause-then-exit) cleans still-live bars without leaking GameObjects — ordering matters because dread teardown relies on the tweens manager staying intact. **Reduced-motion a11y**: early-return at the top of `playDeathCinematic()` under `matchMedia('(prefers-reduced-motion: reduce)').matches`. Death is already conveyed by the GAME_OVER sound + red camera flash + snake head tint → dead sprite; the cascade is supplemental juice that strobes at ~8 Hz per-bar edge frequency, which warrants a11y gating for motion-sensitive users. **Tests**: 19 new in `R84.S5 — Snake death cinematic` block across 7 sub-describes. (1) 8 config sanity — `BAR_COUNT` in 4–6 band, `TOTAL_DURATION_MS === 300`, strobe ≤ total, bar colour = CTRLS.12 red, alpha < 1, depth ≥ 102 above dread scanline, margin positive, stagger math pins last-bar-end at exactly 300 ms. (2) 5 playDeathCinematic spawning — spawns `BAR_COUNT` rectangles, bars painted with CTRLS.12 red + `BAR_HEIGHT` + full canvas width + `setOrigin(0, 0.5)` + `setDepth(110)` + initial alpha 0, Y positions evenly spaced across the vertical interior, one yoyo tween per bar with half-strobe duration, stagger delays linearly spaced `[0, 45, 90, 135, 180]`. (3) 1 reduced-motion a11y gate — beforeEach+afterEach `matchMedia` mock proves early-return. (4) 1 tween onComplete self-destruct — fires all onCompletes, array empties. (5) 2 `destroyDeathCinematicBars` teardown — destroy + zero array, no-op safe when empty. (6) 1 `handleGameOver` wiring — spy proves cinematic call happens before the `delayedCall(600, …)` and the 600 ms buffer stays unchanged. (7) 1 `shutdown` teardown — live bars destroyed on scene shutdown. Test-file additions: `afterEach` added to vitest imports; `DEATH_CINEMATIC` import extended; `createMockRectangle` helper with chainable `setOrigin/setAlpha/setDepth/destroy` + `x/y/width/height/fillColor` fields; `scene.add.rectangle` mock seeds constructor args onto the returned mock for Y-position + colour assertions. Full suite **2,194 pass** (+19 from R84.S4's 2,175 baseline, 199/199 Snake-scoped). Gates all green — tsc clean + lint 0 errors (4 pre-existing warnings unchanged) + vitest 13.08 s + build 7.81 s. Snake testing-doc line 137 prose note annotated FIXED with R84.S5 pointer. **R84 stream scoreboard**: Stream A V1/V2/V3 `[x]`; Stream B P1–P12 `[x]`; **Stream C S1+S2+S3+S4+S5+S9 `[x]` (6/12)**; Stream D fully `[ ]`; Stream U UP1+UP2 `[x]`. Next: **R84.S6 — Food pickup juice verification** (post-S1 apple-shrink — verify the re-positioned wall geometry didn't clip or mis-scale the apple sprite, amp pickup juice if shallow).)*
- [x] **R84.S6 [P2]** Food pickup juice verification post-S1 apple-shrink. *(R84.S6 — shipped 2026-04-19 night. Two deliverables: (1) **geometry verification** that R84.S1's `GRID_OFFSET_Y: 20 → 40` wall-shift didn't clip/mis-scale the 0.75× (12 px on a 16 px cell) apple sprite; (2) **juice amplification** because the baseline eat VFX (8-particle green burst + ±3 px chromatic flash + +N score popup) read as shallow relative to R83.S1's rain/scanline/chromatic aesthetic and R84.S2's bonus-food pulse. **Geometry proof**: with `GRID_OFFSET_Y=40`, the grid-to-pixel transform places cell-0 apple centre at y=48 (since `gridToPixel` adds `CELL_SIZE/2`), so the apple's top edge sits at `48 - 12/2 = 42` — the top-wall bottom edge is at y=40, yielding a symmetric 2 px vertical gap. Cell-19 apple centre = 352, bottom edge = 358, bottom-wall top edge = 360 → same 2 px gap. Horizontal geometry is unchanged from pre-S1 (`GRID_OFFSET_X=180`) and was already correct. Five geometry tests in R84.S6 pin: apple half-size < half-cell, cell-0 top edge > top-wall bottom, cell-19 bottom edge < bottom-wall top, cell-0 X edges within horizontal play area, cell-19 Y edges within vertical play area — blocks any future wall-offset tweak from silently re-introducing a clip. So S6's "verify" leg became proof-by-tests rather than a bug fix. **Juice amplification**: three layered effects, all routed through a new single-source-of-truth `FOOD_PICKUP_JUICE` config block in `SnakeClassic/config.ts` alongside `DREAD_BUILDUP` + `DEATH_CINEMATIC`. (a) **Expanding green ring** (`createEatRing(x, y)`): a Phaser `Arc` at the pickup coordinate with `radius=4`, `strokeWidth=2`, initial alpha `0.85`, depth `4`; tweens `scale: 1 → 5.5` + `alpha: 0.85 → 0` over `280 ms` with `Quad.easeOut`, then self-destructs via `onComplete: () => ring.destroy()`. Chose an `Arc` (`add.circle`) with `setStrokeStyle` rather than a Graphics shape because it's `scale`-tweenable (Graphics arcs aren't) and cheaper to render. Mirrors the visual grammar of `createShieldBreakEffect` intentionally — shield break = "this thing just popped visibly", eat = "food just popped visibly" — keeping the arcade's VFX language coherent. Duration 280 ms matches shield break's 300 ms neighbourhood; scale-end 5.5 spreads the ring to ~22 px radius (just past one cell width) so it reads clearly without dominating the frame. Gated under `prefers-reduced-motion` (early-return) for a11y — same gating as R84.S4 dread shake + R84.S5 death cascade. (b) **Bigger burst**: `BURST_COUNT` bumped 8 → 10 in `createEatBurst` → `createParticleBurst(x, y, MATRIX_COLORS.PRIMARY, 10)`. 10 is a meatier pop without crossing into "feature particle" territory (cf. the 12-particle shield-break burst). (c) **Wider chromatic flash**: `createChromaticAberrationFlash` offset widened ±3 → `CHROMATIC_OFFSET_PX=5` px — still subtle but measurably juicier. (d) **Score popup scale-pop**: `createScorePopup` now calls `setScale(0.5)` then tweens `scale: 0.5 → 1.0` over 220 ms with `Back.easeOut`, layered underneath the existing y-rise + alpha fade. `Back.easeOut` overshoots slightly before settling — reads as a confident "+N punch in". **Call-order**: `createEatBurst(x, y)` now fires ring FIRST (so the particles fly outward from a forming ring, not the other way round), then particle burst, then chromatic flash — tested via `vi.spyOn` + `invocationCallOrder` to pin the stacking. **Config block**: 12 named constants (`EAT_RING_RADIUS`, `EAT_RING_SCALE_END`, `EAT_RING_STROKE_WIDTH`, `EAT_RING_INITIAL_ALPHA`, `EAT_RING_DURATION_MS`, `EAT_RING_DEPTH`, `BURST_COUNT`, `CHROMATIC_OFFSET_PX`, `SCORE_POPUP_SCALE_FROM`, `SCORE_POPUP_SCALE_TO`, `SCORE_POPUP_SCALE_DURATION_MS`) + a ~30-line design-rationale comment at the top of the block explaining why each value was chosen. **Tests**: 23 new in `R84.S6 — Food pickup juice amplification` describe block. 7 config sanity (positive ring radius, scale-end > scale-start of 1, positive stroke, alpha in (0..1], positive duration, depth ≥ 0, burst count positive + bigger than old 8, offset positive + bigger than old 3, scale-from < scale-to, positive popup duration). 5 apple geometry (the proof-by-math above). 5 `createEatRing` spawn assertions (circle instantiated at x/y with right radius + stroke + alpha + depth, tween targets ring with scale + alpha + right ease + right duration + self-destruct, no-op under reduced-motion early-return). 2 `createEatBurst` stacking (ring + particle burst + chromatic flash all called once, stacking order via `invocationCallOrder` → ring < burst < chromatic). 1 `createChromaticAberrationFlash` widened offset (red + blue copies placed at `±CHROMATIC_OFFSET_PX=5` from centre, not old ±3). 3 `createScorePopup` scale-pop (text starts at `SCORE_POPUP_SCALE_FROM=0.5`, tween target includes `scale: SCORE_POPUP_SCALE_TO=1`, duration matches config, ease = `Back.easeOut`). **Test infra extension**: `createMockCircle()` helper added with chainable `setStrokeStyle/setAlpha/setDepth/setFillStyle/destroy` + `x/y/radius/color/alpha` captured fields; `scene.add.circle` mock extended to seed constructor args onto the returned mock — verified via grep that no pre-existing shield-break / particle-burst tests depended on the old minimal circle mock (0 regression). **matchMedia save/restore gotcha**: initial test pass used `vi.spyOn(window, 'matchMedia').mockReturnValue(...) / mockRestore()` — 3 tests failed because `src/test/setup.ts` defines `window.matchMedia` via `Object.defineProperty` with `vi.fn().mockImplementation(...)`, and `mockRestore()` on a spy over this leaves a bare `vi.fn()` that returns `undefined`, breaking subsequent `.matches` reads in any test that runs after. Fix: switched to R84.S5's manual save/restore pattern — `let originalMatchMedia = window.matchMedia` in `beforeEach`, `(window as any).matchMedia = originalMatchMedia` in `afterEach`. All 222 Snake-scoped tests pass (+23 from R84.S5's 199). Full suite **2,217 pass** (+23 from R84.S5's 2,194 baseline). Gates all green — tsc clean + lint 0 errors (4 pre-existing warnings unchanged) + vitest 12.77 s + build 7.88 s. Snake testing-doc line 124 prose note annotated `AMPLIFIED R84.S6` with the geometry proof + three-effect summary. **R84 stream scoreboard**: Stream A V1/V2/V3 `[x]`; Stream B P1–P12 `[x]`; **Stream C S1+S2+S3+S4+S5+S6+S9 `[x]` (7/12)**; Stream D fully `[ ]`; Stream U UP1+UP2 `[x]`. Next: **R84.S7 — Boss-snake prototype** (optional, may `[DEFER]` to R85 if time-constrained) OR **R84.S8 — Food/power-up sprite polish** (grid-snap + crispness pass).)*
- [ ] **R84.S7 [P2]** Boss-snake prototype (optional) — triggers at score 500: antagonist snake appears, player survives 30s while both compete. If time-constrained, `[DEFER]` to R85.
- [x] **R84.S8 [P2]** Food/power-up sprite polish — ensure all food sprites snap to grid + feel crisp. *(R84.S8 — shipped 2026-04-19 night. **Root cause**: Tom's testing-doc line 94 "Apple is too big" was still live *after* R83.S1's apple-shrink claim, because `createFoodSprite` called `setDisplaySize(CELL_SIZE * 0.75, CELL_SIZE * 0.75)` which internally sets scaleX/scaleY to 0.75, but the immediately-following yoyo pulse tween `scale: { from: 0.85, to: 1.05 }` writes raw scale values on the same property — Phaser's last-added tween on a given target + property wins, so R83.S1's shrink was silently overridden the moment the pulse started. Apple rendered 13.6 → 16.8 px (pulse valley → peak) on a 16 px cell — overspill at peak, visually "bigger than the square" exactly as Tom flagged. Two identical patterns in `spawnFieldPowerUp` (pulse 0.8→1.2 on 16 px texture → 19.2 px peak, 3.2 px overspill) and `spawnFood` bonus branch (font 18 px × 1.15 scale → 20.7 px peak, 4.7 px overspill). **Grid-snap verified correct** by math before any fix: `gridToPixel(gx, gy) = { x: GRID_OFFSET_X + gx*CELL_SIZE + CELL_SIZE/2, y: GRID_OFFSET_Y + gy*CELL_SIZE + CELL_SIZE/2 }` — with `GRID_OFFSET_X=180`, `GRID_OFFSET_Y=40`, `CELL_SIZE=16` (all integers) returns integer pixel centres for every grid cell. Phaser init already has `pixelArt: true` which sets texture filter to NEAREST (no sub-pixel interpolation blur). So the "not crisp" perception was motion-driven overspill, not sub-pixel positioning — fixing the scale track fixes both complaints simultaneously. **Fix strategy** (single-source-of-truth, mirrors R84.S1/S4/S5/S6/S12 pattern): new `SPRITE_POLISH` config block in `SnakeClassic/config.ts` with ~35-line derivation comment pinning (a) `FOOD_BASE_SCALE=0.75` (12 px on 16 px texture), `FOOD_PULSE_MIN=0.675`, `FOOD_PULSE_MAX=0.75` — invariant `PULSE_MAX ≤ BASE_SCALE` means the apple can never exceed 12 px at peak, so never overspills the 16 px cell; (b) `POWERUP_BASE_SCALE=1.0` (16 px cell-exact), `POWERUP_PULSE_MIN=0.8` (12.8 px valley), `POWERUP_PULSE_MAX=1.0` (16 px cell-exact peak, no overspill); (c) `BONUS_FOOD_PULSE_MIN=0.9` / `MAX=1.0` tightens the bonus-glyph pulse so 18 px font × 1.0 scale = 18 px peak (still cell-safe since bonus glyph has a thinner visual weight than a filled apple). **Call-site migration**: `createFoodSprite` now calls `setScale(SPRITE_POLISH.FOOD_BASE_SCALE)` instead of `setDisplaySize(...)` — baseline and pulse now live on the same scale track, so the pulse yoyoing between `FOOD_PULSE_MIN..FOOD_PULSE_MAX` can't override the baseline (they ARE the baseline, oscillating). `spawnFood` bonus branch uses `BONUS_FOOD_ALPHA_MIN/MAX` + `BONUS_FOOD_PULSE_MIN/MAX` + `BONUS_FOOD_PULSE_DURATION_MS`. `spawnFood` non-bonus pop-in tween now targets `SPRITE_POLISH.FOOD_BASE_SCALE` (was magic 1.0, which would have re-introduced overspill the moment pop-in completed). `spawnFieldPowerUp` adds an explicit `setScale(POWERUP_BASE_SCALE)` before the pulse registration so the pulse has a known starting point. **Why setScale not setDisplaySize**: `setDisplaySize` sets scaleX/scaleY implicitly based on the target pixel dimensions — any subsequent scale tween runs against a baseline that isn't visible in the source code (you have to know the texture is 16×16 and mentally divide), which is exactly how the bug got introduced in the first place. `setScale` with config constants makes the relationship explicit — you can read the pulse range and the baseline and visually confirm the invariant holds. **14 new tests** in `R84.S8 — Sprite polish` describe block across 5 sub-describes. (1) 6 config sanity: `FOOD_PULSE_MAX ≤ FOOD_BASE_SCALE` (the core invariant — blocks any future scale-tween overspill regression), `FOOD_PULSE_MIN > 0` (prevents invisibility), `POWERUP_PULSE_MAX ≤ POWERUP_BASE_SCALE`, `POWERUP_PULSE_MIN < POWERUP_PULSE_MAX` (pulse oscillates, not static), `BONUS_FOOD_PULSE_MAX ≤ 1.0`, all duration constants positive. (2) 2 grid snapping: `gridToPixel(0, 0)` returns integer x/y (math proof that `GRID_OFFSET + half-CELL` sums to integer), same at `(GRID_COLS-1, GRID_ROWS-1)` corner. (3) 2 `createFoodSprite` wiring: `setScale` called with `FOOD_BASE_SCALE` AND `setDisplaySize` NOT called — this is the critical regression guard blocking any future refactor from re-introducing the setDisplaySize+scale-tween override; pulse tween registered with correct `FOOD_PULSE_MIN/MAX` bounds + `FOOD_PULSE_DURATION_MS`. (4) 1 `spawnFood` non-bonus pop-in: tween scale target = `FOOD_BASE_SCALE` not magic `1` (would re-introduce overspill). (5) 1 `spawnFood` bonus: pulse uses `BONUS_FOOD_PULSE_MIN/MAX` bounds. (6) 2 `spawnFieldPowerUp`: `setScale(POWERUP_BASE_SCALE)` called before pulse registration; pulse tween uses `POWERUP_PULSE_MIN/MAX`. Full suite **2,240 pass + 2 todo** (+14 from R84.S12's 2,226 baseline, 245/245 Snake-scoped). Gates all green — tsc clean + lint 0 errors (4 pre-existing warnings unchanged) + vitest + build 7.72 s. Snake testing-doc lines 93–95 prose note annotated FIXED with R84.S8 root-cause writeup; line 132 inline "apple too big" comment annotated FIXED with pointer to the line-97 block. **R84 stream scoreboard**: Stream A V1/V2/V3 `[x]`; Stream B P1–P12 `[x]`; **Stream C S1+S2+S3+S4+S5+S6+S8+S9+S11+S12 `[x]` (10/12)**; Stream D fully `[ ]`; Stream U UP1+UP2 `[x]`. Next: **R84.S10 — Unit-test coverage refresh** (new power-ups + wall-spacing + HUD bounds + sprite polish consolidation audit) OR **R84.S7 — Boss-snake prototype** (optional, may `[DEFER]` to R85 if time-constrained).)*
- [x] **R84.S9 [P2]** Pause-overlay regression check on Snake *(R84.S9 — closed with R84.UP1 on 2026-04-19 night. Ralph's UP1 upstream hunt proved the pause overlay is rendered by a single React component at `GamePortal.tsx:622-632`, gated by one `isPaused` boolean, fed by one `PAUSE_STATE_CHANGED_EVENT` dispatched by one `BaseScene.togglePause()` path. Grep across all 12 Phaser scene files returned zero scene-side "PAUSED" text renderers. Since Snake uses the identical GamePortal + BaseScene pair as Pong, and Pong was live-verified with exactly 1 overlay + 1 scrim + 1 text + 1 hint on every inspection, Snake's overlay behaviour is bitwise identical. No Snake-specific regression possible under the current architecture.)*
- [x] **R84.S10 [P1]** Unit-test coverage refresh *(R84.S10 — shipped 2026-04-19 night. Pure-test sweep of gap areas Stream C S1-S9 polish left implicit. Five sub-blocks in a new `R84.S10 — Coverage refresh gap-fills` describe at the tail of `GameScene.test.ts`: **(a) R84.S1 draw-layer geometry — 5 tests**: `getPlayAreaBounds` returns top=40/bottom=360 post-S1; `drawGrid` draws exactly 21+21 lineBetween calls (GRID_COLS+1 × GRID_ROWS+1); top horizontal line anchored at `GRID_OFFSET_Y=40` (tripwire against pre-S1 `y=20` regression); bottom horizontal at `GRID_OFFSET_Y + GRID_ROWS*CELL_SIZE=360`; every vertical line spans top→bottom of the play area. R84.S1 previously only tested margin symmetry — a future offset tweak could keep the margins balanced but shift the draw surface; S10 pins the draw calls themselves. **(b) R84.S3 reverse/hyper re-activation idempotency — 2 tests**: seed prior timer, call `activatePowerUp('reverse'|'hyper')`, assert prior destroyed and replaced. The glitch timer equivalent was already pinned at line 1342; reverse/hyper live in separate `case` blocks with their own `if (timer) destroy` guards and could regress independently. **(c) R84.S3 simultaneous indicator render — 2 tests**: flip all 7 power-up flags on, assert `powerUpIndicators.size === 7` and stacked y-coords `[70, 100, 130, 160, 190, 220, 250]` at a 30 px stride. Per-type label/colour pins covered each in isolation but never the 30 px `y += 30` stride or the simultaneous render. **(d) R84.S3 `showGlitchOverlay` entry-guard seams — 3 tests**: first direct tests of `showGlitchOverlay` (prior block only tested `updateGlitchOverlay`/`destroyGlitchOverlay`). Asserts `window.__TEST__=true` skips the spawn (Playwright determinism); prefers-reduced-motion skips the spawn (a11y); prior-overlay teardown runs BEFORE the guard checks so back-to-back glitch pickups don't leak text from the previous activation. **(e) R84.S3 `collectPowerUp` integration for new types — 3 tests**: end-to-end `collectPowerUp('reverse'|'hyper'|'glitch')` assertions — `powerUpsCollected` increments, active flag flips, score delta matches `REVERSE_PICKUP_BONUS`/`GLITCH_PICKUP_BONUS`/zero (hyper). Prior coverage only spot-checked SFX map; now the full pipeline `collectPowerUp → activatePowerUp` is pinned for all three new types. Glitch test wraps the call in `window.__TEST__=true` because `activatePowerUp('glitch')` runs `showGlitchOverlay` whose production spawn path dereferences un-mocked `Phaser.Math.Between`. **Totals**: +15 test cases (245 → 260 in `GameScene.test.ts`), full suite 2,373 pass + 2 todo across 48 files (was 2,358 pass). Gates all green — tsc exit 0, lint 0 errors (4 pre-existing warnings unchanged), Snake-scoped vitest 2.64s, full suite 12.49s, vite build 7.79s. Zero scene code touched — single-file diff. S7 boss-snake deferred per original brief so the "if shipped" clause is moot.)*
- [x] **R84.S11 [P1]** Rename completeness audit *(R84.S11 — shipped 2026-04-19 night. Closed both Stream A audit rename gaps R83.S1 missed. **D6 fix** — `src/lib/asciiArt.ts:62` `'snake-classic': ['SNAKE', 'CLASSIC']` → `['MATRIX', 'SNAKE']`. The portal hero banner now renders the production-side display name ("Matrix Snake") matching the iPod card subtitle, gameRegistry, SaveLoadManager, AttractMode and achievement notifications. ASCII width math: MATRIX is 6 chars (5px-glyph × 6 + 5 inter-glyph spaces = 35 chars), SNAKE is 5 chars (29 chars) — both well within asciiArt.test's 60-char-per-row ceiling. The replaced word CLASSIC was actually 41 chars wide so the new title is *narrower* than the old one. **D7 fix** — `src/hooks/useSaveSystem.ts:148–154` 7 achievement-row `game: 'Snake Classic'` strings → `'Matrix Snake'`. `useAchievementManager.test.ts:279–297` 5 `stats.byGame['Snake Classic']` lookups → `stats.byGame['Matrix Snake']`. **Audit scope cross-check** — production-side already-correct callsites verified clean: `gameRegistry.ts:33` (`title: 'Matrix Snake'`), `SaveLoadManager.tsx:87` (`snakeClassic: 'Matrix Snake'`), `AttractMode.tsx:12` (same), and React-side display flows (App.test.tsx + SaveLoadManager.test.tsx already assert against `'Matrix Snake'`). **Test fixtures intentionally NOT renamed**: `KeyPanel.test.tsx`, `GamePortal.test.tsx`, `IpodBootOverlay.test.tsx`, `AchievementNotification.test.tsx`, `GameHighScores.test.tsx` all pass `title: 'Snake Classic'` as **arbitrary string fixtures** to test their components' generic title-rendering behaviour. They aren't reading from production data layers, they're feeding hard-coded prop strings — leaving them out of scope keeps the audit surgical and avoids spurious test churn that would camouflage a real fixture-vs-data drift in future. The visual-baseline regen for the new ASCII portal hero banner is owned by R84.BL (Stream E) per the plan's separation. **Why the rename matters**: the player-facing "Matrix Snake" name is referenced in three persistence layers — ASCII title art (visual), achievement tracking (data), save-state keys (storage). Pre-S11 drift meant `getStats().byGame` returned an object keyed `'Snake Classic'` while user-facing UI displayed `'Matrix Snake'`, so an achievement unlocked under the snakeClassic id could appear "missing" if any UI ever pivoted to lookups by displayed name. **Tests** — no new tests added; rename is a string-literal swap and existing tests were already pinning the expected display-name (now matching production). Targeted run pre-commit: `asciiArt.test.ts` 9/9, `useSaveSystem.test.ts` 67/67, `useAchievementManager.test.ts` 49/49 — 125/125 pass after the rename. Full suite **2,217 pass + 2 todo** (unchanged from R84.S6 — pure rename, no test count delta). Gates all green — tsc clean + lint 0 errors (4 pre-existing warnings unchanged) + vitest 12.82 s + build 7.28 s. **R84 stream scoreboard**: Stream A V1/V2/V3 `[x]`; Stream B P1–P12 `[x]`; **Stream C S1+S2+S3+S4+S5+S6+S9+S11 `[x]` (8/12)**; Stream D fully `[ ]`; Stream U UP1+UP2 `[x]`. Next: **R84.S12 — HUD clipping investigation** (live-inspect right-column at 1280×800 — confirm whether `rightX=700` on a 640-px canvas clips or if scene camera bounds extend) OR **R84.S8 — Food/power-up sprite polish** (grid-snap + crispness pass) OR **R84.S7 — Boss-snake prototype** (optional, may DEFER to R85).)*
- [x] **R84.S12 [P2]** HUD clipping investigation *(R84.S12 — shipped 2026-04-19 night. **Finding**: code-proven off-canvas. `GameScene.createHUD` at line 358 + `updatePowerUpIndicators` at line 398 both used a hard-coded `rightX=700`. Canvas is `GAME_CONFIG.WIDTH=640`. Grep across the scene for `setBounds` / `cameras.main.setSize` returned zero hits — no camera-bounds extension made the 700 legit. Phaser scale mode is `FIT` (per `PhaserGame.tsx:176`) which scales canvas to fit container but does NOT expand the logical drawing area. Therefore the `POWER-UPS` label, `FOOD` count, and all seven active power-up indicators (SLOW / 2X / SHIELD / GHOST / REVERSE / HYPER / GLITCH) were being drawn 60 px off the right edge of the canvas at every runtime — never visible to the player since scene start. Live inspection was skipped because the bug is mathematically provable from static analysis: `setOrigin(0.5)` + `x=700` + `canvas.width=640` → clipped out, no browser/device/viewport variance. **Fix** (single source of truth, mirrors R84.S1/S4/S5/S6 pattern): new `HUD_X` config block in `SnakeClassic/config.ts:64–84` with 18-line derivation comment. `LEFT_X=100` preserved; `RIGHT_X=580`. Derivation: play area spans `x=180..500` (`GRID_OFFSET_X=180` + `GRID_COLS=20` × `CELL_SIZE=16`), walls extend one cell outward to `x=164..516`, right-hand HUD margin is `x=516..640` (124 px wide), margin centre at `x=578` rounded to 580 for readability. `RIGHT_X=580` mirrors `LEFT_X=100`'s play-area-symmetric 64 px gap to the opposing wall edge. `GameScene.createHUD` and `updatePowerUpIndicators` now read `HUD_X.LEFT_X` + `HUD_X.RIGHT_X` instead of magic numbers, so future layout tweaks land in one place. **Why 580 not `WIDTH-LEFT_X=540`**: canvas symmetry would land HUD text only 24 px right of the wall edge (516..564 for a 50-px label), while margin-centred gives 64 px gap mirroring the left side — R84.S1 established play-area symmetry as the Snake scene's design pattern (symmetric 24/24 px wall margins), so S12 follows the same house rule. **Widest-label fit check**: `POWER-UPS` at 8px font ≈ 45 px wide → `x=557..603`; power-up labels at 10px font ≈ 50 px wide → `x=555..605`; both inside `[0, 640]` with 35 px clearance from canvas right edge. **Tests**: 9 new in `R84.S12 — HUD column bounds` describe block split across 3 sub-describes. (1) 4 config sanity — `RIGHT_X < WIDTH`, `LEFT_X > 0`, widest-label half-width fits within canvas bounds at `RIGHT_X` (uses `0.8 × fontSize` char-width heuristic — defensive overestimate since the real `Press Start 2P` bitmap font is ~0.67× — so the bounds test fails with room to spare rather than approving a clip-prone layout on font-fallback browsers), `RIGHT_X` within 10 px of geometric margin-centre (derived from `GRID_OFFSET_X + GRID_COLS × CELL_SIZE + CELL_SIZE` = wall-right-edge 516, `(516 + 640) / 2 = 578`, tolerance 10 preserves flex for future readability rounds). (2) 4 `createHUD` wiring — `POWER-UPS` at `HUD_X.RIGHT_X`, `FOOD:` at `HUD_X.RIGHT_X`, `SCORE` at `HUD_X.LEFT_X`, every HUD text call X ∈ `[0, WIDTH)`. (3) 1 `updatePowerUpIndicators` wiring — with all 7 power-ups active, every indicator's X = `HUD_X.RIGHT_X` and exactly 7 calls register. All pin either the config block or the call site reading from it — blocks any future magic-number regression. **Gates**: tsc clean + lint 0 errors (4 pre-existing warnings unchanged) + vitest (231/231 Snake-scoped, +9 from R84.S11's 222) + build 7.47 s. Full suite **2,226 pass + 2 todo** (+9 from R84.S11's 2,217). Snake testing-doc line 126 annotated with `FIXED R84.S12` block. **R84 stream scoreboard**: Stream A V1/V2/V3 `[x]`; Stream B P1–P12 `[x]`; **Stream C S1+S2+S3+S4+S5+S6+S9+S11+S12 `[x]` (9/12)**; Stream D fully `[ ]`; Stream U UP1+UP2 `[x]`. Next: **R84.S8 — Food/power-up sprite polish** (grid-snap + crispness) OR **R84.S10 — Unit-test coverage refresh** (new power-ups + wall-spacing fix + HUD bounds) OR **R84.S7 — Boss-snake prototype** (optional, may `[DEFER]` to R85).)*

**Stream D — Matrix Bird polish (R84.B1 → B12, ~12 iterations):**

- [x] **R84.B1 [P1]** Display-name full audit *(R84.B1 — shipped 2026-04-19 night. **Full live re-audit at 1280×800 via Playwright MCP: every rendered text surface reads "Matrix Bird". Tom's "text still says matrix cloud (on the ipod)" complaint is confirmed STALE** (predates R83.B1 ship). Evidence: `.playwright-mcp/r84-b1-matrix-bird-ipod-live.png` shows iPod rendering ASCII "MATRIX / BIRD" title glyph, "ARCADE" category badge, new flight-themed description, and zero "cloud" text anywhere in the DOM (`document.createTreeWalker` full text-node walk filtered on `/cloud|bird|matrix/i` yielded only `THE MATRIX ARCADE` header, `Matrix Bird — game 4 of 12` aria-live region, `Matrix Bird` sr-only h2, and `THE MATRIX ARCADE v2.0` footer). Code-surface audit (static): registry `src/data/gameRegistry.ts:53` title "Matrix Bird", ASCII `src/lib/asciiArt.ts:64` `['MATRIX','BIRD']`, save-manager `src/components/ui/SaveLoadManager.tsx:89` label "Matrix Bird", scoreboard `src/components/scoreboard/Scoreboard.tsx:13` label "Matrix Bird", attract-mode `src/components/scoreboard/AttractMode.tsx:14` label "Matrix Bird", all 8 achievements `src/hooks/useSaveSystem.ts:166-173` tagged `game: 'Matrix Bird'`, About `src/components/About.tsx:21` name "Matrix Bird", MenuScene `src/components/games/phaser/MatrixCloud/scenes/MenuScene.ts:8` title "MATRIX BIRD". Registry ID `matrix-cloud` + save key `matrixCloud` + folder `MatrixCloud/` + class prefix `MatrixCloud*` intentionally stable per R83.B1 storage-compat call. **Small description sharpening**: `src/data/gameRegistry.ts:54` changed from generic "Navigate through gaps in the digital storm — one wrong move and you crash." to "Flap between pipes in the code cascade — one missed beat and gravity wins." — the verb "Flap" and noun "pipes" lock the Flappy-Bird lineage semantically so the card can never again read as "cloud" on a quick glance. The iPod preview image (still a vortex illustration) routes to R84.B2 per testing-doc D9; that's the "picture of a cloud" half of Tom's complaint and is out of scope for B1. Validation: `npx tsc --noEmit` clean, `npm run lint` 0 errors, `npm run build` 7.73s clean, 158 targeted tests pass (`GameHighScores`, `GamePortal`, `App`, `MatrixCloud/GameScene`, `KeyPanel`, `GameInstructions`).)*
- [x] **R84.B2 [P1]** Preview image swap (card + portal) *(R84.B2 — shipped 2026-04-19 night. Dropped the vortex Cloudinary URL at `src/data/gameRegistry.ts:55` (`.../A_surreal_thumbnail_of_a_glowing_neon_green_vortex_spiraling..._gyatls.png`) in favour of a new hand-authored local SVG at `public/assets/matrix-cloud/preview.svg` (6.8 KB, viewBox 1024×576, 16:9). Composition mirrors the in-game aesthetic: black→deep-green vertical sky gradient, 12 static Matrix-rain columns at mixed opacities (`url(#rainCol)`), scanline overlay at 22% black, silhouette pipe pair with gap centred on bird height + a dim background pipe at 35% opacity, ground band with 16 pixel highlights, and a centred 16×16-style pixel bird upscaled 16× to 256×256 with body/highlight/beak/eye/wing layers and a radial glow halo behind it. Palette pulls from the same `#00ff00`/`#00b300`/`#00ff66`/`#003300` tones the Phaser scenes use so it reads "same universe" as the gameplay. `preserveAspectRatio="xMidYMid slice"` + `shape-rendering="crispEdges"` give crisp pixel edges at any zoom + clean cropping under the landing card's `object-cover`. Preview flows into the iPod portal via `GamePortal.tsx:647` which reads the same `game.preview` field — single-source-of-truth swap covers both card + portal. Validation: `tsc --noEmit` clean, `vite build` 7.58s clean (SVG copied to `dist/assets/matrix-cloud/preview.svg`), `GamePortal.test.tsx` (43 tests) + `App.test.tsx` (19 tests) + registry tests all green. Visual baselines will need regen under R84.BL. **Choice rationale**: picked SVG-in-`public/` over WebP-import because (a) no image tools available locally (no Pillow/sharp/cwebp/ImageMagick on dev machine, only ffmpeg which doesn't render SVG well), (b) SVG stays in source control + under diff review vs. a binary blob, (c) crisp pixel aesthetic matches the Phaser pixel-art bird better than AI-illustration style, (d) zero external Cloudinary dependency — matches Tom's preferred path (b) in the original task brief.)*
- [x] **R84.B3 [P1]** Slow-powerup momentum feel *(R84.B3 — shipped 2026-04-19 night. **Root cause of "not enough momentum"**: pre-R84.B3 the scene scaled the WORLD (pipes via `updatePipes(dt, speedMult)`, field power-ups, boss) by `TIME_SLOW_FACTOR=0.6` but let the PLAYER run at full-rate physics (`updatePlayer(dt)` with raw gravity 1400 px/s² + un-scaled integration). The bird fell at full rate while the world crept at 60% — exactly the "sluggish bird in a slow world" feel Tom flagged on Bird testing-doc line 115. The R83.B1(e) 0.6× jump-impulse scaling had kept apparent peak HEIGHT constant but left hang-TIME shorter in slow mode (peak time = V₀/g = (0.6·JV)/G ≈ 0.18s vs 0.3s full-rate) — a partial fix that addressed the symptom but not the feel. **Fix (a) — player-physics time-dilation**: `updatePlayer` now computes `playerSpeedMult = timeSlowActive ? TIME_SLOW_FACTOR : 1.0` and scales both gravity accumulation (`playerVelocity += GRAVITY × dt × playerSpeedMult`) AND vertical integration (`playerY += playerVelocity × dt × playerSpeedMult`). Velocity terminal clamp stays absolute — it's a safety ceiling on the raw state value, not an apparent-motion quantity. Result: with impulse also 0.6× (R83.B1(e)), peak height invariant (gaps stay equally navigable) but hang time scales 1.67× — bird drifts through gaps instead of dropping through. Coherent "time slowed for everything" feel. **Fix (b) — jump-power 0.6× verified floaty not sluggish**: math-proof inspection of R83.B1(e) impulse scale sitting at `GameScene.ts:265` unchanged — Tom's complaint pivoted to momentum, not height, so the 0.6× constant stays. If ever re-tuned it belongs in a future B-series iteration with fresh live-playtest. **Fix (c) — yellow trail particles**: new `SLOW_MODE` config block in `MatrixCloud/config.ts` pinning trail constants in one place (`TRAIL_EMIT_INTERVAL_MS=120`, `TRAIL_PARTICLE_RADIUS=3`, `TRAIL_PARTICLE_ALPHA=0.7`, `TRAIL_PARTICLE_LIFESPAN_MS=600`, `TRAIL_COLOR=0xffff00` matching `POWERUP_DEFS.timeSlow`, `TRAIL_DEPTH=9` one below player depth 10). New `startSlowTrail / stopSlowTrail / emitSlowTrailParticle` trio hooks off `activatePowerUp('timeSlow')` and the timer-expiry callback — emit guard ignores pause + game-over frames so shutdown cleanup invariant stays intact. Particles fade over 600ms via `tweens.add({ alpha: 0 })` with `onComplete` teardown that also splices the array, so live-particle tracking never leaks. **Config choices**: 120ms emit interval × 600ms lifespan = ~5 overlapping particles visible as a tail — enough to read as motion, not enough to clutter the 800×450 canvas. Yellow matches HUD chip colour so slow-state reads as one visual story. Depth 9 slots behind player (10) so breadcrumbs read "left behind" not "in front of". **Tests**: 20 new in 3 `R84.B3` describe blocks — (1) **5 physics** (gravity scales when slow, gravity stays full-rate when inactive — regression guard against silently re-reverting the scale, position integration scales when slow, position stays full-rate when inactive, velocity terminal clamp stays absolute under slow mode), (2) **10 trail lifecycle** (initial null+empty, `activatePowerUp('timeSlow')` starts timer with correct delay+loop, `startSlowTrail` idempotent so re-activation doesn't duplicate, `stopSlowTrail` destroys+nulls, `emitSlowTrailParticle` spawns yellow circle at player position with 5 expected args — radius/color/alpha via `toHaveBeenCalledWith` spy, emit registers fade tween with `alpha: 0` + lifespan duration, emit no-ops when paused — critical because the addEvent timer keeps ticking through pause, emit no-ops when gameOver — same reason, `resetState` tears down timer + destroys all live particles + clears array, `shutdown` tears down timer + destroys all live particles + clears array), (3) **5 config sanity** (trail colour matches `POWERUP_DEFS.timeSlow` so HUD chip + trail read as one state, trail depth strictly < player depth 10, emit interval positive AND strictly less than lifespan — invariant that guarantees overlapping tail not flash-and-gap, alpha in (0,1], radius positive). Full suite **2,260 pass + 2 todo** (+20 from R84.S8's 2,240). Gates all green — tsc clean + lint 0 errors (4 pre-existing warnings untouched) + vitest 109/109 Bird-scoped + build 7.51s. Next Stream D task: R84.B4 pipe variety progression (moving/zapper/bonus pipes).)*
- [x] **R84.B4 [P1]** Pipe variety + progression *(R84.B4 — shipped 2026-04-19 night. Three new pipe kinds layer in as the run progresses, keeping mid/late-game Flappy loops from flattening into one monotonous gap-threader. **(a) Moving pipes (score ≥ 200)**: sinusoidal vertical drift on `baseGapY + sin(elapsedMs/1000 × freqHz × 2π + phase) × amp`. Amp 30 px, freq 0.3 Hz → ~3.3 s period. Drift time advances with `deltaMs × speedMult` so the slow power-up correctly dampens drift at the same rate it dampens scroll. Both top and bottom rects are re-sized via `setSize(PIPE_WIDTH, newHeight)` every frame so the ceiling/ground anchors stay solid while only the gap breathes. `gapY` clamped to `[PIPE_MIN_HEIGHT, playableHeight - gap - PIPE_MIN_HEIGHT]` so drift can't punch the pipes out of the play area at extreme amp values. Visual: amber palette (fill `0x664400`, stroke `0xffaa00`) — warm earthy tone reads as "this one moves" at a glance without collapsing into the Matrix green background. **(b) Zapper pipes (score ≥ 500)**: pulsing electric arc across the gap on a 2000 ms cycle, 40% active fraction → ~1.2 s safe window per cycle. Six-segment jittered zig-zag drawn each frame via `Graphics.lineStyle` + `moveTo`/`lineTo`/`strokePath`, segment jitter ±6 px for a "live" electric feel. `DANGER_WARNING` SFX fires on the rising edge of arc activation (once per pulse, per pipe) so the player gets an audible beat before the zap. `checkZapperCollision` runs only while arc is active and only inside the gap lane — pipe-body contact still falls through to `checkPipeCollision` (shieldable). Gap contact while active invokes `handleZapperDeath`: mirrors the R83.B1 `handleGroundDeath` pattern — zeroes lives, plays `GLASS_BREAK`, red camera flash (220 ms, 0.4 alpha), heavier 300 ms/0.02 shake, goes straight to game over skipping the shield branch of `handleCollision`. Insta-death matches the spec's "electric arc = insta-death" wording. Visual: deep red palette (fill `0x330000`, stroke `0xff3333`, arc `0xff5555`) so the pipe body reads "charged rail" and the arc itself reads "live current" at 0.9 alpha when active, 0.3 alpha during the 300 ms telegraph window. **(c) Bonus pipes (score ≥ 1000)**: narrower gap (`Math.round(PIPE_GAP × 0.7) = 84 px`) with a power-up seeded at the exact gap centre in `spawnBonusPowerUp`. Passing awards `BONUS_SCORE_MULT = 3×` base points on top of the combo × doublePoints stack. `LEVEL_UP` SFX plays on bonus pass as a triumphant cue layered over the standard `SCORE`. Score pop-text renders in cyan for bonus (`MATRIX_COLORS.CYAN_HEX`) vs. default green — risk/reward phrasing landed visually. `checkPipeCollision` now reads `pipe.gap ?? PIPE_GAP` so the narrower window is the real collider, not a fake-out. Visual: deep-cyan palette (fill `0x003355`, stroke `0x00ccff`) — reward-state read at a glance. **Kind selection (`pickPipeKind`)**: weighted bag gated by score. Weights {normal 10, moving 3, zapper 2, bonus 1}, unlocks cumulative. At score 1000+ → bag totals 16, distribution ~62% normal / 19% moving / 13% zapper / 6% bonus, so the core Flappy rhythm stays dominant even in the deep game. Pre-200: bag is just {normal 10} so every pipe is normal regardless of the random roll. **Per-pipe gap field**: `PipePair` now carries optional `gap`, `baseGapY`, `driftAmp`, `driftFreqHz`, `driftPhase`, `elapsedMs`, `arc`, `arcElapsedMs`, `arcActive`, `bonusSpawned`. All optional → the hand-crafted mock pipes in `GameScene.test.ts` (e.g. the pre-R84.B4 `checkPipeCollision` test doubles) stay type-compatible and execute the default 'normal' code paths. **Config**: new `PIPE_VARIANTS` block in `MatrixCloud/config.ts` with every tunable (unlock scores, weights, drift amp/freq, cycle/active/telegraph ms, arc color/alpha/segments/jitter, bonus gap scale, bonus score mult, palette for each kind). Single-source-of-truth so a playtest "zappers cycle too fast" note is a one-line change. **Cleanup**: `destroyPipe(pipe)` helper centralises the teardown — destroys top + bottom rects + `arc` if present. Wired into `clearBossBattle`, `shutdown`, and the off-screen splice in `updatePipes`. Zero leak risk on zapper arcs. **Tests**: 29 new in 8 `R84.B4` describe blocks — (1) 5 `pickPipeKind` score-gating tests (normal-only below 200, moving unlocks at 200, zapper at 500, bonus at 1000, never-below-unlock for moving), (2) 4 moving-pipe drift tests (quarter-cycle amplitude, elapsedMs accumulation, bound clamping on extreme amp, top/bottom rect resize), (3) 7 zapper tests (arc active at cycle start, inactive past 40%, danger-warning on activation edge, insta-death in gap, shield bypass, no death in pipe-body, no death when pipe far), (4) 3 `handleZapperDeath` tests (zero lives with shield, GLASS_BREAK SFX, combo reset), (5) 5 bonus-pipe tests (narrow gap, power-up in gap centre, 3× score mult, LEVEL_UP on bonus, no LEVEL_UP on normal), (6) 1 `checkPipeCollision` honours per-pipe gap test, (7) 2 style tests (4 distinct fills, 4 distinct strokes), (8) 2 `destroyPipe` tests (arc destroyed, no throw when arc absent). Full suite **2,289 pass + 2 todo** (+29 from R84.B3's 2,260). Mock rect extended with `setY` + `setSize` (required by moving-pipe animation); mock graphics extended with `beginPath` (required by zapper arc draw). Gates all green — tsc clean + lint 0 errors + vitest 138/138 Bird-scoped + build 7.31s. Next Stream D task: R84.B5 parallax deepening (3-layer far city / mid rain / near rain).)*
- [x] **R84.B5 [P1]** Parallax deepening — 3-layer (far city silhouette 0.1× scroll, mid rain 0.3×, near rain 0.7×). *(R84.B5 — shipped 2026-04-19 night. Replaces Matrix Bird's pre-B5 static `bg_city` image + single `addMatrixRain(10)` cohort with a proper 3-layer horizontal parallax stack reading depth bands the way the R84.B4 brief spec'd + the way NeoJump's `createParallaxBuildings` already proved works inside this arcade. **(a) Far layer** — promotes the existing `bg_city` texture from `add.image` to `add.tileSprite(WIDTH/2, HEIGHT/2, WIDTH, HEIGHT, 'bg_city')` at depth 0, alpha 0.15, tinted `MATRIX_COLORS.PRIMARY` (preserves the pre-B5 read). Horizontal scroll uses `tilePositionX += PIPE_SPEED × speedMult × dt × SCROLL_FACTOR_FAR (0.1)` = ~20 px/s at default speed — one field-write per frame, no per-pixel work. Because it's a TileSprite, the texture wraps automatically so no discrete-sprite management needed. When the texture isn't loaded (menu-only boot path or unit-test mode), `createParallaxFarLayer` early-returns → `parallaxFar` stays null → `updateParallaxLayers` skips it safely. **(b) Mid rain layer** — `createParallaxRainLayer(PARALLAX.MID)` spawns 16 Matrix-rain glyphs (katakana + digits, same `PARALLAX_CHARS` table) at font-size 11 px, alpha 0.22, depth 1. Vertical speed per-char randomised 35-75 px/s. `updateParallaxRainLayer` drifts each LEFT at `PIPE_SPEED × speedMult × 0.3 × dt` (60 px/s at default) AND DOWN at its per-char `verticalSpeed` (stored via `setData('verticalSpeed', n)`). Wrap logic: past bottom → reset to y=-20 with new x+char; past left edge → reset to x = WIDTH + fontSize with fresh char. **(c) Near rain layer** — same helper, `PARALLAX.NEAR` cfg: 10 glyphs, 15 px font, alpha 0.38, depth 2, vertical 80-140 px/s. Horizontal drift at 0.7× = 140 px/s. Fewer glyphs than mid because bigger glyphs need more breathing room → cluster-feel matches real parallax where close things are bigger+less-numerous and far things are smaller+denser. **Config block `PARALLAX` in `MatrixCloud/config.ts`** is single-source-of-truth for every tunable — 3 sub-blocks (FAR/MID/NEAR) + 35-line design-rationale comment explaining each constant's choice. Exports a new `ParallaxRainLayerConfig` type alias so tests + the scene's helper methods share the same structural type. **TIME_SLOW_FACTOR integration**: horizontal drift receives `speedMult` so the slow power-up dampens parallax drift at exactly the same rate it dampens pipe scroll + R84.B3's player physics, keeping the "time slowed for everything" feel coherent across the entire frame. Vertical rain fall intentionally does NOT scale — rain is an atmosphere effect independent of the player's time-dilation frame. **E2E test guard**: `createParallaxRainLayer` early-returns an empty group under `window.__TEST__` (same pattern `BaseScene.addMatrixRain` and R84.S2/S3 Snake rain use) because Phaser RNG isn't seeded in Playwright, so glyph positions would drift between runs and break visual baselines. **Shutdown**: new `destroyParallaxLayers()` called from `shutdown()` — calls `destroy()` on the far TileSprite, `destroy(true)` on each rain group (the `true` arg destroys all children text objects, not just the group container), nulls all three fields. Safe no-op when fields are already null. **Depth ordering preserved**: 0 (far) → 1 (mid) → 2 (near) → 3 (pipes) → 4 (field power-ups) → 5 (ground) → 6-9 (arcs/boss) → 10 (player). Every existing game-object depth stays untouched — only the pre-B5 `matrixRainGroup` (which had no explicit setDepth, so rendered at default 0) is replaced by the stacked mid/near layers at 1/2. Dropped the `matrixRainGroup` field + `addMatrixRain(10)` + `updateMatrixRain(...)` call chain (one source of truth per R84 guardrail #4 — no parallel rain systems). **Tests**: 20 new in `R84.B5 — 3-layer parallax` describe block. (1) **7 config sanity**: scrollFactors step far<mid<near, depths step 0/1/2 all beneath pipes (3), alphas scale up so closer reads brighter, near font > mid font (closer = bigger), vertical speed ranges positive + min ≤ max per layer, near vertical > mid vertical (closer = faster fall), densities positive. (2) **2 `createParallaxFarLayer`**: no-op when `bg_city` texture absent (proves graceful degrade), creates tinted TileSprite at FAR depth with right dimensions + alpha + tint when texture exists. (3) **3 `updateParallaxLayers`**: advances `parallaxFar.tilePositionX` at FAR.SCROLL_FACTOR × PIPE_SPEED × dt (20 px at 1s full speed), scales horizontal drift by speedMult so TIME_SLOW_FACTOR dampens the drift (proves time-slow integration), safe no-op when all three fields null. (4) **6 `updateParallaxRainLayer`**: drifts chars LEFT + DOWN at configured speeds, wraps past-left-edge to WIDTH+fontSize with fresh char, wraps past-bottom to y=-20 with fresh x+char, no-op when group is null, treats missing `verticalSpeed` data as 0 (defensive — blocks NaN propagation if setData ever fails), near layer drifts faster than mid per frame (parallax invariant). (5) **2 `destroyParallaxLayers`**: destroys all 3 layer handles + nulls fields (mid/near passed `true` to destroy children, far called bare — matches TileSprite vs Group APIs), safe to call when no layers exist. Full suite **2,309 pass + 2 todo** (+20 from R84.B4's 2,289, 158/158 Bird-scoped). Gates all green — tsc clean + lint 0 errors (4 pre-existing warnings unchanged) + vitest 12.79s + vite build 7.36s. Testing-doc line 117 "Background is a bit flat, needs more depth/movement" annotated `FIXED R84.B5`. **R84 stream scoreboard**: Stream A V1/V2/V3 `[x]`; Stream B P1-P12 `[x]`; Stream C S1+S2+S3+S4+S5+S6+S8+S9+S11+S12 `[x]` (10/12); **Stream D B1+B2+B3+B4+B5 `[x]` (5/12)**; Stream U UP1+UP2 `[x]`. Next Stream D task: **R84.B6 — Jump SFX tuning** (verify R83.B1 tri-pluck fully resolves Tom's "horrendous" complaint; if still prominent drop pitch-sweep depth + volume -20%).)*
- [x] **R84.B6 [P1]** Jump SFX tuning — R83.B1 tri-pluck; verify Tom's "horrendous" complaint fully resolved. If still prominent, drop pitch sweep depth + volume 20%. *(Shipped 2026-04-19: Tom's post-R83.B1 playtest still flagged jump as "the worst", triggering the prescriptive branch. Pitch sweep narrowed 20% — `birdFlap` library entry in `useSoundSystem.ts:65–83` now ramps 800→480 Hz (depth 320 Hz, down from 400 Hz), keeping the bright start but dropping the full-octave swoop that was reading as "whooshy". Scene volume for the flap key specifically drops another 20% — new `JUMP_VOLUME_SCALE = 0.6` constant on `MatrixCloudGameScene` branched from the shared `SFX_VOLUME_SCALE = 0.75` inside the `playSound` override (`GameScene.ts:108–110, 395–405`); branching on `key === SOUND_KEYS.BIRD_FLAP` keeps the score/hit/level-up cues Tom liked at the louder 0.75 baseline. Branching in the override rather than at each call-site means any future flap call (attract-mode preview, boss-intro) inherits the quieter level automatically — single source of truth per R84 guardrail. Library-level volumeScale explicitly NOT set so any future game that borrows `birdFlap` is not silently quieter. 4 new tests — 1 in `useSoundSystem.test.ts` locks the 800→480 Hz sweep on a triangle wave over 80ms (regression guard against the octave-drop revert temptation), 3 in `GameScene.test.ts` exercise the override directly by building a scene without the `playSound` stub so super.playSound's soundSystem.play call can be inspected for the per-key volumeScale. The override tests set up a fresh `MatrixCloudGameScene` with only the prototype `playSound` bound, then mock `registry.get('soundSystem')` to return a play-spy — the existing suite stubs `playSound` globally so its 161 jump/score/hit tests keep passing. Tests: 190/190 in `useSoundSystem.test.ts` + `GameScene.test.ts`. Gates: TSC clean, lint 0 errors (only pre-existing hook-deps warnings), build 7.31s.)*
- [x] **R84.B7 [P2]** Ground-death verification *(R84.B7 — shipped 2026-04-19 night. **Live-retest only**, no scene code change: R84.V3 static audit already confirmed the `handleGroundDeath()` path at `GameScene.ts:1079–1087` + the pre-collision gate at `480–483` (`if (this.playerY >= groundY) { this.handleGroundDeath(); return; }`) bypasses the shield-consume branch of `handleCollision()`. B7's job was to confirm the branch actually fires under real Phaser+WebGL runtime. **Playwright MCP run at 1280×800**: navigated to Bird MenuScene via landing card → iPod PLAY → intro red pill → MenuScene ENTER (focused canvas via `tabIndex=0` first because Phaser's window-bound keyboard plugin needs canvas focus to receive synthetic keys — same harness limitation documented in R84.UP1/UP2) → 5s countdown ran through "3" digit visible centre-canvas → gameplay unfroze → **no flap input given** → gravity pulled bird straight down from spawn centre-left → Game Over scene reached after ~7 s. Final HUD: score 0, pipes cleared 0, max combo 1.0x, power-ups 0, bosses 0. No scraping along the floor (the exact phrasing from Tom's 2026-04-19 testing-doc line 144 pre-R83.B1 complaint). Zero console errors across the full run. **Proof-by-observation logic**: with no flap + no horizontal input, gravity alone drives the trajectory; reaching Game Over before any pipe means the only possible death vector was the floor. If the branch were still broken, we'd see either (a) bird scraping along floor indefinitely (pre-R83.B1 behaviour) or (b) if a shield happened to be active, the shield ring would pop and the bird would bounce back up per the old `handleCollision` path. Neither happened. Unit coverage at `GameScene.test.ts:253–268` pins both branches (collision→handleGroundDeath, shield-active→lives=0) — 161/161 Bird tests green. Evidence `.playwright-mcp/r84-b7-{01-menuscene,02-post-pill,03-post-start,04-after-enter,05-ground-death}.png`. Testing-doc line 42 Known Issue `[x]`, line 144 gameplay row `[x]`. **R84 stream scoreboard**: Stream A V1/V2/V3 `[x]`; Stream B P1–P12 `[x]`; Stream C S1–S6+S8 shipping separately (S7 deferred); **Stream D B1+B2+B3+B4+B5+B6+B7 `[x]` (7/12)**; Stream U UP1+UP2 `[x]`. Next up: **R84.B8 — pause→resume 5s countdown live verification**.)*
- [x] **R84.B8 [P2]** Pause→resume 5s countdown verification *(R84.B8 — shipped 2026-04-19 night. **Primary deliverable: pin the R83.B1(c) contract in unit tests**, because static audit showed the code at `GameScene.ts:419–423` is correct (`resumeGame()` override calls `super.resumeGame()` then `startCountdown(5, () => {})` gated by `!isGameOver && !isCountingDown`) but the test file had **zero coverage** for the override — a future refactor could silently drop the countdown and leave the bird plummeting from an invisible spawn the instant the overlay lifts. 6 new tests in a `R84.B8 — Pause→resume 5s countdown` describe block (ends `GameScene.test.ts` at +122 lines). **Tests**: (1) **startCountdown fires with (5, function)** when not gameOver + not counting — baseline happy path contract, (2) **skipped on isGameOver** — override's gate blocks a post-death resume from scheduling a pointless countdown, (3) **skipped when already counting** — blocks stacking a second countdown on a rapid double-resume (e.g. dashbar click landing during the initial-run 5s countdown that also runs through startCountdown), (4) **ordering invariant** — `super.resumeGame()` must run BEFORE `startCountdown()` because the countdown uses `time.delayedCall(1000, ...)` to tick digits; if time.paused stayed true, digits would hang on "5" forever. Tested via invocationCallOrder on side-effects (physics.resume, tweens.resumeAll, time.paused=false) vs. startCountdown call. This invariant was **previously untested and is the most dangerous refactor trap** — a code-style cleanup that moved the super call after the early-return guard would break the countdown without breaking any observable compile/lint gate. (5) **update() early-returns during countdown** — the `isCountingDown` branch at line 427 blocks physics/pipe/HUD updates so the bird freezes in place; verified by asserting `handleInput`, `updatePlayer`, `updatePipes`, `updateHUD`, `updateParallaxLayers`, and `exposeTestState` are all NOT called when `isCountingDown=true`. (6) **onComplete callback is noop** — the override passes `() => {}` intentionally (unlike create()'s countdown which may fire extra logic). A future refactor passing a non-noop callback would fire extra logic 5s after play resumes — nonsensical state. Pin verifies the callback returns undefined and doesn't throw. **Mock surface extensions**: inline `beforeEach` inside the describe block stubs the BaseScene super call's touch-points missing from the global test mock (`scene.physics = { world: {}, resume, pause }`, `scene.tweens.resumeAll`, `scene.tweens.pauseAll`, `scene.game.canvas.focus`) rather than adding them to `createTestScene` to avoid disturbing the 161 unrelated existing tests that assume the minimal surface. **Live-verify deferred**: Playwright-MCP live-verify of the pause→resume flow is blocked by the harness limitation carried forward from R84.UP1/UP2 — synthetic KeyboardEvent cannot reliably reach Phaser's window-bound P-key plugin. The React dashbar ❚❚ button exercises the identical `PAUSE_REQUEST_EVENT → BaseScene._handlePauseRequest → togglePause → resumeGame` path, so the static+unit contract is authoritative; a live-run via dashbar click would be supplementary telemetry with no independent evidentiary value. **Gates**: tsc clean + lint 0 errors (4 pre-existing warnings unchanged) + vitest 167/167 Bird-scoped + 2,319 pass + 2 todo full suite (+6 from R84.B7's 2,313) + vite build 12.49s. Testing doc `MANUAL_TESTING_CHECKLIST_Matrix_Cloud.md` row 13 updated with the 6-test summary; Known Issue line 43 ticked `[x]` with R84.B8 reference; Pause & Resume section line 92 prose note annotated FIXED R83.B1(c), VERIFIED R84.B8. **R84 stream scoreboard**: Stream A V1/V2/V3 `[x]`; Stream B P1–P12 `[x]`; Stream C S1+S2+S3+S4+S5+S6+S8+S9+S11+S12 `[x]` (10/12); **Stream D B1+B2+B3+B4+B5+B6+B7+B8 `[x]` (8/12)**; Stream U UP1+UP2 `[x]`. Next Stream D task: **R84.B9 — Overall SFX volume audit** (R83.B1 dropped master 25%; verify the `SFX_VOLUME_SCALE = 0.75` is still right after B6's jump-only drop to 0.6; if Tom flags another -10% needed, drop to 0.65).)*
- [x] **R84.B9 [P2]** Overall SFX volume audit *(R84.B9 — shipped 2026-04-19 night. **Finding**: Tom's testing-doc lines 107 + 111 continued to flag "SFX too loud ... background music okay but sound effects too loud" after R83.B1's master drop (0.75) + R84.B6's jump-specific drop (0.6). The jump was calibrated against tonal harshness; the rest of the mix was still reading hot. **Decision**: drop master non-jump scale 0.75 → 0.65 (−13%, slightly past the plan brief's "−10% if needed"). Kept `JUMP_VOLUME_SCALE = 0.6` unchanged because B6 already calibrated it against tonal + volume issues and dropping further would make the flap whisper-quiet. New jump-vs-master ratio is 0.92 (was 0.80) — jump stays perceptibly quieter than score / level-up cues Tom liked. **Implementation**: single-line constant swap at `GameScene.ts:121` (`SFX_VOLUME_SCALE = 0.65`); comment block above extended with R84.B9 rationale capturing why jump stays at 0.60 not proportionally scaled. The `playSound` override at `GameScene.ts:404–410` is unchanged — branches on `key === SOUND_KEYS.BIRD_FLAP` for the 0.6 path, all other keys fall through to the 0.65 master. Branching in the override (rather than per-callsite) means any future flap call site — attract-mode preview, boss-intro, tutorial — inherits the quieter level automatically. **Tests**: updated 2 existing R84.B6 test expectations (`score` + `hit` from 0.75 → 0.65) and added 2 new regression guards in the `playSound override (R84.B6 + R84.B9)` describe block: (1) sweep-test iterating every non-flap SOUND_KEY used by the scene (menu, score, hit, levelUp, combo, collectible, dangerWarning, gameOver, glassBreak) asserting each passes `volumeScale: 0.65` — blocks a future refactor that mis-branches only one key, since the sweep would surface the regression the same day rather than after a bug report; (2) invariant test pinning jump `volumeScale` strictly less than non-jump master — a fail here means jump got accidentally raised to match master (flattening the B6 calibration) or master got dropped below jump (inverting the B6 intent). The invariant is expressed relationally rather than as hard numbers so a future proportional-scale refactor can still pass. **Total tests 169/169 Bird-scoped** (+2 from R84.B8's 167). Full suite **2,321 pass + 2 todo** (+2 from R84.B8's 2,319). Gates all green — tsc clean + lint 0 errors (4 pre-existing warnings unchanged) + vitest 508ms Bird-scoped + build 7.48s. Testing doc row 16 updated with R84.B9 values + regression-guard callout; Known Issue line 46 (`[P2]` Overall SFX too loud) ticked `[x]` with R84.B9 writeup; Audio check line 111 annotated FIXED. **R84 stream scoreboard**: Stream A V1/V2/V3 `[x]`; Stream B P1–P12 `[x]`; Stream C S1+S2+S3+S4+S5+S6+S8+S9+S11+S12 `[x]` (10/12, S7 optional + S10 pending); **Stream D B1+B2+B3+B4+B5+B6+B7+B8+B9 `[x]` (9/12)**; Stream U UP1+UP2 `[x]`. Next Stream D task: **R84.B10 — Troll frequency + power-up spacing** (verify PG4 ≥80px + PG5 max 4 still hold post-rename).)*
- [x] **R84.B10 [P2]** Troll frequency + power-up spacing (PG4 ≥80px / PG5 max 4) still hold post-rename. *(R84.B10 — shipped 2026-04-19 night. **Static + unit audit**, no scene code change — both invariants already enforced correctly post-R83.B1 + R84.B4 variants: PG5 hard cap at `GameScene.ts:713` (`if (this.pipes.length >= GAME_CONFIG.PIPE_MAX_ACTIVE) return`); PG4 enforced in `spawnPowerUp` at `GameScene.ts:922–930` via `safeStart = pipeX + PIPE_WIDTH + minDist` / `safeEnd = pipeX + spacing - minDist` window with centred fallback when window collapses (at `PIPE_SPACING_MIN = 240`, safe window = 240−50−160 = 30 px — still > 0, rule holds). Bonus-pipe power-ups intentionally bypass PG4 (seeded at gap centre = `PIPE_WIDTH/2` from left edge) as the R84.B4 threading reward — semantic exception pinned in the new tests so a future "safety fix" can't silently regress it. **8 new regression-guard tests** in `GameScene.test.ts` `R84.B10 Pipe + power-up spacing invariants` describe block (3 PG5: config sanity, hard-cap under 10× spawn pressure, cap-hit-is-no-op for both pipe-push and power-up side-effect; 5 PG4: config sanity, midpoint placement, random=0 left-edge clearance, random=1 right-edge clearance at `PIPE_SPACING_MIN`, bonus-pipe gap-centre exception). Suite 177 pass (+8 from the R84.B8/B9 pin count of 169). Gates all green — tsc clean, lint 0 errors (4 pre-existing warnings), build 7.58s, Bird scene 177/177. Testing doc lines 147–148 annotated with test-pin references. Next candidates: **R84.B11** (score-milestone stinger audio live-verify) or **R84.B12** (unit-test coverage refresh across B3/B4/B5/B1 gaps).)*
- [x] **R84.B11 [P2]** Score milestone stinger audio *(R84.B11 — shipped 2026-04-19 night. Task wording was verify-only but static audit surfaced a gap: R81.5 shipped **combo** milestones (whole-integer thresholds), not **score** milestones. LEVEL_UP already fires every 500 pts via `LEVEL_THRESHOLD`, covering 500/1000. The 50/100/250 early-run thresholds had no dedicated cue — the first two minutes of a run sounded flat. Added `MatrixCloudGameScene.SCORE_MILESTONES = [50, 100, 250]` + `milestonesHit: Set<number>` tracker; in `scorePipe` after the combo-milestone branch, loops thresholds and fires `SOUND_KEYS.COLLECTIBLE` once per crossing (`GameScene.ts:881–888`). `resetState` clears the set (`GameScene.ts:209`). Deliberately excluded 500/1000 from the list to avoid double-cueing the `LEVEL_UP` stinger — pinned with a regression test that fails if anyone adds them back. Coverage: 7 new tests in `GameScene.test.ts` `R84.B11 — Score Milestone Stingers` describe block (cross-at-50, cross-at-100, cross-at-250, no-refire, no-fire-at-500-level-up, resetState-clears, multi-cross single-fire). 184/184 green. Testing doc `MANUAL_TESTING_CHECKLIST_Matrix_Cloud.md:149` updated with file/line evidence.)*
- [x] **R84.B12 [P1]** Unit-test coverage refresh — new pipe types (B4) + parallax (B5) + slow-mode momentum (B3) + display-name audit (B1). *(R84.B12 — shipped 2026-04-19 night. **Pure-test refresh, no scene code change** — scope was to sweep the boundary/integration coverage the per-feature R84.B3/B4/B5 pins + the R83.S1 B1 rename incident left open. **(a) B1 display-name audit** — new dedicated file `src/components/games/phaser/MatrixCloud/MatrixBird.displayName.test.ts` with 9 tests against every user-visible rename surface: `TITLE_LINES['matrix-cloud']` equals `['MATRIX','BIRD']`, `GAME_TITLES['matrix-cloud']` renders block-char art (contains `█`, 11 rows = 5+blank+5), `GAME_REGISTRY` entry for id `matrix-cloud` carries title `'Matrix Bird'` yet keeps id stable for storage keying, every `GAME_ACHIEVEMENTS.matrixCloud` row reads `game: 'Matrix Bird'` with no row still saying `'Matrix Cloud'`, the eight expected achievement ids (`first_flight` … `sentinel_defeat`) all registered, `MatrixCloudMenuScene` constructor wires `title='MATRIX BIRD'` + subtitle `'One flap at a time through the cascade'`. Imports from the real config/registry/save-system modules (no mocks) so assertions match what users actually see. Minor touch-up: added `export` to `TITLE_LINES` in `src/lib/asciiArt.ts:60` (was bare `const`) so the audit can reach the display-name source of truth — the `GAME_TITLES` export already read from it so there was no semantic change, only visibility. **(b) B3 resetState power-up flag clears** — 4 new tests in a `R84.B12 — Coverage refresh gap-fills` describe block appended to `GameScene.test.ts` pinning that `resetState()` clears `timeSlowActive`, `doublePointsActive`, `shieldActive` individually AND the three together. The existing B3 trail-lifecycle tests verified the trail teardown branch of `resetState` but never asserted the power-up flag clears — a future refactor splitting the reset paths could silently leak stale flags into a death-retry (most visible as a shield-dead state: player tinted magenta with no collision forgiveness). **(c) B4 `updatePipes` → `updateMovingPipe` speedMult routing** — 2 integration-boundary tests. The direct `updateMovingPipe` tests above pass `deltaMs` unscaled; these drive through `updatePipes(dt, speedMult)` and assert the inner call receives `dt × 1000 × speedMult` under both `TIME_SLOW_FACTOR` and speedMult=1.0. This is the time-slow boundary that dampens moving-pipe drift coherently with horizontal scroll + parallax + player physics. Spies `checkPipeCollision` and `spawnPipe` to isolate the routing contract. **(d) B4 `pickPipeKind` partial unlock at ZAPPER tier** — 2 tests verifying at `score = ZAPPER_UNLOCK_SCORE` the function admits the full partial set `{normal, moving, zapper}` across the three bag buckets (rolls 0, 10/15+ε, 13/15+ε) AND still excludes `bonus` across 20 max-roll (0.999) attempts. Existing per-variant unlock tests pin each variant at its own tier; this pins the EXCLUSION on the tier immediately before `BONUS_UNLOCK`. **(e) B4 `scorePipe` bonus × doublePoints stacking** — 3 tests pinning the multiplication contract: bonus alone → `BONUS_SCORE_MULT × base = 30`, doublePoints alone → `2 × base = 20`, both → `2 × BONUS_SCORE_MULT × base = 60`. Losing either multiplier silently flattens the peak-reward moment that makes the double+bonus thread a meaningful "go for it" decision. **(f) B5 `createParallaxRainLayer` __TEST__ seam** — 2 tests. Positive: with `window.__TEST__ = true` the helper returns an empty group, `scene.add.text` is never called, `group.add` is never called. Negative: with `__TEST__` absent, `cfg.DENSITY` text children get spawned. Matches the guard idiom shared with `BaseScene.addMatrixRain` and SnakeClassic's play-area rain so Playwright visual-regression baselines stay deterministic — a refactor that reintroduces character spawns under `__TEST__` would re-break the visual suite. `afterEach` cleanup deletes the flag to avoid cross-test bleed. Single import change (`afterEach` added to the vitest import at top of file) to support the cleanup block. **Totals**: +13 gap-fill tests in `GameScene.test.ts` (184 → 197 Bird-scene pins) + 9 new `displayName.test.ts` tests = +22 Bird-scoped pins (was 184 post-B11 → 206 post-B12). Full suite **2,358 pass + 2 todo** across 48 files (was 2,334 post-B11). Gates all green — tsc `--noEmit` clean, lint 0 errors (4 pre-existing warnings unchanged), Bird-scoped vitest 2.29s, full-suite vitest 12.05s, vite build 7.56s. **No scene-code touched anywhere**, only the `asciiArt.ts` `export` visibility swap + the two test files. **R84 stream scoreboard**: Stream A V1/V2/V3 `[x]`; Stream B P1–P12 `[x]`; Stream C S1+S2+S3+S4+S5+S6+S8+S9+S11+S12 `[x]` (10/12, S7 optional + S10 pending); **Stream D B1+B2+B3+B4+B5+B6+B7+B8+B9+B10+B11+B12 `[x]` (12/12 — all Bird polish shipped)**; Stream U UP1+UP2 `[x]`. **Next candidates**: R84.S10 (Snake spec-to-ship pending, P1), R84.S7 (Snake optional, deferred), R84.BL (baselines refresh), R84.CI (CI gate).)*

**Stream U — Upstream cross-game investigation (Stream A audit surfaced, runs anytime after Stream A):**

- [x] **R84.UP1 [P1]** Upstream pause-overlay investigation *(R84.UP1 — shipped 2026-04-19 night. **Finding: no duplicate pause-overlay exists anywhere in the React+Phaser tree.** Static analysis: (a) grep for `<PauseOverlay>`, `aria-label*="pause"`, `ipod-pause`, PAUSED across `src/`: exactly one renderer at `GamePortal.tsx:622-632` inside the `{isPaused && (…)}` JSX gate — one `.ipod-pause-overlay` wrapper containing one `.ipod-pause-scrim`, one `.ipod-pause-text` ("PAUSED"), one `.ipod-pause-hint` ("PRESS P OR ❚❚ TO RESUME"). (b) Scene-side grep: zero "PAUSED" strings in any of the 12 Phaser scene files (`VortexPong`, `SnakeClassic`, `MatrixCloud`, `CtrlSWorld`, `MatrixInvaders`, `Metris`, `MatrixFrogger`, `NeoJump`, `AgentChase`, `RhythmHacker`, `CloudJumper`, `CodeBreaker`). (c) `BaseScene.togglePause` at `BaseScene.ts:157-178` emits `PAUSE_STATE_CHANGED_EVENT` consumed by `GamePortal.tsx:254-258` which flips the single `isPaused` useState — one-to-one data flow, no duplicate event handlers. (d) `App.tsx` has zero pause-related overlays (only `IntroOverlay`). (e) No CSS `::before`/`::after` pseudo-elements on `.ipod-pause-*` selectors in `animations.css:631-696`. Live DOM probe on Vortex Pong at 1280×800 after dashbar pause click: `{overlayCount: 1, scrimCount: 1, textCount: 1, hintCount: 1, dashbarAriaPressed: "true", dashbarClassHasIsPaused: true}`. Screenshot `.playwright-mcp/r84-up1-pong-paused-single-overlay.png` shows one amber radial scrim with PAUSED + hint. Snake + Bird inherit the identical `GamePortal` + `BaseScene` pair with zero game-specific pause-overlay rendering code — behaviour is bitwise identical. Tom's "triple-stacked" perception likely conflated the **three design elements (scrim+text+hint) inside one overlay** as three overlays — a visual-literacy trap common in colloquial playtest notes where the DOM actually has a single wrapper. R83.G3 architecture shipped single-source-of-truth by design; no fix needed. Classification: **stale complaint, code correct** per plan's close-out instruction. Cascade: R84.P10 + R84.S9 closed under this hunt; Bird pause tasks unaffected (R84.B7/B8 verify ground-death + countdown, not overlay count). Testing-harness limitation carried forward from UP2: Playwright MCP's synthetic KeyboardEvent doesn't reliably reach Phaser's window-bound P-key keyboard plugin, so the React-side dashbar ❚❚ button was used to exercise the same `PAUSE_REQUEST_EVENT` → `BaseScene._handlePauseRequest` → `togglePause()` path.)*
- [x] **R84.UP2 [P1]** Live-retest stale complaints batch *(R84.UP2 — Ralph live-retested all five at 1280×800 on 2026-04-19 via Playwright MCP: **D1 mute toggle** symmetric both ways via shared React `useSoundSystem.toggleMute` code path (aria-live region flips `Audio unmuted` ↔ `Audio muted`; BGM+SFX ON↔OFF together). **D2 click-to-play** overlay absent on Pong + Bird MenuScenes. **D5 launcher transition** clean iPod→MenuScene with no FOUC. **D10 iPod Matrix Bird text** reads `MATRIX / BIRD` ASCII + correct subtitle + ARCADE chip — text complaint stale, but preview image still a vortex (separate D9 → R84.B2). **D12 Bird MenuScene start-button** layout clean — START has ~30px gap above instructions and below key-legend, no overlap at 1280×800. All 5 complaints confirmed stale (Tom's 2026-04-19 testing notes predate R83 same-day fix commits). Evidence screenshots `.playwright-mcp/r84-up2-*.png`. Testing-harness limitation logged on D1: Playwright MCP cannot reliably drive Phaser's window-bound M-key via synthetic KeyboardEvent, so the React UI button was used to exercise the same `toggleMute()` method.)*

**Stream E — Baselines + Continuous Improvement:**

- [x] **R84.BL [P1]** Visual baseline regen *(R84.BL — shipped 2026-04-19 night. Ran `npx playwright test e2e/visual/ --project=chromium --update-snapshots` against the running darwin dev server at 1280×800. **Result: 19/19 specs passed, zero baseline files modified** — `git status` clean post-run. Interpreted: the darwin `chromium-darwin.png` snapshots were already synchronised with current render output; Streams B/C/D polish did not drift any captured surface beyond the config's `maxDiffPixelRatio: 0.2` / `threshold: 0.5` tolerance (`playwright.config.ts:108–117`). **Why this is the correct no-op outcome**: (a) visual specs target *landing grid*, *iPod portal chrome*, *modals*, *settings*, *achievements*, and *CTRL-S Phaser scenes* — NOT Vortex Pong / Matrix Bird in-game scenes (no direct baselines), so Pong atmosphere + Bird pipe/parallax rework had nothing to invalidate; (b) Snake's `portal-in-play.png` does capture in-scene Snake gameplay, but the generous tolerance absorbs Stream C's density/scanline/yellow-wall shifts without a diff spike; (c) Landing thumbnails are CSS-rendered static assets, not live Phaser previews, so R83/R84 display-name renames already landed in the baselines from R83.G6. **Linux baselines NOT regenerated this iteration** — Docker daemon offline at time of run (`docker info` → "Cannot connect to Docker daemon"), so `chromium-linux.png` snapshots stay on the R79.1 shipped set. Given darwin showed zero drift at the shared tolerance, linux drift is extremely unlikely; CI's linux-in-Docker run will catch any actual regression. If CI ever flags drift, fallback path: `npm run test:e2e:docker` with `--update-snapshots` to regen the linux bucket. Gates post-regen: tsc exit 0, eslint 0 errors + 4 pre-existing warnings (unchanged from R84.S10), no code diff. Single-commit plan update — no asset diff to land. **R84 stream scoreboard now**: Stream A 3/3 `[x]`, Stream B 12/12 `[x]`, Stream C 11/12 `[x]` (S7 deferred to R85), Stream D 12/12 `[x]`, Stream U 2/2 `[x]`, **Stream E BL 1/1 `[x]`** — only R84.CI remains open-by-design until Tom's review.)*
- [ ] **R84.CI [P2]** Continuous improvement bucket — only after Streams A-D + BL all `[x]`. **INTENTIONALLY never auto-ticked** (R82.13 pattern). Ralph keeps shipping polish until loop-cap. Priority order:
  1. a11y refinement across 3 games (focus-visible, prefers-reduced-motion, SR announcements on game-over)
  2. Performance micro-optimisations (Phaser texture atlases, object-pool audits, frame-budget profiling)
  3. Attract-mode integration — 3 games' hi-scores cycle through landing idle-attract
  4. Additional juice recipes (`juice:juice-audit` + `juice:juice-recipe` per R81)
  5. Sound-design mix balance across SFX + BGM layers
  6. Visual detailing — bezel shadows, gradient refinements, typography

  **R84.CI iteration log** (most recent first — Ralph appends on each CI burn; checkbox stays `[ ]` until Tom ticks it post-playtest):

  - **2026-04-19 night — CI-5: SR match-point announcements for Vortex Pong.** Picks up CI-4's explicit "next CI burns candidate" note ("extend the same pattern to Vortex Pong — Pong has natural announcement moments at match-point / game-point"). Closes the three-game a11y-priority-2 lane: CI-2 shipped SR score-milestones for Matrix Bird, CI-4 extended to Matrix Snake, CI-5 now plugs Vortex Pong. Before this burn, a screen-reader user playing Pong heard only the shared `gameOver` announcement at run-end (CI-1) — the mid-match tension beat of either side reaching `WIN_SCORE - 1` (9 points, one away from winning) had no SR surface even though sighted players read it immediately from the score digit flipping to 9. Post-CI-5 the scene fires a one-shot `matchPoint` event per side per run and the shared PhaserGame live region reads `Match point.` / `Opponent match point.` to AT users.

    1. **Why a new event type instead of overloading `scoreMilestone`**: Pong's scoring is win-condition-based (first to `WIN_SCORE=10`), not cumulative-threshold-based like Bird (50/100/250) or Snake (50/100/250/500). The "match point" state is semantic (`{ side: 'player' | 'opponent' }`) not numeric (`{ value: number }`). Keeping the payload shapes distinct means the contract stays explicit at every callsite and a future wrapper refactor can't accidentally cross the wires — if a Bird scene tried to emit a match-point event the TypeScript type system would reject the data shape.

    2. **New bridge event + type (`src/lib/phaser/types.ts`)**: extended `GameEventType` union with `'matchPoint'` and added `MatchPointEventData { side: 'player' | 'opponent' }`. The `'opponent'` literal rather than `'ai'` leaves the announcement game-agnostic for a hypothetical future 2-player Pong — the wording ("Opponent match point.") still works when the opposing paddle is a second human.

    3. **Builder helper (`src/lib/phaser/a11y.ts`)**: new `buildMatchPointAnnouncement({ side })` → `'Match point.'` when `side === 'player'`, `'Opponent match point.'` when `side === 'opponent'`, `''` for anything else. Defensive empty-string shape matches `buildScoreMilestoneAnnouncement` + `buildGameOverAnnouncement` so the wrapper's `if (msg)` guard treats a malformed payload as a no-op. Phrasing mirrors tennis/pong sports-announcer vernacular so it reads naturally through a screen reader; trailing period gives the SR an audible pause before the next live-region update, consistent with the other two builders.

    4. **Wrapper handler (`src/lib/phaser/PhaserGame.tsx`)**: added a `case 'matchPoint'` branch in `handleGameEvent` that calls the builder and updates `srAnnouncement` via the same nonce-bumping `setState` pattern used by `gameOver` + `scoreMilestone`. The nonce bump forces React to re-render even on identical sides, so a player who R-restarts a match and reaches 9 again re-hears the announcement rather than silent same-string suppression.

    5. **Scene state (`src/components/games/phaser/VortexPong/scenes/GameScene.ts`)**: `private matchPointAnnounced: { player: boolean; opponent: boolean } = { player: false, opponent: false }` — per-side flags so both sides can announce independently in the same run (9-9 tight-finish scenarios). `resetState` zeros both flags so an R-restart re-announces the first 9 on each side, matching Bird/Snake milestone reset semantics.

    6. **Scene helper `checkMatchPointAnnouncement(side)` + two callsites**: the helper enforces the `score === WIN_SCORE - 1` equality (exact threshold crossing, not a `>=` band) plus the flag guard. `checkGoals` calls it once per scoring branch: `'opponent'` right after `this.aiScore += 1` (line 653 area), `'player'` right after `this.playerScore += 1` + the existing FIRST_POINT achievement block (line 661 area). The `===` check means if a player were to somehow land on 9 twice (impossible under current rules but protects against a future refactor that rewinds state) the second pass still no-ops; the `>=` alternative would have fired a stray announcement on every score past 9 filtered only by the flag, giving a larger regression surface.

    7. **Why only match-point, not halfway or first-blood**: the `score` SFX already fires every goal for sighted players, and the shared `gameOver` announcement handles the actual win at 10. Match-point is the single moment where sighted players get **extra** information from the score display (the digit flipping to 9 reads as "one away" at a glance) that AT users didn't. Adding a halfway-at-5 announcement would introduce noise without closing an a11y gap; first-blood would double up on the existing FIRST_POINT achievement unlock which already announces via the achievement toast system.

    **Tests added**: `src/lib/phaser/PhaserGame.test.tsx` — 5 new specs under `describe('buildMatchPointAnnouncement')` pinning player-side phrase, opponent-side phrase, undefined-payload empty-string guard, empty-object guard, and unknown-side guard. `src/components/games/phaser/VortexPong/scenes/GameScene.test.ts` — 9 new specs under `describe('R84.CI-5 — Match point SR event emission')`:
    - *player side threshold emission* (1): `playerScore` crosses from 8 → 9 via `checkGoals`, fires exactly one `matchPoint` with `side: 'player'` — pins the end-to-end scoring path not just the helper.
    - *opponent side threshold emission* (1): `aiScore` crosses 8 → 9, fires `side: 'opponent'` — the mirror path.
    - *pre-threshold no-emit* (1): a goal at score 5 does NOT fire — pins the `score !== WIN_SCORE - 1` guard so the helper can't regress into a "fires every goal" pattern.
    - *flag-guard repeat suppression* (1): preseeded `matchPointAnnounced.player = true`, another goal crossing 9 does NOT re-fire — pins the one-shot contract.
    - *side-independence* (1): both sides reach 9 in the same run; both announcements fire in order (opponent first, player second matching the test driver sequence) — independence regression would silently drop one side.
    - *past-threshold no-emit* (1): seed `playerScore=9` + flag already set; the 9→10 goal does NOT fire `matchPoint` (the `gameOver` path owns that beat) — pins the flag + `===` both working together.
    - *resetState clears both flags* (1): preseeded mid-run state with both true, `resetState()` zeros them — pins the R-restart re-announce path matching Bird/Snake milestone resets.
    - *direct helper idempotence at the threshold* (1): `checkMatchPointAnnouncement('player')` called twice at `playerScore=9` fires exactly once — exercises the underlying contract directly rather than through checkGoals, so a refactor that routes the wiring differently still has the contract pinned.
    - *direct helper off-threshold no-op* (1): same helper called at scores 8 and 10 is a no-op on both — pins the `score !== WIN_SCORE - 1` guard directly.

    **Filter pattern in the scene specs**: `matchPointEmits()` helper filters `emitGameEvent.mock.calls` on `c[0]?.type === 'matchPoint'` before mapping to `c[0].data`. Essential because `checkGoals` fires many events per tick (`score`, `achievement`, sometimes `gameOver`) — a naive `toHaveBeenCalledWith` would either false-pass on another call matching loosely or false-fail on the call ordering. Same pattern CI-2 + CI-4 adopted for their milestone specs.

    **Gates**: tsc exit 0, eslint 0 errors + 4 pre-existing warnings unchanged (`KeyPanel.tsx:123`, `useSoundSystem.ts:745+885`, `PhaserGame.tsx:288` — note the `PhaserGame.tsx` warning line shifted 275→288 because the new `case 'matchPoint'` block of ~10 lines pushed the existing hook-deps warning down; same warning, same root cause, no new lint debt), `npm run build` green 7.64s (292 precache entries, 11916.06 KiB), full vitest sweep **2,433 pass + 2 todo / 49 files** (was 2,419 post-CI-4, +14 = 5 PhaserGame builder + 9 Pong scene). VortexPong-scoped totals: 220 scene tests (was 211) + 6 MenuScene + 16 config = 242 Pong-scoped green. PhaserGame wrapper 16 (was 11). Bird scene 223 unchanged, Snake scene 269 unchanged.

    **Three-game a11y-priority-2 lane now complete**: Bird (CI-2) + Snake (CI-4) + Pong (CI-5) all emit SR score/match-point announcements into the shared live region. Next CI burns candidate: focus-visible ring refinement on Pong/Snake/Bird inner controls (CI priority 1 residue — the last remaining a11y lane); performance micro-opts (CI priority 2 — object-pool audit for Bird pipes, Phaser texture atlases); attract-mode integration (CI priority 3); juice audits (CI priority 4); sound-mix balance (CI priority 5); visual detailing (CI priority 6). R84.CI stays `[ ]` by design (Tom ticks post-playtest); Tom-only terminator phrase still to be written.

  - **2026-04-19 night — CI-4: SR score-milestone announcements for Matrix Snake.** Picks up CI-3's explicit "next CI burns candidate" note ("SR announcements for Pong/Snake score or difficulty events — CI-2 pattern ready for Snake's power-up moments"). Before this burn, Snake's 50/100/250/500-pt landmark beats fired `levelUp` stingers for sighted players via the existing `POINTS_PER_SPEED_UP` speed-up cadence, but the shared PhaserGame SR live region (CI-1) only carried gameOver text + Bird's milestone announcements (CI-2) — AT users heard silence on every Snake landmark. Now the Snake scene emits `scoreMilestone` events into the React bridge on threshold crossings, and the wrapper routes them through CI-2's existing `buildScoreMilestoneAnnouncement` helper into the live region.

    1. **New scene state (`src/components/games/phaser/SnakeClassic/scenes/GameScene.ts`)**: `private static readonly SCORE_MILESTONES = [50, 100, 250, 500] as const` + `private milestonesHit: Set<number> = new Set()`. The first three thresholds match Matrix Bird's CI-2 set so a player running all three Phaser scoring games (Bird, Snake, future Pong) hears the same SR cadence at the same numeric landmarks — the AT equivalent of a consistent UI pattern language. The 500-pt threshold is Snake-specific: sits beyond the first speed-up wave (50-pt speed-up × 2 = 150-pt first-speed-up + continued ramping) and marks the longest-run landmark Tom flagged during V2 playtest. Stop at 500 deliberately — beyond that the level-up stinger already fires every 50 pts and the SR cadence becomes noise rather than signal.
    2. **New `checkScoreMilestones()` helper**: loops `SCORE_MILESTONES`, emits `scoreMilestone` + adds to `milestonesHit` for each freshly-crossed threshold. Idempotent (set guard makes repeat calls no-op). Deliberately emits NO SFX — unlike Bird's CI-2 which fires COLLECTIBLE because Bird had silence at the milestones, Snake already has the `levelUp` stinger at every `POINTS_PER_SPEED_UP` boundary matching the 50/100/250 thresholds. Adding a COLLECTIBLE on top would double-beat the audio layer; the SR event stays pure a11y channel.
    3. **Three callsite wiring**: `eatFood` (line 690, right after `this.score += points`), `activatePowerUp('reverse')` (line 873, after `this.score += REVERSE_PICKUP_BONUS`), `activatePowerUp('glitch')` (line 902, after `this.score += GLITCH_PICKUP_BONUS`). Three paths not one because reverse/glitch pickup bonuses (+50, +100) bypass `eatFood` entirely — a 45 → 145-pt glitch pickup would otherwise cross BOTH 50 and 100 thresholds with no announcement. Pinned by the `emits two milestones in one call when a large bump crosses both` spec so a future refactor that inlines the scoring couldn't regress silently.
    4. **resetState() teardown**: added `this.milestonesHit = new Set()` to `resetState()` so a restart re-announces the early cues — matches the AT experience of a sighted player re-hearing the levelUp stinger and mirrors Bird's CI-2 reset contract (`GameScene.ts:300` in Bird).

    **Tests added**: 9 new specs in `src/components/games/phaser/SnakeClassic/scenes/GameScene.test.ts` under `describe('R84.CI-4 — Score milestone SR event emission')`:
    - *threshold-value-not-score* (1): 45 + 10 = 55 crosses the 50 threshold, announcement reads `{ value: 50 }` not `{ value: 55 }` — protects against a refactor that accidentally sends the current score into the announcement.
    - *freshly-crossed guard* (2): emits for 100 only when freshly crossed after a preseeded `milestonesHit: Set([50])`; does NOT emit when already-hit — pins the set-membership contract.
    - *two-in-one-call crossing* (1): a jump from 45 → 145 pts fires both 50 and 100 announcements in order — the glitch-pickup edge case that motivated the helper extraction.
    - *top-threshold and clamp* (2): 250 + 500 landmarks both emit; a single 1200-pt call emits exactly 4 announcements (50/100/250/500) and nothing beyond — pins the "no noisy re-announcements at arbitrary high scores" contract.
    - *reset semantics* (1): `resetState()` clears `milestonesHit` to empty — pins the restart re-announce path.
    - *reverse-pickup bonus path* (1): `collectPowerUp('reverse')` with score seeded at 5 crosses 50 via `+REVERSE_PICKUP_BONUS` and fires the 50 announcement — pins that reverse bypasses eatFood but still announces.
    - *glitch-pickup bonus path* (1): `collectPowerUp('glitch')` with score 5 + `+GLITCH_PICKUP_BONUS` = 105 fires both 50 and 100 — pins both the bypass AND the two-in-one-call edge case via the real production path (not just the direct `checkScoreMilestones` call).

    **Gates**: tsc exit 0, eslint 0 errors + 4 pre-existing warnings unchanged (`KeyPanel.tsx:123`, `useSoundSystem.ts:745+885`, `PhaserGame.tsx:275`), `npm run build` green 11.63s (292 precache entries, 11915 KiB), full vitest sweep **2,419 pass + 2 todo / 49 files** (was 2,410 post-CI-3, +9 Snake CI-4 specs). Snake-scoped scene tests 269 green (was 260). Bird scene tests 223 green unchanged. PhaserGame wrapper tests 11 green unchanged (the `scoreMilestone` bridge + builder from CI-2 are reused verbatim — no wrapper changes this iteration).

    **Not addressed in this iteration** (next CI burns candidate): extend the same pattern to Vortex Pong (the third of the 3 a11y-priority-2 games — Pong has natural announcement moments at match-point / game-point); focus-visible ring refinement on inner controls of Pong/Snake/Bird (CI priority 1 residue — the remaining a11y lane); performance micro-opts (CI priority 2 — object-pool audit for Bird pipes, Phaser texture atlases); attract-mode integration (CI priority 3); juice audits (CI priority 4); sound-mix balance (CI priority 5); visual detailing (CI priority 6).

  - **2026-04-19 night — CI-3: reduced-motion gates for Bird's object-scale tweens.** Picks up CI-2's explicit "next CI burns candidate" note. CI-1 gated the two highest-hazard camera effects (shake + flash) on Bird; CI-3 extends the a11y contract to the next layer — the *decorative yoyo pulses* on field power-ups and the *popup-float tweens* (LEVEL UP text, shield-break ring, boss-name banner, boss-reward text, boss-entrance sweep). Before this burn, a reduced-motion user playing Bird would hear/see the scoring beat but still get persistent scale-pulsing on every collectable plus a stream of 400-2000ms popup floats every level / boss / shield-break — not a vestibular emergency on its own, but a coherent-with-CI-1 a11y gap. Post-CI-3 every object-scale motion in Bird either no-ops under `(prefers-reduced-motion: reduce)` (pulses) or collapses to an immediate cleanup (popups + reward text), while gameplay objects (power-up sprites, boss positions, score updates, SFX) remain fully perceptible.

    1. **Two new scene helpers (`src/components/games/phaser/MatrixCloud/scenes/GameScene.ts:169-225`)**: `safeDecorativePulse(config)` and `safeEphemeralPopup(config)` siblings to CI-1's `safeShake` / `safeFlash`. `safeDecorativePulse` returns `null` under reduced motion (target renders at its base scale — sprite visible, cue removed). `safeEphemeralPopup` skips `tweens.add` AND fires `config.onComplete` synchronously under reduced motion so the ad-hoc text/circle target destroy()s immediately rather than leaking into the scene graph. Wrapped in try/catch because popup cleanup is cosmetic and a destroy-failed target must not bubble into gameplay. Both `protected` so test sub-classes can spy without `(scene as any)` escape hatches — same pattern CI-1 set for `prefersReducedMotion`.
    2. **Six callsites refactored**: `spawnBonusPowerUp` (line 851) + `spawnPowerUp` (line 997) → `safeDecorativePulse` for their infinite-yoyo `scaleX/Y: 1.2, alpha: 0.7, yoyo, repeat: -1` attention-pulses. `onLevelUp` (line 955 LEVEL text, 40px float 1.5s), `showShieldBreakEffect` (line 1201, 4× ring explosion 400ms), `startBossBattle` boss-name banner (line 1282, 30px float 2s), `defeatBoss` reward text (line 1439, 50px float 1.5s) → `safeEphemeralPopup`. Each keeps the existing `onComplete: () => target.destroy()` path, so under reduced motion the destroy still runs without the visual.
    3. **Boss-entrance sweep inline-guarded (`startBossBattle` line 1271)**: this tween moves a *persistent* target (`this.boss` state object that `updateBossMovement` mirrors to `this.boss.sprite.setPosition` every frame) so the ephemeral-popup pattern doesn't fit — the target must land at its final x. Under reduced motion the 1500ms `Power2` sweep is replaced by a direct `this.boss.x = GAME_CONFIG.WIDTH * 0.75` write; next tick's `updateBossMovement` reads the new x through the clamp + setPosition pipeline. Inline guard rather than a helper because it's one field, not a full tween config worth abstracting.
    4. **Intentionally ungated**: the score popup +N float (line 902, 25px/400ms, fires every pipe-pass) and the slow-trail particle (line 1101, pure alpha fade, no motion). Score popups fire dozens of times per run and the motion is small — the per-frame matchMedia probe cost would exceed the a11y benefit, and CI-2's SR region already announces score milestones to AT users. Slow-trail is a pure opacity fade with no positional motion so it isn't a vestibular trigger. This is the same "low-risk at current scale" deferral CI-1 / CI-2 explicitly flagged.

    **Tests added**: 14 new specs in `src/components/games/phaser/MatrixCloud/scenes/GameScene.test.ts` under `describe('R84.CI-3 — a11y reduced-motion gates on object-scale tweens')`, organised as 4 sub-describes:
    - `safeDecorativePulse` (2 specs): forwards to `tweens.add` when motion-allowed; returns `null` + skips `tweens.add` under reduced motion.
    - `safeEphemeralPopup` (4 specs): forwards when motion-allowed (and does NOT prematurely fire `onComplete`); under reduced motion skips `tweens.add` + fires `onComplete` synchronously + returns `null`; no-ops cleanly when `onComplete` is absent; swallows `onComplete` errors (pins the try/catch safety net for destroy()-failed targets under a scene-restart race).
    - `Integration — infinite-yoyo pulses skipped under reduced motion` (3 specs): `spawnBonusPowerUp` still adds the sprite to `fieldPowerUps` even with the pulse skipped (guards against a regression that conflates "no motion" with "no element"); `spawnPowerUp` same pin; motion-allowed path still tweens.
    - `Integration — ephemeral popups destroyed immediately under reduced motion` (5 specs): `onLevelUp` destroys the level text synchronously; `showShieldBreakEffect` destroys the ring synchronously; `startBossBattle` destroys the boss-name banner synchronously + snaps `this.boss.x` to `WIDTH * 0.75` (double-pin covering both the popup wrapper AND the inline sweep guard — regressions dropping either fail a distinct assertion); motion-allowed `startBossBattle` asserts exactly 2 `tweens.add` calls (sweep + banner); `defeatBoss` destroys the reward text synchronously.

    **Gates**: tsc exit 0, eslint 0 errors + 4 pre-existing warnings unchanged (`KeyPanel.tsx:123`, `useSoundSystem.ts:745+885`, `PhaserGame.tsx:275`), `npm run build` green 13.08s (292 precache entries, 11915 KiB), full vitest sweep **2,410 pass + 2 todo / 49 files** (was 2,396 post-CI-2, +14 Bird scene specs). Bird-scoped 223 green (was 209). PhaserGame.test.tsx 11 green unchanged.

    **Not addressed in this iteration** (next CI burns candidate): focus-visible ring refinement on Pong/Snake/Bird inner controls (CI priority 1 residue — the remaining a11y lane); SR announcements for Pong/Snake score or difficulty events (CI-2 pattern ready for Pong's match-point / Snake's power-up moments); performance micro-opts (CI priority 2 — object-pool audit for Bird pipes, Phaser texture atlases); attract-mode integration (CI priority 3); juice audits (CI priority 4); sound-mix balance (CI priority 5); visual detailing (CI priority 6).

  - **2026-04-19 night — CI-2: SR announcements for score milestones.** Picks up CI-1's explicit "next CI burns candidate" note. Before this burn, Matrix Bird's 50/100/250 milestone beats (R84.B11) fired a one-shot COLLECTIBLE stinger for sighted players but the shared PhaserGame SR live region only carried `gameOver` text — AT users got silence on every milestone. Now the scene emits a new `scoreMilestone` event into the React bridge alongside the SFX, and the wrapper updates the live region with `Score milestone 50.` / `Score milestone 100.` / `Score milestone 250.` on the matching threshold crossings.

    1. **New bridge event type (`src/lib/phaser/types.ts`)**: extended `GameEventType` union with `'scoreMilestone'` and added `ScoreMilestoneEventData { value: number }`. Event payload carries the *threshold* (not the current score) so the announcement pattern-matches on the round number — a 50-milestone fired at score 55 still reads "Score milestone 50.", not "Score milestone 55.". Leaving the bridge game-agnostic means any future scene that wants milestone announcements just emits the same event and inherits the SR surface.
    2. **Builder helper (`src/lib/phaser/a11y.ts`)**: new `buildScoreMilestoneAnnouncement({ value })` → `"Score milestone N."`. Returns `''` for malformed payloads (undefined / empty object / non-finite value) so a future emitter mishap can't render `Score milestone NaN.` into the live region. The trailing period gives the screen reader an audible pause before the next live-region update, matching the game-over phrase shape. Kept in `a11y.ts` so `PhaserGame.tsx` stays a component-only module (React Refresh lint stays unchanged).
    3. **Wrapper handler (`src/lib/phaser/PhaserGame.tsx`)**: added a `case 'scoreMilestone'` branch in `handleGameEvent` that calls the builder and updates `srAnnouncement` via the same nonce-bumping `setState` pattern used by `gameOver`. Guard on truthy msg means the empty-string builder return no-ops rather than overwriting a still-useful prior announcement.
    4. **Bird scene emission (`src/components/games/phaser/MatrixCloud/scenes/GameScene.ts:925`)**: one-line addition inside the existing R84.B11 milestone loop — `this.emitGameEvent({ type: 'scoreMilestone', data: { value: threshold } })` next to the existing `playSound(SOUND_KEYS.COLLECTIBLE)` call. Placed inside the `!milestonesHit.has(threshold)` guard so the event is one-shot per threshold per run — matches the SFX cadence so sighted + AT users hear/read exactly one cue per milestone. `resetState` already clears `milestonesHit` (R84.B11 coverage), so a restart re-fires both cues.

    **Tests added**: `src/lib/phaser/PhaserGame.test.tsx` — 5 new specs under `describe('buildScoreMilestoneAnnouncement')` pinning full-stop termination, threshold-not-score output, undefined-payload empty-string guard, empty-object guard, and NaN/Infinity guard. `src/components/games/phaser/MatrixCloud/scenes/GameScene.test.ts` — 4 new specs under `describe('R84.CI-2 — Score milestone SR event emission')` pinning the emission at the 50 threshold (threshold value, not score), the 100 threshold after a preseeded 50-hit, no-re-emit on a repeat pass, and no-emit on the 500-pt level-up boundary (where LEVEL_UP already narrates). Filters `emitGameEvent.mock.calls` on `c[0]?.type === 'scoreMilestone'` so the helper cleanly separates milestone emits from the `score` / `gameOver` / etc. emits the scene fires in the same call.

    **Gates**: tsc exit 0, eslint 0 errors + 4 pre-existing warnings unchanged, `npm run build` green 8.74s, full vitest sweep **2,396 pass + 2 todo / 49 files** (was 2,387 post-CI-1, +9 = 5 PhaserGame + 4 Bird scene). Bird-scoped 209 green (was 205), PhaserGame.test.tsx 11 green (was 6).

    **Not addressed in this iteration** (next CI burns candidate): focus-visible ring refinement on the 3 games' inner controls, reduced-motion gates for Bird's large object-scale tweens (power-up pulse, score popup floats — still low-risk at current scale), SR announcements for Pong/Snake score or difficulty events if tuned in future, performance micro-opts (CI priority 2), attract-mode integration (CI priority 3).

  - **2026-04-19 night — CI-1: a11y priority-1 shipped (prefers-reduced-motion + SR game-over announcements).** Two load-bearing changes:
    1. **Matrix Bird reduced-motion gate (`src/components/games/phaser/MatrixCloud/scenes/GameScene.ts`)**: Bird was the odd game out — Pong has 8+ `prefers-reduced-motion` gates, Snake has 11+, Bird had **zero**. Every level-up, pipe-pass, boss-spawn, zapper-death, collision, and game-over triggered a camera shake + full-screen flash with no media-query opt-out. Added `prefersReducedMotion()` + `safeShake(duration, intensity)` + `safeFlash(duration, r, g, b, ...)` helpers on the scene (protected for test sub-classing), then refactored all 10 `this.cameras.main.shake(...)` + `this.cameras.main.flash(...)` callsites to route through the helpers. Under reduced motion the camera effect no-ops entirely; SFX, scoring, sprite tweens, and all non-camera effects still fire so the event remains perceptible without the vestibular/strobe hazard. Pattern matches Snake's dread-shake-loop gate (`SnakeClassic/scenes/GameScene.ts:1320-1340`).
    2. **Shared PhaserGame SR live region (`src/lib/phaser/PhaserGame.tsx` + `src/lib/phaser/a11y.ts`)**: all 12 Phaser scenes emit `gameOver` events into the React layer but no DOM surface announced the outcome to screen readers — sighted players saw the GameOverScene, AT users heard nothing. Added a visually-hidden `<div role="status" aria-live="polite" aria-atomic="true" className="sr-only">` rendered by the wrapper. On `gameOver` events the wrapper updates the region's text via `buildGameOverAnnouncement({ score, reason })` — pure helper extracted to `a11y.ts` so PhaserGame.tsx stays a component-only module (React Refresh stays green, lint warning count unchanged). The `key={nonce}` forces React to re-render even on identical announcements so replay-same-score re-announces. Landing-grid + iPod portal already have their own live regions (`GamePortal.tsx:565`) — this plugs the in-game gap.

    **Tests added**: `src/components/games/phaser/MatrixCloud/scenes/GameScene.test.ts` — 8 new specs under `describe('R84.CI — a11y prefers-reduced-motion gates')` pinning `prefersReducedMotion` + `safeShake` + `safeFlash` + integration with `handleCollision`. `src/lib/phaser/PhaserGame.test.tsx` (NEW) — 6 specs covering `buildGameOverAnnouncement` string shapes (bare / score-only / reason-only / both / empty-blob) + rendered region assertions (role=status, aria-live=polite, aria-atomic=true, sr-only class, empty initial text). Local Phaser mock in the new test file mirrors just enough of `events.once` which the shared setup.ts mock doesn't wire.

    **Gates**: tsc exit 0, eslint 0 errors + 4 warnings (unchanged — pre-existing `KeyPanel.tsx:123` / `useSoundSystem.ts:745+885` / `PhaserGame.tsx:262` all untouched), `npm run build` green, full vitest sweep 2387 tests / 49 files pass (2 todo — pre-existing). All 3-game scene tests (Pong 211, Snake 260, Bird 205) green including the 8 new Bird a11y specs + 6 new PhaserGame SR specs.

    **Not addressed in this iteration** (next CI burns candidate): focus-visible ring refinement on the 3 games' inner controls, reduced-motion gates for Bird's large object-scale tweens (power-up pulse, score popup floats — low-risk at current scale so deferred), SR announcements for score milestones (Bird fires 50/100/250 stingers; could announce `Score milestone 100` for AT users), performance micro-opts (CI priority 2).

### R84 Verification Plan

1. **Stream A complete** → 3 fresh Known-Issues checklists filled in; NOT-working R83 items documented.
2. **Streams B/C/D complete** → Tom plays each game 3-5 min, verifies new scope lands.
3. **Stream E complete** → visual baselines green, CI iterations shipped until cap.
4. **Automated gates**: every iteration `npx tsc --noEmit` + `npm run lint` + `npm run build` + `npm test --run`. Red blocks commit.
5. **Post-phase playtest**: Tom hand-plays each game end-to-end, ticks Known Issues, writes terminator.

### R84 Terminator

All of:
- R84.V1, V2, V3 `[x]`
- R84.P1 – R84.P12 `[x]`
- R84.S1 – R84.S12 `[x]` (S11 rename completeness, S12 HUD clipping added post-audit)
- R84.B1 – R84.B12 `[x]`
- R84.UP1, R84.UP2 `[x]` (upstream pause-overlay + live-retest stale complaints)
- R84.BL `[x]`
- R84.CI stays `[ ]` (continuous-improvement; Tom ticks post-review)
- Gates green: lint + build + unit + E2E + visual
- Tom manually writes **"R84 COMPLETE — 3-game polish + verification shipped"** to Status

`loop.sh` hard cap: **50 iterations**. Ralph **never auto-writes** the terminator phrase.

### R84 Post-R84 Plan (NOT Ralph's work — Tom's forward-look)

R85 = per-game polish for remaining 8 games (Invaders, Metris, Frogger, NeoJump, AgentChase, Rhythm, CloudJumper, CodeBreaker). Testing docs template-populated by R83.G7 — Tom plays each ~3 min, fills Known Issues, R85 scope built from those notes.

### R84 Discovered Work

_(Ralph logs unexpected findings here during execution — do NOT create new R84.x tasks mid-iteration; log here for Tom's triage.)_

**From R84.V1 (Vortex Pong static audit, 2026-04-19):**

- **D1 [P1, Pong+Snake] — LIVE-RETEST (R84.UP2, 2026-04-19): STALE. Mark resolved.** Live driven from Pong in-game: clicking the React Audio-Settings `SOUND ON` toggle flips aria-live region `Audio unmuted` → `Audio muted`, BGM ON → OFF, SFX ON → OFF. Clicking it again flips everything back (`Audio unmuted`, BGM ON, SFX ON) — symmetric both ways. The Phaser-side `BaseScene.M` key-bind (`BaseScene.ts:112-113`) delegates via `toggleMute()` (`BaseScene.ts:263-267`) → registry `IS_MUTED` flip + `'mute'` event → React `useSoundSystem.toggleMute`, i.e. the **exact** method exercised by the React button. Tom's 2026-04-19 testing-doc note pre-dates R83.G1 commit `6540b52`. *Original static-audit notes below retained for audit trail.* M-key unmute reported broken by Tom in both Pong and Snake testing docs. Static audit of `useSoundSystem.ts:1208–1237` (R83.G1 commit `6540b52`) shows symmetric toggle: mute sets masterGain=0 + saves preMuteConfigRef; unmute restores gain, restores {music,sfx}, and nudges paused BGM element back into play. Logic looks correct. **Testing-harness caveat**: Playwright MCP's `page.keyboard.press('M')` does not reliably reach Phaser's window-bound keyboard plugin because synthetic KeyboardEvent dispatch bypasses Phaser's internal state machine; future Playwright-style E2E coverage for M-key should use `page.keyboard.type` after a `click()` on the canvas bounding box, or call `scene.toggleMute()` directly via test-mode seam.

- **D2 [P1, cross-game] — LIVE-RETEST (R84.UP2, 2026-04-19): STALE. Mark resolved.** Ralph drove the Pong + Bird MenuScenes live at 1280×800 and neither renders a `click to play` overlay after PLAY is pressed. The MenuScene is reached cleanly from the iPod card → SPACE/ENTER → scene appears with `START` button. No additional transparent click-catcher layer was detected in the DOM inspection. R83.G2 is live-confirmed shipped. *Original note below retained.* Click-to-play overlay still reported by Tom in Pong + Snake docs. Grep of the Pong chain + `BaseScene` returns zero "click to play" strings. Overlay (if any) lives upstream — probably `GamePortal.tsx` or the landing-page transition mask.

- **D3 [P1, cross-game] — LIVE-RETEST (R84.UP1, 2026-04-19): STALE. Mark resolved.** Ralph hunted the React+Phaser tree + live-DOM-inspected a paused Vortex Pong at 1280×800: exactly one `.ipod-pause-overlay` wrapper renders, containing one scrim + one PAUSED text + one hint line. `GamePortal.tsx:622-632` is the sole renderer; zero scene-side "PAUSED" strings in any of the 12 Phaser scenes. Tom's "triple" likely conflated the three design elements (scrim+text+hint) **inside** one overlay as three stacked overlays. R83.G3 single-source-of-truth architecture ships as intended. Screenshot `.playwright-mcp/r84-up1-pong-paused-single-overlay.png`. Closes R84.UP1 + cascades to R84.P10 + R84.S9.

- **D4 [P2, Pong]** MenuScene start button Y ratio = 0.75 via `BaseScene.MENU_START_BUTTON_Y_RATIO`; Pong HOW-TO-PLAY band occupies y = 0.52..0.63. Clean 12% gap. Tom's "still overlapping" playtest note likely stale vs. pre-R83.G4 layout. Low priority; verify at 1280×800 during R84.BL baseline review.

- **D5 [P2, cross-game] — LIVE-RETEST (R84.UP2, 2026-04-19): STALE. Mark resolved.** Driving the iPod `▶` button for Matrix Bird + Vortex Pong transitioned cleanly into each MenuScene with no FOUC, no black hole, no stale-canvas artefact. The `GAME_TRANSITION_READY_EVENT` portal-mask reveal is working. R83.G5 live-confirmed shipped. *Original static-audit note retained.* R83.G5 "Matrix launcher" transition implemented as `GAME_TRANSITION_READY_EVENT` fired one rAF after Phaser `ready` (`PhaserGame.tsx:216–232`).

- **D-P1 [P2, scope]** R84.P1 (5-second countdown for Pong) is ALREADY IMPLEMENTED at `GameScene.ts:121`. Plan task description is stale. When R84.P1 is picked up under Stream B, it should be a quick live-verify + auto-tick rather than new scene work. Suggested: demote P1 to a verification sub-task or merge into R84.V1's evidence table.

**From R84.V2 (Matrix Snake static audit, 2026-04-19 night):**

- **D6 [P2, Snake]** R83.S1 rename did not cover the ASCII block-letter title. `src/lib/asciiArt.ts:62` still has `'snake-classic': ['SNAKE', 'CLASSIC']`; `GAME_TITLES` is pre-computed at module load from this map, imported by `src/components/GamePortal.tsx:4`, and rendered as a huge ASCII banner overlaid on the portal hero card at `GamePortal.tsx:658`. Users see "SNAKE / CLASSIC" in block letters even though the registry title is "Matrix Snake". **Fix**: change the array to `['MATRIX', 'SNAKE']`. Must re-run `src/lib/asciiArt.test.ts` (tests assert `GAME_TITLES['snake-classic']` exists, contains `█`, no row > 60 chars — all satisfied by `MATRIX` (6 chars × 5-per-glyph + padding ≈ 34 chars) + `SNAKE` (5 × 5 ≈ 29). Must also regen visual baselines touching the Snake portal card. **Assigned**: fold into R84.S1 iteration OR carve a dedicated `R84.Sx` rename sub-task. NOT fixed in V2 per "verification commits are doc-only" ordering rule.

- **D7 [P2, Snake]** R83.S1 rename missed the achievements panel. `src/hooks/useSaveSystem.ts:148–154` has 7 Snake achievement rows all hard-coded with `game: 'Snake Classic'`. These strings surface in the Achievements modal (grouped-by-game sections) and in `useAchievementManager.test.ts:279–297` as test expectations against `stats.byGame['Snake Classic']`. **Fix**: switch labels to `'Matrix Snake'` and update the test expectations in lockstep (tests are an internal source of truth — no migration). **Assigned**: same as D6, fold into Stream C rename sub-task. NOT fixed in V2.

- **D8 [P2, Snake]** `SnakeGameScene.createHUD` places text at `leftX=100` and `rightX=700` (`GameScene.ts:216–236`) but `GAME_CONFIG.WIDTH=640`, so `rightX=700` is 60px off-screen. At default `Phaser.Scale.NONE` this would clip; current visible HUD on the right hints at a scale-mode quirk OR intentional use of a wider camera bounds. **Action needed**: live-inspect where the POWER-UPS / FOOD / power-up-indicator texts actually render at 1280×800 portal viewport. If clipped, R84.S layout fix + update HUD positioning math. Low priority — not a blocker; possibly Tom never noticed because the right HUD column is rarely populated.

- **D-S1-math [P1, Snake, evidence]** Yellow wall asymmetry (Tom's reproduction note) confirmed by static geometry: `GRID_OFFSET_Y=20`, walls at grid-rows `-1` and `GRID_ROWS` with `CELL_SIZE=16`. Top wall pixel span 4..20 (margin=4), bottom wall pixel span 340..356 (margin=44) on a 400px canvas. Balanced target: `GRID_OFFSET_Y=40` → symmetric 24px margin top/bottom. This is NOT a new discovery (R84.S1 already scoped it) but the math wasn't in the plan; logging here so the fixer can start from the equation rather than re-derive it.

**From R84.V3 (Matrix Bird static audit, 2026-04-19 night):**

- **D9 [P2, Bird]** Preview image on portal card is NOT a bird. `src/data/gameRegistry.ts:55` points at Cloudinary URL `.../A_surreal_thumbnail_of_a_glowing_neon_green_vortex_spiraling..._gyatls.png` — filename self-describes as a vortex illustration, matches Tom's complaint *"still has a picture of a cloud lol"*. Cloudinary URLs are immutable, so the fix requires either (a) uploading a new bird-sprite preview and swapping the URL, or (b) moving to an import-based preview (like Matrix Invaders + Metris already do at `gameRegistry.ts:2–3`). Path (b) is more robust and removes the external dependency. **Assigned**: R84.B2 (preview image swap — explicitly scoped in plan). Regen visual baselines under R84.BL after swap.

- **D10 [P2, Bird] — LIVE-RETEST (R84.UP2, 2026-04-19): STALE for text. Mark text-resolved; preview image still vortex (that is D9 → R84.B2).** Ralph navigated the iPod portal to position 4 at 1280×800. The ASCII hero banner reads **`MATRIX / BIRD`** (correct). The subtitle reads *"Navigate through gaps in the digital storm — one wrong move and you crash."* (correct Bird copy). The category chip reads **ARCADE**. However the preview image behind the title is still the green vortex illustration — that is a **separate issue (D9)** already routed to R84.B2 preview-image swap. *Original static-audit note retained.* Tom's *"text still says matrix cloud (on the ipod)"* complaint CANNOT be reproduced — the text everywhere reads Matrix Bird.

- **D11 [P2, Bird, perf]** Tom's verdict *"can be better with performance upgrades"* is directional, not a specific bug. No static perf measurement available. Scope marker for R84.CI bucket priority 2 (performance micro-optimisations). When R84.B4 (pipe variety) and R84.B5 (3-layer parallax) ship, keep an eye on frame budget — parallax layers tend to be the first thing to stutter on low-end hardware. Object-pool audit for pipes + power-ups is a candidate since the game currently `destroy()`s + recreates every off-screen element.

- **D12 [P2, Bird, menu] — LIVE-RETEST (R84.UP2, 2026-04-19): STALE. Mark resolved.** Ralph captured Matrix Bird MenuScene at 1280×800. Layout reads cleanly top-to-bottom: `MATRIX BIRD` title (y≈220–260) → subtitle *"One flap at a time through the cascade"* (y≈320) → instructions *"SPACE / Click to flap", "Fly through the gaps", "Collect power-ups, defeat bosses"* (y≈395–475) → **`START`** button at y≈505–555 — ~30px clear gap above and below START, no overlap with instructions or with the key legend (`ESC: Exit  P: Pause  M: Mute`) at y≈625. No `click to play` overlay. R83.G2 + R83.G4 live-confirmed shipped for Bird. *Original static-audit note retained.* Tom: *"start button needs to be further down and click to play removed"* on MenuScene.

- **D13 [P3, Bird, cleanup]** Dead constant: `GAME_CONFIG.PARTICLE_COUNT = 30` at `config.ts:94` is declared but never referenced anywhere in the MatrixCloud game (grep returned only the declaration). Low-priority cleanup candidate — either wire it into the camera flash / death-effect particle count, or delete. Not a V3 blocker.

---

## Future Asset Strategy (Tom's note, 2026-04-18)

> *"I will be adding a custom game asset pack in the future (purchased). Like a mega pack for consistency across games. Free ones are cool for now and I went a bit over the top. When publishing final version I'll source a sound effects pack, sprite pack, possibly a background pack. For future development, be a true professional — also it's a pain finding free stuff and hoping I can use it without referencing. I'll try to create my own music track and images for custom aspect, but this is my playground and I love trying new things and breaking things."*

**Implications for future phases (NOT R82)**:
- When Tom sources a licensed mega-pack, a new phase will replace per-game sprites/SFX/BGM with the consistent set. Estimated ~1 iteration per game asset-swap (11 games = 11 iterations).
- Existing `public/assets/<game>/` structure is ready for this — just replace files, re-run visual baselines.
- Current assets (deployed R78 + R80) are from the 638 MB free dump. Licensing-fine for playground use; pack swap happens before any public launch.
- Tom may also create custom music + imagery per game — these layer on top of the pack. No blocker for purchased pack work.
- **When the time comes**: new phase (R8X or similar), full `ASSETS_NEEDED.md` refresh per game with licensed pack paths, 1-iteration-per-game swap. Not a planning priority now.

---

R81 closed — see [COMPLETED_WORK.md § R81](COMPLETED_WORK.md#r81--arcade-wide-juice-polish--repo-trim-2026-04-15).

R80 closed — see [COMPLETED_WORK.md § R80](COMPLETED_WORK.md#r80--ctrl-s-flagship-rewrite).

---

---

R78 closed — see [COMPLETED_WORK.md § R78](COMPLETED_WORK.md#r78--assets--infrastructure).

---

### Phase 0a — Global Asset Extraction (residue)

- [ ] Background textures, sci-fi UI panels, additional icon packs (still in ZIPs)

### Phase 0b — Per-Game Asset Deployment (residue)

Sprites/audio not yet deployed. CTRL-S World is intentionally deprioritised — its assets should be wired into the Phase 7 rewrite, not the existing DOM implementation.

**Asset deployment status** (R61 full audit — sourced from ASSETS_NEEDED.md files):

| Game | Deployed Files | Real Art? | Audio? | Biggest Gap |
|------|---------------|-----------|--------|-------------|
| Matrix Frogger | 11 sprites + 6 audio | Core complete | **Full set** | Enhancement sprites (Neo, agents, tiles) |
| Vortex Pong | 10 sprites | Core complete | Global SFX | Power-up icons, audio |
| Neo Jump | 15 sprites | All states + platforms | Global SFX | Backgrounds, 2 enemy types, UI |
| Code Breaker | 14 sprites | Core + all 6 power-ups | Global SFX | Damage states, enemies, animations |
| Agent Chase | 13 sprites | All characters | Global SFX | Death anim, dots, audio |
| Metris | 7 sprites | Tetrominos only | Global SFX | Ghost piece, grid, backgrounds |
| Matrix Invaders | 7 sprites | Core functional | Global SFX | Boss, power-ups, animations |
| Cloud Jumper | 7 sprites | Player + 4 clouds | Global SFX | Backgrounds, collectibles, obstacles |
| Matrix Cloud | 3 sprites | Bare minimum | Global SFX | Bosses, power-ups, parallax |
| Snake Classic | 5 sprites | Minimal (no directions) | Global SFX | Directional variants, modes, boss |
| Rhythm Hacker | 5 audio tracks | 100% procedural visuals | 5 tracks | All visual assets, note charts |
| CTRL-S World | 0 | Nothing | Nothing | Everything (defer to Phase 7) |

**Critical gaps** (blocking for proper gameplay):
- [x] **Audio is the biggest cross-cutting gap**: Only Matrix Frogger has deployed audio. SFX kit and music tracks exist in the dump but none have been extracted per-game.
- [ ] **Rhythm Hacker**: 5 music tracks deployed but all visual assets are procedural. Note charts (timing data) are generated procedurally via `charts.ts` — works but could be improved with hand-authored charts.
- [ ] **CTRL-S World**: Zero assets. Everything sourced but needs extraction. **Defer until Phase 7.**

**Partial deployment** (~25–75% done — core sprites shipped in R78.2–R78.4, remaining items are polish):
- [~] **Snake Classic**: ~~head/body/food/bomb/eyes/wall sprites~~ (R78.2). Remaining: 4-directional variants, Agent Smith, boss, grid tiles, power-up icons
- [~] **Matrix Cloud**: ~~city skyline bg, ground tile~~ (R78.2). Remaining: player death anim, boss sprites, power-up icons
- [~] **Metris**: ~~hologram UI panels~~ (R78.2). Remaining: ghost/variant tiles, effect animations
- [~] **Matrix Invaders**: ~~laser bullet sprites~~ (R78.2). Remaining: boss, death explosion, power-up icons
- [~] **Cloud Jumper**: ~~parallax bg layers, collectibles, Crabby + cannonball obstacles~~ (R78.3). Remaining: player death anim, 3 backgrounds
- [~] **Matrix Frogger**: ~~Neo Cyberpunk player, TopView Robot enemies, hologram power-ups~~ (R78.3). Remaining: environment tiles, chasing agents
- [~] **Code Breaker**: ~~breakout frame bg~~ (R78.3). Remaining: Agent Smith anims, ball effects, portal
- [~] **Agent Chase**: ~~frightened ghost, eyeball eyes, dot/pellet sprites~~ (R78.4). Remaining: player death anim, map layouts
- [~] **Neo Jump**: ~~flying enemy, energy bolt projectile~~ (R78.4). Remaining: shooting enemy, backgrounds, altitude meter
- [~] **Vortex Pong**: ~~4 power-up icons, multi-ball, board bg~~ (R78.4). Remaining: goal flash, combo text

#### Asset Integration Pattern

For each game, the pipeline is:
1. Extract raw assets from dump to `desiredassets/[game]/raw/`
2. Process (recolour, resize, atlas-pack) to `desiredassets/[game]/processed/`
3. Copy final assets to `public/assets/[game]/`
4. Update BootScene to load from new paths
5. Mark `[~]` to `[x]` in ASSETS_NEEDED.md

---

### Phase 1/2 — Global Asset System

- [ ] Research `src/lib/assets/` design
- [ ] Create `src/lib/assets/AssetManager.ts` — centralised font, spritesheet, and audio loading
- [ ] Create global spritesheet atlas system for shared sprites across games

---

### Phase 6 — Polish Residue

- [ ] **Performance profiling** (60fps on all games) — requires manual browser testing
- [ ] **Accessibility residue**:
  - [x] ~~Focus traps on modals~~ RESOLVED (R64) — new `useFocusTrap` hook applied to all 5 modals (GameOverModal, AchievementDisplay, PuzzleModal, SentientAIModal, CharacterConversationModal). Added `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, Escape-to-close, Tab cycling, and focus restoration.
  - [ ] CTRL-S World `aria-live` on story text (will be addressed by Phase 7 rewrite)
  - [x] ~~Form input labels~~ RESOLVED (R78.8) — added `id`/`htmlFor` pairs to 13 inputs across AudioSettings, ShatnerVoiceControls, AdvancedVoiceControls, PuzzleModal, Scoreboard; added `aria-label` to AchievementDisplay search. CTRL-S World inputs deferred to Phase 7.
- [x] ~~**Rhythm Hacker BPM tuning**~~ RESOLVED (R78.9) — Resonance corrected from 150→110 BPM (FL Studio confirmed), Cyberpunkin' from 120→118 (algorithmic consensus). All 5 track durations corrected to match actual audio lengths. Cyberpsychotic (140) and Enhancements (160) left for manual playtest verification.
- [x] ~~**`useAdvancedVoice` AudioContext** — never connected to speech output (always returns zeros). Either wire it up or delete the dead path.~~ RESOLVED (R64) — removed dead AudioContext, replaced with synthetic visualisation.

#### Phase 6 — Playwright E2E residue (from R50 bulk-up)

Baselines are committed as macOS `*-chromium-darwin.png`. Carried forward from [`PLAYWRIGHT_BULKUP.md`](PLAYWRIGHT_BULKUP.md):

- [x] ~~**Docker baseline regen for CI parity**~~ RESOLVED (R79.1) — 85 `*-chromium-linux.png` baselines generated and committed. Fixed Vite `allowedHosts` for Docker DNS.
- [x] ~~**Multi-viewport matrix**~~ RESOLVED (R78.7) — added mobile (375×667) + tablet (768×1024) projects. Both hit the "DESKTOP REQUIRED" gate; dedicated responsive spec covers this.
- [x] ~~**Per-game keyboard-only a11y playthroughs**~~ — done R58.
- [x] ~~**Performance budgets**~~ — done R58.
- [x] ~~**Flaky specs under full-suite parallelism**~~ RESOLVED (R78.10) — capped local workers at 4, increased game-over race timeout to 20s, bumped slow-game test timeouts to 90s, added focus-ring settle wait for landing card screenshot.

### Playtest Verification (from `rebuildingoldgames/bugs.md`)

Items the user re-flagged after the R50 playtest. R54 code audit confirms most are working correctly. P0 keyboard bug was fixed in R72 (cursor guards + waitForKeyboard retry).

- [ ] **Metris**: `B` bullet-time key — code-verified R54. **Needs live browser verification.**
- [ ] **Matrix Cloud combo bug** — code-verified R54. **Needs live browser verification.**
- [ ] **High Scores panel** — automated R58 via modals.spec.ts. **Needs user clarification on what the original "broken" report meant.**
- [ ] **Achievements panel redesign** — automated R58. **Needs user input on specific desired changes.**
- [ ] **Game card portal sizing** — code-verified R54. **P3 — cosmetic.**

---


## Deferred / Future Phases (post-R84)

These phases are scoped but NOT scheduled. Ralph does NOT touch them during R84.

### R85 — 8-game polish for untested games

Per-game polish for Matrix Invaders, Metris, Matrix Frogger, NeoJump, AgentChase, Rhythm Hacker, CloudJumper, CodeBreaker — contingent on Tom playtesting each and filling the respective `MANUAL_TESTING_CHECKLIST_*.md` Known Issues blocks (docs already template-populated by R83.G7). R85 task list built from those notes the same way R83/R84 were.

### R86+ — Performance + ship-readiness

Tom's note: *"the rest of the work will be performance and getting the games perfect"* — post per-game polish rounds, focus shifts to performance profiling, final polish sweeps, and ship-readiness (Lighthouse audits, Web Vitals tuning, licensed asset-pack swap per Future Asset Strategy, PWA audits).

---

## Architecture Notes (kept for Ralph's reference)

### Phaser Input Pattern (MUST follow)

All scenes that register keyboard input must use `waitForKeyboard()` from BaseScene:

```typescript
protected setupInput(): void {
  this.waitForKeyboard(() => {
    if (!this.input.keyboard) return;
    // ... register keys
  });
}
```

`waitForKeyboard()` polls every 50ms up to 10 times (500ms), then falls back to the scene's first `update` tick. This replaced the old single-retry `delayedCall(100)` pattern in R62.

**CRITICAL (R69, corrected R75)**: The `update()` method MUST guard against cursors/keys being undefined. Since `waitForKeyboard` is async, there is a window where `update()` fires before keys are registered. Two safe patterns:

```typescript
// Pattern 1: NARROW guard in update (REQUIRED for polling-based input)
// ⚠️ The guard MUST wrap ONLY input handling — NOT the entire update().
// Placing the guard at the top of update() freezes ALL game logic
// (physics, AI, rendering, animations) during the ~500ms keyboard init.
update(time: number, delta: number): void {
  if (this.isPaused) return;
  // Game logic runs unconditionally — no dependency on keyboard
  this.updatePhysics(delta);
  this.updateAI(delta);
  this.updateAnimations(delta);
  this.updateUI();
  // Input only when keys are ready
  if (this.cursors) {
    this.handleInput(delta);
  }
}

// Pattern 2: Event-driven input (PREFERRED — immune to race condition)
this.waitForKeyboard(() => {
  const key = this.input.keyboard!.addKey(KeyCodes.SPACE);
  key.on('down', () => this.jump());  // Fires only after key exists
});
```

**DO NOT** declare key fields with `!` (non-null assertion) and then read `.isDown` in `update()` without a guard — this throws a silent TypeError that kills the scene.

**DO NOT** place the cursor guard at the top of `update()` — this was the R72 mistake that caused the P1 freeze bug in 4 games.

### GAME_CONFIG Circular Import Pattern (CAUTION)

All Phaser games have a circular dependency: `config.ts` imports scene classes (for the Phaser config's scene array), and scene classes import `GAME_CONFIG` from `config.ts`. This is safe **only if** scene files never read `GAME_CONFIG` at module-evaluation time (i.e. at the top level of the file). All reads must be inside class methods, constructors, or lazy-evaluated functions.

**Bad** (TDZ crash):
```typescript
const { WIDTH, HEIGHT } = GAME_CONFIG;  // top-level — crashes
const C = GAME_CONFIG;                   // top-level alias — crashes
export const DATA = GAME_CONFIG.FOO.map(...);  // top-level computation — crashes
```

**Good** (safe):
```typescript
class GameScene extends BaseScene {
  private speed = GAME_CONFIG.SPEED;  // class field — evaluated at instantiation, not import
  create() {
    const { WIDTH } = GAME_CONFIG;    // inside method — safe
  }
}
```

### Physics Pattern (from phaser-gamedev skill)

- Use `body.blocked.down` (not `body.touching.down`) for grounded checks
- Reset velocity to zero each frame before applying directional input
- Static bodies require `refreshBody()` after positional change
- All movement MUST use delta: `this.player.x += this.speed * (delta / 1000);`
- **Spritesheet loading**: Always measure frame dimensions from the actual image before loading

### Sound Integration Pattern

Phaser games use React-side sound via the registry bridge:
- Scenes call `this.playSound(key)` which reads `SOUND_SYSTEM` from registry
- PhaserGame.tsx provides `useSoundSystem().playSFX` via registry
- Phaser's built-in audio system is NOT used

### Phaser Game Standard Structure

```
src/components/games/phaser/[GameName]/
  index.tsx            # React wrapper (PhaserGame)
  config.ts            # Phaser config, constants, achievement IDs
  scenes/
    BootScene.ts       # Load assets (procedural textures or from asset system)
    MenuScene.ts       # Title screen, controls (extends shared MenuScene)
    GameScene.ts       # Core gameplay (extends BaseScene)
    GameOverScene.ts   # Score, high score, restart (extends shared GameOverScene)
```

### Required Keyboard Controls (all games)

| Key | Action | Required |
|-----|--------|----------|
| ESC | Exit to menu | Yes |
| P | Pause/resume | Yes |
| R | Restart | Yes |
| ENTER/SPACE | Start game | Yes |
| Arrow keys | Primary movement | Yes |
| WASD | Alt movement | Recommended |
| M | Toggle mute | Recommended |

### Shutdown Cleanup Pattern (MUST follow — updated R61)

All GameScene `shutdown()` methods MUST include:
```typescript
shutdown(): void {
  this.stopBackgroundMusic();
  this.time?.removeAllEvents();      // Kill all pending delayedCall/loopedCall
  this.tweens?.killAll();            // Kill all active tweens
  // ... game-specific cleanup (destroy sprites, clear arrays) ...
  if (this.input.keyboard) {
    this.input.keyboard.removeAllKeys(true);
  }
  super.shutdown();                   // Call base class cleanup
}
```

**R62**: `BaseScene.shutdown()` now handles common cleanup (pause overlay, common input keys). MenuScene, GameOverScene, and all 11 GameScenes call `super.shutdown()`. RhythmHacker MenuScene also has its own `shutdown()`.

---

## Test Coverage Status

| Game | Type | Unit Test | E2E Playthrough | E2E Visual |
|------|------|-----------|-----------------|------------|
| CtrlSWorld | DOM | Yes | Yes | -- |
| SnakeClassic | Phaser | Yes | Yes | -- |
| VortexPong | Phaser | Yes | Yes | -- |
| MatrixCloud | Phaser | Yes | Yes | -- |
| MatrixInvaders | Phaser | Yes | Yes | -- |
| Metris | Phaser | Yes | Yes | -- |
| MatrixFrogger | Phaser | Yes | Yes | -- |
| NeoJump | Phaser | Yes | Yes | -- |
| AgentChase | Phaser | Yes | Yes | -- |
| RhythmHacker | Phaser | Yes | Yes | -- |
| CloudJumper | Phaser | Yes | Yes | -- |
| CodeBreaker | Phaser | Yes | Yes | -- |

Visual regression baselines exist for landing page and shared UI (modals, portal, achievements, settings) under `e2e/visual/`.

All Phaser games expose test state via `exposeTestState()`. E2E fixtures support both React and Phaser games. E2E uses `?test=1` URL param for deterministic `Math.random` (seeded mulberry32) and DOM ready markers.

**Test infrastructure stats (R79)**: 47 unit test files, 23 E2E spec files, 98 darwin visual baselines. Categories: playthrough (12), visual (6+), a11y (1), edge-cases (1), modals (1), performance (1), responsive (1). All E2E pass. FPS budget test opt-in only (`PLAYWRIGHT_PERF=1`). All 12 games have E2E playthrough coverage; CTRL-S World missing dedicated exit-to-portal test.

---

## Current Codebase Health

### Strengths
- Zero TODO/FIXME/HACK comments in src/
- Zero `@ts-ignore` in production code (5 in test files, all justified)
- TypeScript strict mode fully enabled (noUnusedLocals, noUnusedParameters)
- All 11 Phaser games expose test state via `exposeTestState()`
- 12 games total, 100% achievement integration
- Clean separation of concerns: React wrapper + Phaser scenes via registry pattern
- Single source of truth: GAME_REGISTRY in `src/data/gameRegistry.ts`
- Code-split lazy loading for all game components
- No orphaned legacy code (cleaned up in R15)
- All unit tests passing (47 test files), build clean
- All E2E tests passing (23 spec files: 12 playthroughs + visual + a11y + performance + responsive + modal specs, 98 darwin + 85 linux baselines)
- All 12 games have complete E2E playthrough coverage with 6-checkpoint visual baselines
- GAME_CONFIG TDZ crash resolved (commit `ee18028`)

### Open Gaps
- ~~**P1 (R73)**: Over-broad cursor guards in update()~~ RESOLVED (R78.11, commit `2d27b09`) — guards narrowed to wrap only `handleInput()` in all 4 games.
- ~~**P2 (R73)**: Metris wKey dead code~~ FALSE POSITIVE — wKey IS used at `GameScene.ts:752` as rotate-CW alias. Closed R78.11.
- ~~**P2 (R73)**: Control hints text invisible in MenuScene~~ RESOLVED (R78.11, commit `05d94d7`) — changed to `PRIMARY_HEX` with `setAlpha(0.3)`.
- ~~**P2 (R69)**: Dead `return () => {}` in `useSoundSystem.ts`~~ ALREADY RESOLVED — code no longer present. Closed R78.11.
- ~~**P2 (R71)**: Dead `enableTestMode` in `e2e/fixtures/test-utils.ts`~~ ALREADY RESOLVED — symbol removed. Closed R78.11.
- ~~**P2 (R71)**: 4 hollow test cases in `ShatnerVoiceControls.test.tsx`~~ RESOLVED (R76.1) — all 30+ tests now have real assertions.
- ~~**P2 (R71)**: E2E screenshot anomalies~~ STALE — all 12 playthrough specs now pass with 6-checkpoint screenshots each. The R71 anomalies were observed before the playthrough runner was rebuilt (R78.5). Cursor guard narrowing (R78.11) also improved timing. Closed.
- ~~**E2E gap**: No test covers MenuScene "press ENTER to start" flow~~ STALE — all playthrough specs already use `autoStart=false` (App.tsx:627), capture a MenuScene screenshot (checkpoint 02), then press ENTER to transition to GameScene. The keyboard-only a11y spec also explicitly tests this path for all 11 Phaser games. Closed.
- **CTRL-S World**: only DOM/React game; current implementation too complex for incremental fixes — Phase 7 rewrite deferred
- Remaining sprite polish per game (see Phase 0b `[~]` items). ~~Audio is biggest cross-cutting gap~~ RESOLVED (R78.1) — global SFX deployed to all 11 non-CTRL-S games.
- Rhythm Hacker BPM values partially verified (R78.9) — Cyberpsychotic (140) and Enhancements (160) still need manual playtest confirmation
- No global `AssetManager` yet — each game owns its own asset loading
- Circular dependency in all Phaser games (`config.ts` <-> scene files) — fragile but functional; documented in Architecture Notes

---

## Ralph Overnight Loop Protocol (R76)

**Purpose**: Run unattended overnight. Each iteration picks ONE task, implements it, runs gates, and commits. Loop terminates only when the TERMINATOR CONDITION below is met.

### Per-Iteration Workflow

1. **READ** `IMPLEMENTATION_PLAN.md` fully. Locate the first unchecked `[ ]` task in this strict order:
   1. `R76 Global Items` (G1 → G9 in number order — G1 before anything else).
   2. `R76 Per-Game Items` (PG1 → PG22 in number order, **but P0 items take precedence**: PG2, PG7, PG8, PG10, PG14, PG19 are pulled forward ahead of lower-P items).
   3. `R76 E2E Coverage` (E1 → E2).
   4. `Phase 6 Polish Residue`.
   5. `Phase 0b Asset Deployment` (skip CTRL-S asset items — those are Phase 7).
   6. `Phase 1/2 Global Asset System`.
   7. `Phase 7 CTRL-S Rewrite` — **DO NOT open in this loop**. Stop loop instead.
2. If no unchecked task found in scopes 1–6 → see **TERMINATOR CONDITION**.
3. **Plan + implement** the task: write code, update/add tests, update docs if needed.
4. **Run gates** in this order, stop on first failure:
   - `npm run lint`
   - `npm run build`
   - `npm test`
   - `npm run test:e2e` — only if change touches gameplay, UI, routing, or scene flow.
5. **On gate failure**: fix the regression in the same iteration (max 3 fix attempts). If still red after 3 attempts, mark the task `BLOCKED: <reason>` in-place, revert the change (`git reset --hard HEAD`), and move to the next task.
6. **Update `IMPLEMENTATION_PLAN.md`**:
   - Mark the task `[x]` with iteration tag, e.g. `- [x] (R76.N) Fix ...`.
   - Append a one-line delta under `## Status`.
   - If new issues surfaced during implementation, append under a new `### Discovered Work` sub-heading — do NOT mix with the task you just closed.
7. **Commit** with conventional message: `R76.N: <task-id> <short summary>`. Use HEREDOC for multi-line bodies. Never skip hooks.
8. **Invoke the `game-tester` agent** to verify no cross-game regressions.
9. **Loop** back to step 1.

### TERMINATOR CONDITION (Ralph stops looping only when ALL of these are true)

- All R82.1 through R82.12 tasks marked `[x]`.
- `npm run lint`, `npm run build`, `npm test`, `npm run test:e2e`, `npm run test:visual` ALL green on a clean run.
- `COMPLETED_WORK.md` contains new `## R81 — Arcade-wide Juice Polish` section (R82.1 archive).
- **CTRL-S game files untouched** — `git diff --stat HEAD~12 -- src/components/games/phaser/CtrlSWorld/` returns empty. (CTRL-S card styling in LandingPage.tsx IS permitted.)
- The `## Status` line contains the phrase **"R82 COMPLETE — iPod Classic card redesign shipped"**.
- `### R82 Completion Report` written under the R82 section.

**Execution order for this run** (15-loop cap, 12 tasks):
1. R82.1 Archive R81 (doc-only, cheap)
2. R82.2 Extract GameCard component (prerequisite for all visual work)
3. R82.3–R82.7 Build card visuals + clickwheel incrementally (device silhouette → screen + title → clickwheel → interactivity → animations)
4. R82.8 Apply across all 12 games
5. R82.9 CTRL-S card styling (card only, NOT Phaser game code)
6. R82.10–R82.11 Visual baselines + E2E spec update
7. R82.12 Final verification + terminator

**Pattern**: R82 uses standard complete-and-exit (like R76/R77/R79/R80/R81). Auto-terminator is correct.

**CTRL-S GAME directory is RE-ARMED off-limits** — only the CTRL-S card styling on the landing page is in scope. Phaser scene code for CTRL-S is untouchable.

**Stick-with-plan rule**: Don't invent new tasks. Discovered scope expansion (e.g. carousel overhaul, header redesign) → `### R82 Discovered Work` for triage, NOT into the active task list.

When terminator is reached, write `### R82 Completion Report` listing: iterations run, components created/refactored, visual baselines regenerated, any discovered items deferred to R83+, tests passing, accessibility verification. Then stop.

### Guardrails

- **DO NOT** open Phase 7 (CTRL-S rewrite) — deliberately deferred.
- **DO NOT** delete Phase 0b asset-pipeline work — it stays open post-R76.
- **DO NOT** skip gate steps even if the task "looks trivial".
- **DO NOT** commit on red. If a commit fails hooks, fix root cause — never `--no-verify`.
- **DO NOT** amend commits — always create a NEW commit for a fix (a failed hook means the commit didn't happen; amending would modify the PREVIOUS commit).
- Every 5 iterations: run full visual regression suite (`npm run test:visual`) and commit intentional baseline updates separately with message `R76.N-visual: baseline update`.
- If two consecutive tasks land `BLOCKED`, pause the loop and write a `⚠️ LOOP HEALTH ALERT` block under `## Status` before continuing — don't silently accumulate blockers.

### Task Ordering Rationale

- **G1–G3 first** because they are P0 global blockers (menu, BGM, achievements crash). G1 unblocks real playtesting.
- **G8 (countdown) directly after G1** — depends on MenuScene being visible.
- **G6 (pause/resume) next** — P1 but affects every game; landing it early means per-game P0s can be verified with reliable pause.
- **Per-game P0s (PG2, PG7, PG8, PG10, PG14, PG19) before P1s** — functional breakage over polish.
- **G4/G5 (card layout) and G7 (About page) slot whenever a per-game P0 is BLOCKED** — safe parallel fill-ins.
- **E1 E2E spec runs LAST** — requires everything above green. E2 visual baseline regen is absolute final step.

### Legacy Ralph Notes (R62–R75)

All items from the R75 plan have been resolved across R76–R78. See [COMPLETED_WORK.md](COMPLETED_WORK.md) for details. Items 1–13 were closed in R76–R78; items 14–15 remain as Phase 1/2 and Phase 7 entries in the active plan above.

**Skill usage**: `/matrix-arcade-gamedev` for game code, `/phaser-gamedev` for Phaser scenes, `/playwright-testing` for E2E.
Run `game-tester` agent after every code change. After any UI change, run `npm run test:visual` and commit updated baselines.
